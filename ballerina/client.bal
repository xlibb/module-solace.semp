// AUTO-GENERATED FILE. DO NOT MODIFY.
// This file is auto-generated by the Ballerina OpenAPI tool.

// Copyright (c) 2025, WSO2 LLC. (http://www.wso2.com).
//
// WSO2 LLC. licenses this file to you under the Apache License,
// Version 2.0 (the "License"); you may not use this file except
// in compliance with the License.
// You may obtain a copy of the License at
//
// http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing,
// software distributed under the License is distributed on an
// "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
// KIND, either express or implied.  See the License for the
// specific language governing permissions and limitations
// under the License.

import ballerina/data.jsondata;
import ballerina/http;

# SEMP (starting in `v2`) is a RESTful API for configuring, monitoring, and administering a Solace PubSub+ broker. This specification defines the following API:
#
#
# API|Base Path|Purpose
# :---|:---|:---
# Configuration|/SEMP/v2/config|Reading and writing configuration
#
#
#
# The following APIs are also available:
#
#
# API|Base Path|Purpose
# :---|:---|:---
# Action|/SEMP/v2/action|Performing actions
# Monitoring|/SEMP/v2/monitor|Querying operational state
#
#
#
# For tutorials, architectural and protocol design documentation, and other information about the SEMP API, consult the [SEMP documentation](https://docs.solace.com/Admin/SEMP/Using-SEMP.htm) on the Solace website. The SEMP API specifications are also [available for download](https://solace.com/downloads/).
#
# If you need additional support, please contact us at [support@solace.com](mailto:support@solace.com).
public isolated client class Client {
    final http:Client clientEp;

    # Gets invoked to initialize the `connector`.
    #
    # + config - The configurations to be used when initializing the `connector` 
    # + serviceUrl - URL of the target service 
    # + return - An error if connector initialization failed 
    public isolated function init(ConnectionConfig config, string serviceUrl = "https://www.solace.com/SEMP/v2/config") returns error? {
        http:ClientConfiguration httpClientConfig = {auth: config.auth, httpVersion: config.httpVersion, http1Settings: config.http1Settings, http2Settings: config.http2Settings, timeout: config.timeout, forwarded: config.forwarded, followRedirects: config.followRedirects, poolConfig: config.poolConfig, cache: config.cache, compression: config.compression, circuitBreaker: config.circuitBreaker, retryConfig: config.retryConfig, cookieConfig: config.cookieConfig, responseLimits: config.responseLimits, secureSocket: config.secureSocket, proxy: config.proxy, socketConfig: config.socketConfig, validation: config.validation, laxDataBinding: config.laxDataBinding};
        self.clientEp = check new (serviceUrl, httpClientConfig);
    }

    # Get a Broker object.
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The Broker object's attributes, and the request metadata. 
    remote isolated function getBroker(map<string|string[]> headers = {}, *GetBrokerQueries queries) returns BrokerResponse|error {
        string resourcePath = string `/`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Update a Broker object.
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Broker object's attributes. 
    # + return - The Broker object's attributes after being updated, and the request metadata. 
    remote isolated function updateBroker(Broker payload, map<string|string[]> headers = {}, *UpdateBrokerQueries queries) returns BrokerResponse|error {
        string resourcePath = string `/`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Get an About object.
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The About object's attributes, and the request metadata. 
    remote isolated function getAbout(map<string|string[]> headers = {}, *GetAboutQueries queries) returns AboutResponse|error {
        string resourcePath = string `/about`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Get an API Description object.
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The API Description object's attributes, and the request metadata. 
    remote isolated function getAboutApi(map<string|string[]> headers = {}, *GetAboutApiQueries queries) returns AboutApiResponse|error {
        string resourcePath = string `/about/api`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Get a User object.
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The User object's attributes, and the request metadata. 
    remote isolated function getAboutUser(map<string|string[]> headers = {}, *GetAboutUserQueries queries) returns AboutUserResponse|error {
        string resourcePath = string `/about/user`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Get a list of User Message VPN objects.
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The list of User Message VPN objects' attributes, and the request metadata. 
    remote isolated function getAboutUserMsgVpns(map<string|string[]> headers = {}, *GetAboutUserMsgVpnsQueries queries) returns AboutUserMsgVpnsResponse|error {
        string resourcePath = string `/about/user/msgVpns`;
        map<Encoding> queryParamEncoding = {"where": {style: FORM, explode: false}, "select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Get a User Message VPN object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The User Message VPN object's attributes, and the request metadata. 
    remote isolated function getAboutUserMsgVpn(string msgVpnName, map<string|string[]> headers = {}, *GetAboutUserMsgVpnQueries queries) returns AboutUserMsgVpnResponse|error {
        string resourcePath = string `/about/user/msgVpns/${getEncodedUri(msgVpnName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Get a list of Certificate Authority objects.
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The list of Certificate Authority objects' attributes, and the request metadata. 
    #
    # # Deprecated
    @deprecated
    remote isolated function getCertAuthorities(map<string|string[]> headers = {}, *GetCertAuthoritiesQueries queries) returns CertAuthoritiesResponse|error {
        string resourcePath = string `/certAuthorities`;
        map<Encoding> queryParamEncoding = {"where": {style: FORM, explode: false}, "select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create a Certificate Authority object.
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Certificate Authority object's attributes. 
    # + return - The Certificate Authority object's attributes after being created, and the request metadata. 
    #
    # # Deprecated
    @deprecated
    remote isolated function createCertAuthority(CertAuthority payload, map<string|string[]> headers = {}, *CreateCertAuthorityQueries queries) returns CertAuthorityResponse|error {
        string resourcePath = string `/certAuthorities`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get a Certificate Authority object.
    #
    # + certAuthorityName - The name of the Certificate Authority.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The Certificate Authority object's attributes, and the request metadata. 
    #
    # # Deprecated
    @deprecated
    remote isolated function getCertAuthority(string certAuthorityName, map<string|string[]> headers = {}, *GetCertAuthorityQueries queries) returns CertAuthorityResponse|error {
        string resourcePath = string `/certAuthorities/${getEncodedUri(certAuthorityName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Replace a Certificate Authority object.
    #
    # + certAuthorityName - The name of the Certificate Authority.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Certificate Authority object's attributes. 
    # + return - The Certificate Authority object's attributes after being replaced, and the request metadata. 
    #
    # # Deprecated
    @deprecated
    remote isolated function replaceCertAuthority(string certAuthorityName, CertAuthority payload, map<string|string[]> headers = {}, *ReplaceCertAuthorityQueries queries) returns CertAuthorityResponse|error {
        string resourcePath = string `/certAuthorities/${getEncodedUri(certAuthorityName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Delete a Certificate Authority object.
    #
    # + certAuthorityName - The name of the Certificate Authority.
    # + headers - Headers to be sent with the request 
    # + return - The request metadata. 
    #
    # # Deprecated
    @deprecated
    remote isolated function deleteCertAuthority(string certAuthorityName, map<string|string[]> headers = {}) returns SempMetaOnlyResponse|error {
        string resourcePath = string `/certAuthorities/${getEncodedUri(certAuthorityName)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Update a Certificate Authority object.
    #
    # + certAuthorityName - The name of the Certificate Authority.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Certificate Authority object's attributes. 
    # + return - The Certificate Authority object's attributes after being updated, and the request metadata. 
    #
    # # Deprecated
    @deprecated
    remote isolated function updateCertAuthority(string certAuthorityName, CertAuthority payload, map<string|string[]> headers = {}, *UpdateCertAuthorityQueries queries) returns CertAuthorityResponse|error {
        string resourcePath = string `/certAuthorities/${getEncodedUri(certAuthorityName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Get a list of OCSP Responder Trusted Common Name objects.
    #
    # + certAuthorityName - The name of the Certificate Authority.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The list of OCSP Responder Trusted Common Name objects' attributes, and the request metadata. 
    #
    # # Deprecated
    @deprecated
    remote isolated function getCertAuthorityOcspTlsTrustedCommonNames(string certAuthorityName, map<string|string[]> headers = {}, *GetCertAuthorityOcspTlsTrustedCommonNamesQueries queries) returns CertAuthorityOcspTlsTrustedCommonNamesResponse|error {
        string resourcePath = string `/certAuthorities/${getEncodedUri(certAuthorityName)}/ocspTlsTrustedCommonNames`;
        map<Encoding> queryParamEncoding = {"where": {style: FORM, explode: false}, "select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create an OCSP Responder Trusted Common Name object.
    #
    # + certAuthorityName - The name of the Certificate Authority.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The OCSP Responder Trusted Common Name object's attributes. 
    # + return - The OCSP Responder Trusted Common Name object's attributes after being created, and the request metadata. 
    #
    # # Deprecated
    @deprecated
    remote isolated function createCertAuthorityOcspTlsTrustedCommonName(string certAuthorityName, CertAuthorityOcspTlsTrustedCommonName payload, map<string|string[]> headers = {}, *CreateCertAuthorityOcspTlsTrustedCommonNameQueries queries) returns CertAuthorityOcspTlsTrustedCommonNameResponse|error {
        string resourcePath = string `/certAuthorities/${getEncodedUri(certAuthorityName)}/ocspTlsTrustedCommonNames`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get an OCSP Responder Trusted Common Name object.
    #
    # + certAuthorityName - The name of the Certificate Authority.
    # + ocspTlsTrustedCommonName - The expected Trusted Common Name of the OCSP responder remote certificate.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The OCSP Responder Trusted Common Name object's attributes, and the request metadata. 
    #
    # # Deprecated
    @deprecated
    remote isolated function getCertAuthorityOcspTlsTrustedCommonName(string certAuthorityName, string ocspTlsTrustedCommonName, map<string|string[]> headers = {}, *GetCertAuthorityOcspTlsTrustedCommonNameQueries queries) returns CertAuthorityOcspTlsTrustedCommonNameResponse|error {
        string resourcePath = string `/certAuthorities/${getEncodedUri(certAuthorityName)}/ocspTlsTrustedCommonNames/${getEncodedUri(ocspTlsTrustedCommonName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete an OCSP Responder Trusted Common Name object.
    #
    # + certAuthorityName - The name of the Certificate Authority.
    # + ocspTlsTrustedCommonName - The expected Trusted Common Name of the OCSP responder remote certificate.
    # + headers - Headers to be sent with the request 
    # + return - The request metadata. 
    #
    # # Deprecated
    @deprecated
    remote isolated function deleteCertAuthorityOcspTlsTrustedCommonName(string certAuthorityName, string ocspTlsTrustedCommonName, map<string|string[]> headers = {}) returns SempMetaOnlyResponse|error {
        string resourcePath = string `/certAuthorities/${getEncodedUri(certAuthorityName)}/ocspTlsTrustedCommonNames/${getEncodedUri(ocspTlsTrustedCommonName)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Get a list of Client Certificate Authority objects.
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The list of Client Certificate Authority objects' attributes, and the request metadata. 
    remote isolated function getClientCertAuthorities(map<string|string[]> headers = {}, *GetClientCertAuthoritiesQueries queries) returns ClientCertAuthoritiesResponse|error {
        string resourcePath = string `/clientCertAuthorities`;
        map<Encoding> queryParamEncoding = {"where": {style: FORM, explode: false}, "select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create a Client Certificate Authority object.
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Client Certificate Authority object's attributes. 
    # + return - The Client Certificate Authority object's attributes after being created, and the request metadata. 
    remote isolated function createClientCertAuthority(ClientCertAuthority payload, map<string|string[]> headers = {}, *CreateClientCertAuthorityQueries queries) returns ClientCertAuthorityResponse|error {
        string resourcePath = string `/clientCertAuthorities`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get a Client Certificate Authority object.
    #
    # + certAuthorityName - The name of the Certificate Authority.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The Client Certificate Authority object's attributes, and the request metadata. 
    remote isolated function getClientCertAuthority(string certAuthorityName, map<string|string[]> headers = {}, *GetClientCertAuthorityQueries queries) returns ClientCertAuthorityResponse|error {
        string resourcePath = string `/clientCertAuthorities/${getEncodedUri(certAuthorityName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Replace a Client Certificate Authority object.
    #
    # + certAuthorityName - The name of the Certificate Authority.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Client Certificate Authority object's attributes. 
    # + return - The Client Certificate Authority object's attributes after being replaced, and the request metadata. 
    remote isolated function replaceClientCertAuthority(string certAuthorityName, ClientCertAuthority payload, map<string|string[]> headers = {}, *ReplaceClientCertAuthorityQueries queries) returns ClientCertAuthorityResponse|error {
        string resourcePath = string `/clientCertAuthorities/${getEncodedUri(certAuthorityName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Delete a Client Certificate Authority object.
    #
    # + certAuthorityName - The name of the Certificate Authority.
    # + headers - Headers to be sent with the request 
    # + return - The request metadata. 
    remote isolated function deleteClientCertAuthority(string certAuthorityName, map<string|string[]> headers = {}) returns SempMetaOnlyResponse|error {
        string resourcePath = string `/clientCertAuthorities/${getEncodedUri(certAuthorityName)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Update a Client Certificate Authority object.
    #
    # + certAuthorityName - The name of the Certificate Authority.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Client Certificate Authority object's attributes. 
    # + return - The Client Certificate Authority object's attributes after being updated, and the request metadata. 
    remote isolated function updateClientCertAuthority(string certAuthorityName, ClientCertAuthority payload, map<string|string[]> headers = {}, *UpdateClientCertAuthorityQueries queries) returns ClientCertAuthorityResponse|error {
        string resourcePath = string `/clientCertAuthorities/${getEncodedUri(certAuthorityName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Get a list of OCSP Responder Trusted Common Name objects.
    #
    # + certAuthorityName - The name of the Certificate Authority.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The list of OCSP Responder Trusted Common Name objects' attributes, and the request metadata. 
    remote isolated function getClientCertAuthorityOcspTlsTrustedCommonNames(string certAuthorityName, map<string|string[]> headers = {}, *GetClientCertAuthorityOcspTlsTrustedCommonNamesQueries queries) returns ClientCertAuthorityOcspTlsTrustedCommonNamesResponse|error {
        string resourcePath = string `/clientCertAuthorities/${getEncodedUri(certAuthorityName)}/ocspTlsTrustedCommonNames`;
        map<Encoding> queryParamEncoding = {"where": {style: FORM, explode: false}, "select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create an OCSP Responder Trusted Common Name object.
    #
    # + certAuthorityName - The name of the Certificate Authority.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The OCSP Responder Trusted Common Name object's attributes. 
    # + return - The OCSP Responder Trusted Common Name object's attributes after being created, and the request metadata. 
    remote isolated function createClientCertAuthorityOcspTlsTrustedCommonName(string certAuthorityName, ClientCertAuthorityOcspTlsTrustedCommonName payload, map<string|string[]> headers = {}, *CreateClientCertAuthorityOcspTlsTrustedCommonNameQueries queries) returns ClientCertAuthorityOcspTlsTrustedCommonNameResponse|error {
        string resourcePath = string `/clientCertAuthorities/${getEncodedUri(certAuthorityName)}/ocspTlsTrustedCommonNames`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get an OCSP Responder Trusted Common Name object.
    #
    # + certAuthorityName - The name of the Certificate Authority.
    # + ocspTlsTrustedCommonName - The expected Trusted Common Name of the OCSP responder remote certificate.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The OCSP Responder Trusted Common Name object's attributes, and the request metadata. 
    remote isolated function getClientCertAuthorityOcspTlsTrustedCommonName(string certAuthorityName, string ocspTlsTrustedCommonName, map<string|string[]> headers = {}, *GetClientCertAuthorityOcspTlsTrustedCommonNameQueries queries) returns ClientCertAuthorityOcspTlsTrustedCommonNameResponse|error {
        string resourcePath = string `/clientCertAuthorities/${getEncodedUri(certAuthorityName)}/ocspTlsTrustedCommonNames/${getEncodedUri(ocspTlsTrustedCommonName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete an OCSP Responder Trusted Common Name object.
    #
    # + certAuthorityName - The name of the Certificate Authority.
    # + ocspTlsTrustedCommonName - The expected Trusted Common Name of the OCSP responder remote certificate.
    # + headers - Headers to be sent with the request 
    # + return - The request metadata. 
    remote isolated function deleteClientCertAuthorityOcspTlsTrustedCommonName(string certAuthorityName, string ocspTlsTrustedCommonName, map<string|string[]> headers = {}) returns SempMetaOnlyResponse|error {
        string resourcePath = string `/clientCertAuthorities/${getEncodedUri(certAuthorityName)}/ocspTlsTrustedCommonNames/${getEncodedUri(ocspTlsTrustedCommonName)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Get a list of Cluster objects.
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The list of Cluster objects' attributes, and the request metadata. 
    remote isolated function getDmrClusters(map<string|string[]> headers = {}, *GetDmrClustersQueries queries) returns DmrClustersResponse|error {
        string resourcePath = string `/dmrClusters`;
        map<Encoding> queryParamEncoding = {"where": {style: FORM, explode: false}, "select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create a Cluster object.
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Cluster object's attributes. 
    # + return - The Cluster object's attributes after being created, and the request metadata. 
    remote isolated function createDmrCluster(DmrCluster payload, map<string|string[]> headers = {}, *CreateDmrClusterQueries queries) returns DmrClusterResponse|error {
        string resourcePath = string `/dmrClusters`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get a Cluster object.
    #
    # + dmrClusterName - The name of the Cluster.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The Cluster object's attributes, and the request metadata. 
    remote isolated function getDmrCluster(string dmrClusterName, map<string|string[]> headers = {}, *GetDmrClusterQueries queries) returns DmrClusterResponse|error {
        string resourcePath = string `/dmrClusters/${getEncodedUri(dmrClusterName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Replace a Cluster object.
    #
    # + dmrClusterName - The name of the Cluster.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Cluster object's attributes. 
    # + return - The Cluster object's attributes after being replaced, and the request metadata. 
    remote isolated function replaceDmrCluster(string dmrClusterName, DmrCluster payload, map<string|string[]> headers = {}, *ReplaceDmrClusterQueries queries) returns DmrClusterResponse|error {
        string resourcePath = string `/dmrClusters/${getEncodedUri(dmrClusterName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Delete a Cluster object.
    #
    # + dmrClusterName - The name of the Cluster.
    # + headers - Headers to be sent with the request 
    # + return - The request metadata. 
    remote isolated function deleteDmrCluster(string dmrClusterName, map<string|string[]> headers = {}) returns SempMetaOnlyResponse|error {
        string resourcePath = string `/dmrClusters/${getEncodedUri(dmrClusterName)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Update a Cluster object.
    #
    # + dmrClusterName - The name of the Cluster.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Cluster object's attributes. 
    # + return - The Cluster object's attributes after being updated, and the request metadata. 
    remote isolated function updateDmrCluster(string dmrClusterName, DmrCluster payload, map<string|string[]> headers = {}, *UpdateDmrClusterQueries queries) returns DmrClusterResponse|error {
        string resourcePath = string `/dmrClusters/${getEncodedUri(dmrClusterName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Get a list of Certificate Matching Rule objects.
    #
    # + dmrClusterName - The name of the Cluster.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The list of Certificate Matching Rule objects' attributes, and the request metadata. 
    remote isolated function getDmrClusterCertMatchingRules(string dmrClusterName, map<string|string[]> headers = {}, *GetDmrClusterCertMatchingRulesQueries queries) returns DmrClusterCertMatchingRulesResponse|error {
        string resourcePath = string `/dmrClusters/${getEncodedUri(dmrClusterName)}/certMatchingRules`;
        map<Encoding> queryParamEncoding = {"where": {style: FORM, explode: false}, "select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create a Certificate Matching Rule object.
    #
    # + dmrClusterName - The name of the Cluster.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Certificate Matching Rule object's attributes. 
    # + return - The Certificate Matching Rule object's attributes after being created, and the request metadata. 
    remote isolated function createDmrClusterCertMatchingRule(string dmrClusterName, DmrClusterCertMatchingRule payload, map<string|string[]> headers = {}, *CreateDmrClusterCertMatchingRuleQueries queries) returns DmrClusterCertMatchingRuleResponse|error {
        string resourcePath = string `/dmrClusters/${getEncodedUri(dmrClusterName)}/certMatchingRules`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get a Certificate Matching Rule object.
    #
    # + dmrClusterName - The name of the Cluster.
    # + ruleName - The name of the rule.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The Certificate Matching Rule object's attributes, and the request metadata. 
    remote isolated function getDmrClusterCertMatchingRule(string dmrClusterName, string ruleName, map<string|string[]> headers = {}, *GetDmrClusterCertMatchingRuleQueries queries) returns DmrClusterCertMatchingRuleResponse|error {
        string resourcePath = string `/dmrClusters/${getEncodedUri(dmrClusterName)}/certMatchingRules/${getEncodedUri(ruleName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Replace a Certificate Matching Rule object.
    #
    # + dmrClusterName - The name of the Cluster.
    # + ruleName - The name of the rule.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Certificate Matching Rule object's attributes. 
    # + return - The Certificate Matching Rule object's attributes after being replaced, and the request metadata. 
    remote isolated function replaceDmrClusterCertMatchingRule(string dmrClusterName, string ruleName, DmrClusterCertMatchingRule payload, map<string|string[]> headers = {}, *ReplaceDmrClusterCertMatchingRuleQueries queries) returns DmrClusterCertMatchingRuleResponse|error {
        string resourcePath = string `/dmrClusters/${getEncodedUri(dmrClusterName)}/certMatchingRules/${getEncodedUri(ruleName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Delete a Certificate Matching Rule object.
    #
    # + dmrClusterName - The name of the Cluster.
    # + ruleName - The name of the rule.
    # + headers - Headers to be sent with the request 
    # + return - The request metadata. 
    remote isolated function deleteDmrClusterCertMatchingRule(string dmrClusterName, string ruleName, map<string|string[]> headers = {}) returns SempMetaOnlyResponse|error {
        string resourcePath = string `/dmrClusters/${getEncodedUri(dmrClusterName)}/certMatchingRules/${getEncodedUri(ruleName)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Update a Certificate Matching Rule object.
    #
    # + dmrClusterName - The name of the Cluster.
    # + ruleName - The name of the rule.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Certificate Matching Rule object's attributes. 
    # + return - The Certificate Matching Rule object's attributes after being updated, and the request metadata. 
    remote isolated function updateDmrClusterCertMatchingRule(string dmrClusterName, string ruleName, DmrClusterCertMatchingRule payload, map<string|string[]> headers = {}, *UpdateDmrClusterCertMatchingRuleQueries queries) returns DmrClusterCertMatchingRuleResponse|error {
        string resourcePath = string `/dmrClusters/${getEncodedUri(dmrClusterName)}/certMatchingRules/${getEncodedUri(ruleName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Get a list of Certificate Matching Rule Attribute Filter objects.
    #
    # + dmrClusterName - The name of the Cluster.
    # + ruleName - The name of the rule.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The list of Certificate Matching Rule Attribute Filter objects' attributes, and the request metadata. 
    remote isolated function getDmrClusterCertMatchingRuleAttributeFilters(string dmrClusterName, string ruleName, map<string|string[]> headers = {}, *GetDmrClusterCertMatchingRuleAttributeFiltersQueries queries) returns DmrClusterCertMatchingRuleAttributeFiltersResponse|error {
        string resourcePath = string `/dmrClusters/${getEncodedUri(dmrClusterName)}/certMatchingRules/${getEncodedUri(ruleName)}/attributeFilters`;
        map<Encoding> queryParamEncoding = {"where": {style: FORM, explode: false}, "select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create a Certificate Matching Rule Attribute Filter object.
    #
    # + dmrClusterName - The name of the Cluster.
    # + ruleName - The name of the rule.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Certificate Matching Rule Attribute Filter object's attributes. 
    # + return - The Certificate Matching Rule Attribute Filter object's attributes after being created, and the request metadata. 
    remote isolated function createDmrClusterCertMatchingRuleAttributeFilter(string dmrClusterName, string ruleName, DmrClusterCertMatchingRuleAttributeFilter payload, map<string|string[]> headers = {}, *CreateDmrClusterCertMatchingRuleAttributeFilterQueries queries) returns DmrClusterCertMatchingRuleAttributeFilterResponse|error {
        string resourcePath = string `/dmrClusters/${getEncodedUri(dmrClusterName)}/certMatchingRules/${getEncodedUri(ruleName)}/attributeFilters`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get a Certificate Matching Rule Attribute Filter object.
    #
    # + dmrClusterName - The name of the Cluster.
    # + ruleName - The name of the rule.
    # + filterName - The name of the filter.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The Certificate Matching Rule Attribute Filter object's attributes, and the request metadata. 
    remote isolated function getDmrClusterCertMatchingRuleAttributeFilter(string dmrClusterName, string ruleName, string filterName, map<string|string[]> headers = {}, *GetDmrClusterCertMatchingRuleAttributeFilterQueries queries) returns DmrClusterCertMatchingRuleAttributeFilterResponse|error {
        string resourcePath = string `/dmrClusters/${getEncodedUri(dmrClusterName)}/certMatchingRules/${getEncodedUri(ruleName)}/attributeFilters/${getEncodedUri(filterName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Replace a Certificate Matching Rule Attribute Filter object.
    #
    # + dmrClusterName - The name of the Cluster.
    # + ruleName - The name of the rule.
    # + filterName - The name of the filter.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Certificate Matching Rule Attribute Filter object's attributes. 
    # + return - The Certificate Matching Rule Attribute Filter object's attributes after being replaced, and the request metadata. 
    remote isolated function replaceDmrClusterCertMatchingRuleAttributeFilter(string dmrClusterName, string ruleName, string filterName, DmrClusterCertMatchingRuleAttributeFilter payload, map<string|string[]> headers = {}, *ReplaceDmrClusterCertMatchingRuleAttributeFilterQueries queries) returns DmrClusterCertMatchingRuleAttributeFilterResponse|error {
        string resourcePath = string `/dmrClusters/${getEncodedUri(dmrClusterName)}/certMatchingRules/${getEncodedUri(ruleName)}/attributeFilters/${getEncodedUri(filterName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Delete a Certificate Matching Rule Attribute Filter object.
    #
    # + dmrClusterName - The name of the Cluster.
    # + ruleName - The name of the rule.
    # + filterName - The name of the filter.
    # + headers - Headers to be sent with the request 
    # + return - The request metadata. 
    remote isolated function deleteDmrClusterCertMatchingRuleAttributeFilter(string dmrClusterName, string ruleName, string filterName, map<string|string[]> headers = {}) returns SempMetaOnlyResponse|error {
        string resourcePath = string `/dmrClusters/${getEncodedUri(dmrClusterName)}/certMatchingRules/${getEncodedUri(ruleName)}/attributeFilters/${getEncodedUri(filterName)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Update a Certificate Matching Rule Attribute Filter object.
    #
    # + dmrClusterName - The name of the Cluster.
    # + ruleName - The name of the rule.
    # + filterName - The name of the filter.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Certificate Matching Rule Attribute Filter object's attributes. 
    # + return - The Certificate Matching Rule Attribute Filter object's attributes after being updated, and the request metadata. 
    remote isolated function updateDmrClusterCertMatchingRuleAttributeFilter(string dmrClusterName, string ruleName, string filterName, DmrClusterCertMatchingRuleAttributeFilter payload, map<string|string[]> headers = {}, *UpdateDmrClusterCertMatchingRuleAttributeFilterQueries queries) returns DmrClusterCertMatchingRuleAttributeFilterResponse|error {
        string resourcePath = string `/dmrClusters/${getEncodedUri(dmrClusterName)}/certMatchingRules/${getEncodedUri(ruleName)}/attributeFilters/${getEncodedUri(filterName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Get a list of Certificate Matching Rule Condition objects.
    #
    # + dmrClusterName - The name of the Cluster.
    # + ruleName - The name of the rule.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The list of Certificate Matching Rule Condition objects' attributes, and the request metadata. 
    remote isolated function getDmrClusterCertMatchingRuleConditions(string dmrClusterName, string ruleName, map<string|string[]> headers = {}, *GetDmrClusterCertMatchingRuleConditionsQueries queries) returns DmrClusterCertMatchingRuleConditionsResponse|error {
        string resourcePath = string `/dmrClusters/${getEncodedUri(dmrClusterName)}/certMatchingRules/${getEncodedUri(ruleName)}/conditions`;
        map<Encoding> queryParamEncoding = {"where": {style: FORM, explode: false}, "select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create a Certificate Matching Rule Condition object.
    #
    # + dmrClusterName - The name of the Cluster.
    # + ruleName - The name of the rule.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Certificate Matching Rule Condition object's attributes. 
    # + return - The Certificate Matching Rule Condition object's attributes after being created, and the request metadata. 
    remote isolated function createDmrClusterCertMatchingRuleCondition(string dmrClusterName, string ruleName, DmrClusterCertMatchingRuleCondition payload, map<string|string[]> headers = {}, *CreateDmrClusterCertMatchingRuleConditionQueries queries) returns DmrClusterCertMatchingRuleConditionResponse|error {
        string resourcePath = string `/dmrClusters/${getEncodedUri(dmrClusterName)}/certMatchingRules/${getEncodedUri(ruleName)}/conditions`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get a Certificate Matching Rule Condition object.
    #
    # + dmrClusterName - The name of the Cluster.
    # + ruleName - The name of the rule.
    # + 'source - Certificate field to be compared with the Attribute.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The Certificate Matching Rule Condition object's attributes, and the request metadata. 
    remote isolated function getDmrClusterCertMatchingRuleCondition(string dmrClusterName, string ruleName, string 'source, map<string|string[]> headers = {}, *GetDmrClusterCertMatchingRuleConditionQueries queries) returns DmrClusterCertMatchingRuleConditionResponse|error {
        string resourcePath = string `/dmrClusters/${getEncodedUri(dmrClusterName)}/certMatchingRules/${getEncodedUri(ruleName)}/conditions/${getEncodedUri('source)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete a Certificate Matching Rule Condition object.
    #
    # + dmrClusterName - The name of the Cluster.
    # + ruleName - The name of the rule.
    # + 'source - Certificate field to be compared with the Attribute.
    # + headers - Headers to be sent with the request 
    # + return - The request metadata. 
    remote isolated function deleteDmrClusterCertMatchingRuleCondition(string dmrClusterName, string ruleName, string 'source, map<string|string[]> headers = {}) returns SempMetaOnlyResponse|error {
        string resourcePath = string `/dmrClusters/${getEncodedUri(dmrClusterName)}/certMatchingRules/${getEncodedUri(ruleName)}/conditions/${getEncodedUri('source)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Get a list of Link objects.
    #
    # + dmrClusterName - The name of the Cluster.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The list of Link objects' attributes, and the request metadata. 
    remote isolated function getDmrClusterLinks(string dmrClusterName, map<string|string[]> headers = {}, *GetDmrClusterLinksQueries queries) returns DmrClusterLinksResponse|error {
        string resourcePath = string `/dmrClusters/${getEncodedUri(dmrClusterName)}/links`;
        map<Encoding> queryParamEncoding = {"where": {style: FORM, explode: false}, "select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create a Link object.
    #
    # + dmrClusterName - The name of the Cluster.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Link object's attributes. 
    # + return - The Link object's attributes after being created, and the request metadata. 
    remote isolated function createDmrClusterLink(string dmrClusterName, DmrClusterLink payload, map<string|string[]> headers = {}, *CreateDmrClusterLinkQueries queries) returns DmrClusterLinkResponse|error {
        string resourcePath = string `/dmrClusters/${getEncodedUri(dmrClusterName)}/links`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get a Link object.
    #
    # + dmrClusterName - The name of the Cluster.
    # + remoteNodeName - The name of the node at the remote end of the Link.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The Link object's attributes, and the request metadata. 
    remote isolated function getDmrClusterLink(string dmrClusterName, string remoteNodeName, map<string|string[]> headers = {}, *GetDmrClusterLinkQueries queries) returns DmrClusterLinkResponse|error {
        string resourcePath = string `/dmrClusters/${getEncodedUri(dmrClusterName)}/links/${getEncodedUri(remoteNodeName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Replace a Link object.
    #
    # + dmrClusterName - The name of the Cluster.
    # + remoteNodeName - The name of the node at the remote end of the Link.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Link object's attributes. 
    # + return - The Link object's attributes after being replaced, and the request metadata. 
    remote isolated function replaceDmrClusterLink(string dmrClusterName, string remoteNodeName, DmrClusterLink payload, map<string|string[]> headers = {}, *ReplaceDmrClusterLinkQueries queries) returns DmrClusterLinkResponse|error {
        string resourcePath = string `/dmrClusters/${getEncodedUri(dmrClusterName)}/links/${getEncodedUri(remoteNodeName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Delete a Link object.
    #
    # + dmrClusterName - The name of the Cluster.
    # + remoteNodeName - The name of the node at the remote end of the Link.
    # + headers - Headers to be sent with the request 
    # + return - The request metadata. 
    remote isolated function deleteDmrClusterLink(string dmrClusterName, string remoteNodeName, map<string|string[]> headers = {}) returns SempMetaOnlyResponse|error {
        string resourcePath = string `/dmrClusters/${getEncodedUri(dmrClusterName)}/links/${getEncodedUri(remoteNodeName)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Update a Link object.
    #
    # + dmrClusterName - The name of the Cluster.
    # + remoteNodeName - The name of the node at the remote end of the Link.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Link object's attributes. 
    # + return - The Link object's attributes after being updated, and the request metadata. 
    remote isolated function updateDmrClusterLink(string dmrClusterName, string remoteNodeName, DmrClusterLink payload, map<string|string[]> headers = {}, *UpdateDmrClusterLinkQueries queries) returns DmrClusterLinkResponse|error {
        string resourcePath = string `/dmrClusters/${getEncodedUri(dmrClusterName)}/links/${getEncodedUri(remoteNodeName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Get a list of Link Attribute objects.
    #
    # + dmrClusterName - The name of the Cluster.
    # + remoteNodeName - The name of the node at the remote end of the Link.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The list of Link Attribute objects' attributes, and the request metadata. 
    remote isolated function getDmrClusterLinkAttributes(string dmrClusterName, string remoteNodeName, map<string|string[]> headers = {}, *GetDmrClusterLinkAttributesQueries queries) returns DmrClusterLinkAttributesResponse|error {
        string resourcePath = string `/dmrClusters/${getEncodedUri(dmrClusterName)}/links/${getEncodedUri(remoteNodeName)}/attributes`;
        map<Encoding> queryParamEncoding = {"where": {style: FORM, explode: false}, "select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create a Link Attribute object.
    #
    # + dmrClusterName - The name of the Cluster.
    # + remoteNodeName - The name of the node at the remote end of the Link.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Link Attribute object's attributes. 
    # + return - The Link Attribute object's attributes after being created, and the request metadata. 
    remote isolated function createDmrClusterLinkAttribute(string dmrClusterName, string remoteNodeName, DmrClusterLinkAttribute payload, map<string|string[]> headers = {}, *CreateDmrClusterLinkAttributeQueries queries) returns DmrClusterLinkAttributeResponse|error {
        string resourcePath = string `/dmrClusters/${getEncodedUri(dmrClusterName)}/links/${getEncodedUri(remoteNodeName)}/attributes`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get a Link Attribute object.
    #
    # + dmrClusterName - The name of the Cluster.
    # + remoteNodeName - The name of the node at the remote end of the Link.
    # + attributeName - The name of the Attribute.
    # + attributeValue - The value of the Attribute.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The Link Attribute object's attributes, and the request metadata. 
    remote isolated function getDmrClusterLinkAttribute(string dmrClusterName, string remoteNodeName, string attributeName, string attributeValue, map<string|string[]> headers = {}, *GetDmrClusterLinkAttributeQueries queries) returns DmrClusterLinkAttributeResponse|error {
        string resourcePath = string `/dmrClusters/${getEncodedUri(dmrClusterName)}/links/${getEncodedUri(remoteNodeName)}/attributes/${getEncodedUri(attributeName)},${getEncodedUri(attributeValue)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete a Link Attribute object.
    #
    # + dmrClusterName - The name of the Cluster.
    # + remoteNodeName - The name of the node at the remote end of the Link.
    # + attributeName - The name of the Attribute.
    # + attributeValue - The value of the Attribute.
    # + headers - Headers to be sent with the request 
    # + return - The request metadata. 
    remote isolated function deleteDmrClusterLinkAttribute(string dmrClusterName, string remoteNodeName, string attributeName, string attributeValue, map<string|string[]> headers = {}) returns SempMetaOnlyResponse|error {
        string resourcePath = string `/dmrClusters/${getEncodedUri(dmrClusterName)}/links/${getEncodedUri(remoteNodeName)}/attributes/${getEncodedUri(attributeName)},${getEncodedUri(attributeValue)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Get a list of Remote Address objects.
    #
    # + dmrClusterName - The name of the Cluster.
    # + remoteNodeName - The name of the node at the remote end of the Link.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The list of Remote Address objects' attributes, and the request metadata. 
    remote isolated function getDmrClusterLinkRemoteAddresses(string dmrClusterName, string remoteNodeName, map<string|string[]> headers = {}, *GetDmrClusterLinkRemoteAddressesQueries queries) returns DmrClusterLinkRemoteAddressesResponse|error {
        string resourcePath = string `/dmrClusters/${getEncodedUri(dmrClusterName)}/links/${getEncodedUri(remoteNodeName)}/remoteAddresses`;
        map<Encoding> queryParamEncoding = {"where": {style: FORM, explode: false}, "select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create a Remote Address object.
    #
    # + dmrClusterName - The name of the Cluster.
    # + remoteNodeName - The name of the node at the remote end of the Link.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Remote Address object's attributes. 
    # + return - The Remote Address object's attributes after being created, and the request metadata. 
    remote isolated function createDmrClusterLinkRemoteAddress(string dmrClusterName, string remoteNodeName, DmrClusterLinkRemoteAddress payload, map<string|string[]> headers = {}, *CreateDmrClusterLinkRemoteAddressQueries queries) returns DmrClusterLinkRemoteAddressResponse|error {
        string resourcePath = string `/dmrClusters/${getEncodedUri(dmrClusterName)}/links/${getEncodedUri(remoteNodeName)}/remoteAddresses`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get a Remote Address object.
    #
    # + dmrClusterName - The name of the Cluster.
    # + remoteNodeName - The name of the node at the remote end of the Link.
    # + remoteAddress - The FQDN or IP address (and optional port) of the remote node. If a port is not provided, it will vary based on the transport encoding: 55555 (plain-text), 55443 (encrypted), or 55003 (compressed).
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The Remote Address object's attributes, and the request metadata. 
    remote isolated function getDmrClusterLinkRemoteAddress(string dmrClusterName, string remoteNodeName, string remoteAddress, map<string|string[]> headers = {}, *GetDmrClusterLinkRemoteAddressQueries queries) returns DmrClusterLinkRemoteAddressResponse|error {
        string resourcePath = string `/dmrClusters/${getEncodedUri(dmrClusterName)}/links/${getEncodedUri(remoteNodeName)}/remoteAddresses/${getEncodedUri(remoteAddress)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete a Remote Address object.
    #
    # + dmrClusterName - The name of the Cluster.
    # + remoteNodeName - The name of the node at the remote end of the Link.
    # + remoteAddress - The FQDN or IP address (and optional port) of the remote node. If a port is not provided, it will vary based on the transport encoding: 55555 (plain-text), 55443 (encrypted), or 55003 (compressed).
    # + headers - Headers to be sent with the request 
    # + return - The request metadata. 
    remote isolated function deleteDmrClusterLinkRemoteAddress(string dmrClusterName, string remoteNodeName, string remoteAddress, map<string|string[]> headers = {}) returns SempMetaOnlyResponse|error {
        string resourcePath = string `/dmrClusters/${getEncodedUri(dmrClusterName)}/links/${getEncodedUri(remoteNodeName)}/remoteAddresses/${getEncodedUri(remoteAddress)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Get a list of Trusted Common Name objects.
    #
    # + dmrClusterName - The name of the Cluster.
    # + remoteNodeName - The name of the node at the remote end of the Link.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The list of Trusted Common Name objects' attributes, and the request metadata. 
    #
    # # Deprecated
    @deprecated
    remote isolated function getDmrClusterLinkTlsTrustedCommonNames(string dmrClusterName, string remoteNodeName, map<string|string[]> headers = {}, *GetDmrClusterLinkTlsTrustedCommonNamesQueries queries) returns DmrClusterLinkTlsTrustedCommonNamesResponse|error {
        string resourcePath = string `/dmrClusters/${getEncodedUri(dmrClusterName)}/links/${getEncodedUri(remoteNodeName)}/tlsTrustedCommonNames`;
        map<Encoding> queryParamEncoding = {"where": {style: FORM, explode: false}, "select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create a Trusted Common Name object.
    #
    # + dmrClusterName - The name of the Cluster.
    # + remoteNodeName - The name of the node at the remote end of the Link.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Trusted Common Name object's attributes. 
    # + return - The Trusted Common Name object's attributes after being created, and the request metadata. 
    #
    # # Deprecated
    @deprecated
    remote isolated function createDmrClusterLinkTlsTrustedCommonName(string dmrClusterName, string remoteNodeName, DmrClusterLinkTlsTrustedCommonName payload, map<string|string[]> headers = {}, *CreateDmrClusterLinkTlsTrustedCommonNameQueries queries) returns DmrClusterLinkTlsTrustedCommonNameResponse|error {
        string resourcePath = string `/dmrClusters/${getEncodedUri(dmrClusterName)}/links/${getEncodedUri(remoteNodeName)}/tlsTrustedCommonNames`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get a Trusted Common Name object.
    #
    # + dmrClusterName - The name of the Cluster.
    # + remoteNodeName - The name of the node at the remote end of the Link.
    # + tlsTrustedCommonName - The expected trusted common name of the remote certificate.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The Trusted Common Name object's attributes, and the request metadata. 
    #
    # # Deprecated
    @deprecated
    remote isolated function getDmrClusterLinkTlsTrustedCommonName(string dmrClusterName, string remoteNodeName, string tlsTrustedCommonName, map<string|string[]> headers = {}, *GetDmrClusterLinkTlsTrustedCommonNameQueries queries) returns DmrClusterLinkTlsTrustedCommonNameResponse|error {
        string resourcePath = string `/dmrClusters/${getEncodedUri(dmrClusterName)}/links/${getEncodedUri(remoteNodeName)}/tlsTrustedCommonNames/${getEncodedUri(tlsTrustedCommonName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete a Trusted Common Name object.
    #
    # + dmrClusterName - The name of the Cluster.
    # + remoteNodeName - The name of the node at the remote end of the Link.
    # + tlsTrustedCommonName - The expected trusted common name of the remote certificate.
    # + headers - Headers to be sent with the request 
    # + return - The request metadata. 
    #
    # # Deprecated
    @deprecated
    remote isolated function deleteDmrClusterLinkTlsTrustedCommonName(string dmrClusterName, string remoteNodeName, string tlsTrustedCommonName, map<string|string[]> headers = {}) returns SempMetaOnlyResponse|error {
        string resourcePath = string `/dmrClusters/${getEncodedUri(dmrClusterName)}/links/${getEncodedUri(remoteNodeName)}/tlsTrustedCommonNames/${getEncodedUri(tlsTrustedCommonName)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Get a list of Domain Certificate Authority objects.
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The list of Domain Certificate Authority objects' attributes, and the request metadata. 
    remote isolated function getDomainCertAuthorities(map<string|string[]> headers = {}, *GetDomainCertAuthoritiesQueries queries) returns DomainCertAuthoritiesResponse|error {
        string resourcePath = string `/domainCertAuthorities`;
        map<Encoding> queryParamEncoding = {"where": {style: FORM, explode: false}, "select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create a Domain Certificate Authority object.
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Domain Certificate Authority object's attributes. 
    # + return - The Domain Certificate Authority object's attributes after being created, and the request metadata. 
    remote isolated function createDomainCertAuthority(DomainCertAuthority payload, map<string|string[]> headers = {}, *CreateDomainCertAuthorityQueries queries) returns DomainCertAuthorityResponse|error {
        string resourcePath = string `/domainCertAuthorities`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get a Domain Certificate Authority object.
    #
    # + certAuthorityName - The name of the Certificate Authority.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The Domain Certificate Authority object's attributes, and the request metadata. 
    remote isolated function getDomainCertAuthority(string certAuthorityName, map<string|string[]> headers = {}, *GetDomainCertAuthorityQueries queries) returns DomainCertAuthorityResponse|error {
        string resourcePath = string `/domainCertAuthorities/${getEncodedUri(certAuthorityName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Replace a Domain Certificate Authority object.
    #
    # + certAuthorityName - The name of the Certificate Authority.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Domain Certificate Authority object's attributes. 
    # + return - The Domain Certificate Authority object's attributes after being replaced, and the request metadata. 
    remote isolated function replaceDomainCertAuthority(string certAuthorityName, DomainCertAuthority payload, map<string|string[]> headers = {}, *ReplaceDomainCertAuthorityQueries queries) returns DomainCertAuthorityResponse|error {
        string resourcePath = string `/domainCertAuthorities/${getEncodedUri(certAuthorityName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Delete a Domain Certificate Authority object.
    #
    # + certAuthorityName - The name of the Certificate Authority.
    # + headers - Headers to be sent with the request 
    # + return - The request metadata. 
    remote isolated function deleteDomainCertAuthority(string certAuthorityName, map<string|string[]> headers = {}) returns SempMetaOnlyResponse|error {
        string resourcePath = string `/domainCertAuthorities/${getEncodedUri(certAuthorityName)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Update a Domain Certificate Authority object.
    #
    # + certAuthorityName - The name of the Certificate Authority.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Domain Certificate Authority object's attributes. 
    # + return - The Domain Certificate Authority object's attributes after being updated, and the request metadata. 
    remote isolated function updateDomainCertAuthority(string certAuthorityName, DomainCertAuthority payload, map<string|string[]> headers = {}, *UpdateDomainCertAuthorityQueries queries) returns DomainCertAuthorityResponse|error {
        string resourcePath = string `/domainCertAuthorities/${getEncodedUri(certAuthorityName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Get a list of Message VPN objects.
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The list of Message VPN objects' attributes, and the request metadata. 
    remote isolated function getMsgVpns(map<string|string[]> headers = {}, *GetMsgVpnsQueries queries) returns MsgVpnsResponse|error {
        string resourcePath = string `/msgVpns`;
        map<Encoding> queryParamEncoding = {"where": {style: FORM, explode: false}, "select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create a Message VPN object.
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Message VPN object's attributes. 
    # + return - The Message VPN object's attributes after being created, and the request metadata. 
    remote isolated function createMsgVpn(MsgVpn payload, map<string|string[]> headers = {}, *CreateMsgVpnQueries queries) returns MsgVpnResponse|error {
        string resourcePath = string `/msgVpns`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get a Message VPN object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The Message VPN object's attributes, and the request metadata. 
    remote isolated function getMsgVpn(string msgVpnName, map<string|string[]> headers = {}, *GetMsgVpnQueries queries) returns MsgVpnResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Replace a Message VPN object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Message VPN object's attributes. 
    # + return - The Message VPN object's attributes after being replaced, and the request metadata. 
    remote isolated function replaceMsgVpn(string msgVpnName, MsgVpn payload, map<string|string[]> headers = {}, *ReplaceMsgVpnQueries queries) returns MsgVpnResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Delete a Message VPN object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + headers - Headers to be sent with the request 
    # + return - The request metadata. 
    remote isolated function deleteMsgVpn(string msgVpnName, map<string|string[]> headers = {}) returns SempMetaOnlyResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Update a Message VPN object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Message VPN object's attributes. 
    # + return - The Message VPN object's attributes after being updated, and the request metadata. 
    remote isolated function updateMsgVpn(string msgVpnName, MsgVpn payload, map<string|string[]> headers = {}, *UpdateMsgVpnQueries queries) returns MsgVpnResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Get a list of ACL Profile objects.
    #
    # + msgVpnName - The name of the Message VPN.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The list of ACL Profile objects' attributes, and the request metadata. 
    remote isolated function getMsgVpnAclProfiles(string msgVpnName, map<string|string[]> headers = {}, *GetMsgVpnAclProfilesQueries queries) returns MsgVpnAclProfilesResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/aclProfiles`;
        map<Encoding> queryParamEncoding = {"where": {style: FORM, explode: false}, "select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create an ACL Profile object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The ACL Profile object's attributes. 
    # + return - The ACL Profile object's attributes after being created, and the request metadata. 
    remote isolated function createMsgVpnAclProfile(string msgVpnName, MsgVpnAclProfile payload, map<string|string[]> headers = {}, *CreateMsgVpnAclProfileQueries queries) returns MsgVpnAclProfileResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/aclProfiles`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get an ACL Profile object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + aclProfileName - The name of the ACL Profile.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The ACL Profile object's attributes, and the request metadata. 
    remote isolated function getMsgVpnAclProfile(string msgVpnName, string aclProfileName, map<string|string[]> headers = {}, *GetMsgVpnAclProfileQueries queries) returns MsgVpnAclProfileResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/aclProfiles/${getEncodedUri(aclProfileName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Replace an ACL Profile object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + aclProfileName - The name of the ACL Profile.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The ACL Profile object's attributes. 
    # + return - The ACL Profile object's attributes after being replaced, and the request metadata. 
    remote isolated function replaceMsgVpnAclProfile(string msgVpnName, string aclProfileName, MsgVpnAclProfile payload, map<string|string[]> headers = {}, *ReplaceMsgVpnAclProfileQueries queries) returns MsgVpnAclProfileResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/aclProfiles/${getEncodedUri(aclProfileName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Delete an ACL Profile object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + aclProfileName - The name of the ACL Profile.
    # + headers - Headers to be sent with the request 
    # + return - The request metadata. 
    remote isolated function deleteMsgVpnAclProfile(string msgVpnName, string aclProfileName, map<string|string[]> headers = {}) returns SempMetaOnlyResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/aclProfiles/${getEncodedUri(aclProfileName)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Update an ACL Profile object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + aclProfileName - The name of the ACL Profile.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The ACL Profile object's attributes. 
    # + return - The ACL Profile object's attributes after being updated, and the request metadata. 
    remote isolated function updateMsgVpnAclProfile(string msgVpnName, string aclProfileName, MsgVpnAclProfile payload, map<string|string[]> headers = {}, *UpdateMsgVpnAclProfileQueries queries) returns MsgVpnAclProfileResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/aclProfiles/${getEncodedUri(aclProfileName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Get a list of Client Connect Exception objects.
    #
    # + msgVpnName - The name of the Message VPN.
    # + aclProfileName - The name of the ACL Profile.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The list of Client Connect Exception objects' attributes, and the request metadata. 
    remote isolated function getMsgVpnAclProfileClientConnectExceptions(string msgVpnName, string aclProfileName, map<string|string[]> headers = {}, *GetMsgVpnAclProfileClientConnectExceptionsQueries queries) returns MsgVpnAclProfileClientConnectExceptionsResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/aclProfiles/${getEncodedUri(aclProfileName)}/clientConnectExceptions`;
        map<Encoding> queryParamEncoding = {"where": {style: FORM, explode: false}, "select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create a Client Connect Exception object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + aclProfileName - The name of the ACL Profile.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Client Connect Exception object's attributes. 
    # + return - The Client Connect Exception object's attributes after being created, and the request metadata. 
    remote isolated function createMsgVpnAclProfileClientConnectException(string msgVpnName, string aclProfileName, MsgVpnAclProfileClientConnectException payload, map<string|string[]> headers = {}, *CreateMsgVpnAclProfileClientConnectExceptionQueries queries) returns MsgVpnAclProfileClientConnectExceptionResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/aclProfiles/${getEncodedUri(aclProfileName)}/clientConnectExceptions`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get a Client Connect Exception object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + aclProfileName - The name of the ACL Profile.
    # + clientConnectExceptionAddress - The IP address/netmask of the client connect exception in canonical CIDR form.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The Client Connect Exception object's attributes, and the request metadata. 
    remote isolated function getMsgVpnAclProfileClientConnectException(string msgVpnName, string aclProfileName, string clientConnectExceptionAddress, map<string|string[]> headers = {}, *GetMsgVpnAclProfileClientConnectExceptionQueries queries) returns MsgVpnAclProfileClientConnectExceptionResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/aclProfiles/${getEncodedUri(aclProfileName)}/clientConnectExceptions/${getEncodedUri(clientConnectExceptionAddress)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete a Client Connect Exception object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + aclProfileName - The name of the ACL Profile.
    # + clientConnectExceptionAddress - The IP address/netmask of the client connect exception in canonical CIDR form.
    # + headers - Headers to be sent with the request 
    # + return - The request metadata. 
    remote isolated function deleteMsgVpnAclProfileClientConnectException(string msgVpnName, string aclProfileName, string clientConnectExceptionAddress, map<string|string[]> headers = {}) returns SempMetaOnlyResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/aclProfiles/${getEncodedUri(aclProfileName)}/clientConnectExceptions/${getEncodedUri(clientConnectExceptionAddress)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Get a list of Publish Topic Exception objects.
    #
    # + msgVpnName - The name of the Message VPN.
    # + aclProfileName - The name of the ACL Profile.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The list of Publish Topic Exception objects' attributes, and the request metadata. 
    #
    # # Deprecated
    @deprecated
    remote isolated function getMsgVpnAclProfilePublishExceptions(string msgVpnName, string aclProfileName, map<string|string[]> headers = {}, *GetMsgVpnAclProfilePublishExceptionsQueries queries) returns MsgVpnAclProfilePublishExceptionsResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/aclProfiles/${getEncodedUri(aclProfileName)}/publishExceptions`;
        map<Encoding> queryParamEncoding = {"where": {style: FORM, explode: false}, "select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create a Publish Topic Exception object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + aclProfileName - The name of the ACL Profile.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Publish Topic Exception object's attributes. 
    # + return - The Publish Topic Exception object's attributes after being created, and the request metadata. 
    #
    # # Deprecated
    @deprecated
    remote isolated function createMsgVpnAclProfilePublishException(string msgVpnName, string aclProfileName, MsgVpnAclProfilePublishException payload, map<string|string[]> headers = {}, *CreateMsgVpnAclProfilePublishExceptionQueries queries) returns MsgVpnAclProfilePublishExceptionResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/aclProfiles/${getEncodedUri(aclProfileName)}/publishExceptions`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get a Publish Topic Exception object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + aclProfileName - The name of the ACL Profile.
    # + topicSyntax - The syntax of the topic for the exception to the default action taken.
    # + publishExceptionTopic - The topic for the exception to the default action taken. May include wildcard characters.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The Publish Topic Exception object's attributes, and the request metadata. 
    #
    # # Deprecated
    @deprecated
    remote isolated function getMsgVpnAclProfilePublishException(string msgVpnName, string aclProfileName, string topicSyntax, string publishExceptionTopic, map<string|string[]> headers = {}, *GetMsgVpnAclProfilePublishExceptionQueries queries) returns MsgVpnAclProfilePublishExceptionResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/aclProfiles/${getEncodedUri(aclProfileName)}/publishExceptions/${getEncodedUri(topicSyntax)},${getEncodedUri(publishExceptionTopic)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete a Publish Topic Exception object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + aclProfileName - The name of the ACL Profile.
    # + topicSyntax - The syntax of the topic for the exception to the default action taken.
    # + publishExceptionTopic - The topic for the exception to the default action taken. May include wildcard characters.
    # + headers - Headers to be sent with the request 
    # + return - The request metadata. 
    #
    # # Deprecated
    @deprecated
    remote isolated function deleteMsgVpnAclProfilePublishException(string msgVpnName, string aclProfileName, string topicSyntax, string publishExceptionTopic, map<string|string[]> headers = {}) returns SempMetaOnlyResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/aclProfiles/${getEncodedUri(aclProfileName)}/publishExceptions/${getEncodedUri(topicSyntax)},${getEncodedUri(publishExceptionTopic)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Get a list of Publish Topic Exception objects.
    #
    # + msgVpnName - The name of the Message VPN.
    # + aclProfileName - The name of the ACL Profile.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The list of Publish Topic Exception objects' attributes, and the request metadata. 
    remote isolated function getMsgVpnAclProfilePublishTopicExceptions(string msgVpnName, string aclProfileName, map<string|string[]> headers = {}, *GetMsgVpnAclProfilePublishTopicExceptionsQueries queries) returns MsgVpnAclProfilePublishTopicExceptionsResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/aclProfiles/${getEncodedUri(aclProfileName)}/publishTopicExceptions`;
        map<Encoding> queryParamEncoding = {"where": {style: FORM, explode: false}, "select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create a Publish Topic Exception object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + aclProfileName - The name of the ACL Profile.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Publish Topic Exception object's attributes. 
    # + return - The Publish Topic Exception object's attributes after being created, and the request metadata. 
    remote isolated function createMsgVpnAclProfilePublishTopicException(string msgVpnName, string aclProfileName, MsgVpnAclProfilePublishTopicException payload, map<string|string[]> headers = {}, *CreateMsgVpnAclProfilePublishTopicExceptionQueries queries) returns MsgVpnAclProfilePublishTopicExceptionResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/aclProfiles/${getEncodedUri(aclProfileName)}/publishTopicExceptions`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get a Publish Topic Exception object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + aclProfileName - The name of the ACL Profile.
    # + publishTopicExceptionSyntax - The syntax of the topic for the exception to the default action taken.
    # + publishTopicException - The topic for the exception to the default action taken. May include wildcard characters.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The Publish Topic Exception object's attributes, and the request metadata. 
    remote isolated function getMsgVpnAclProfilePublishTopicException(string msgVpnName, string aclProfileName, string publishTopicExceptionSyntax, string publishTopicException, map<string|string[]> headers = {}, *GetMsgVpnAclProfilePublishTopicExceptionQueries queries) returns MsgVpnAclProfilePublishTopicExceptionResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/aclProfiles/${getEncodedUri(aclProfileName)}/publishTopicExceptions/${getEncodedUri(publishTopicExceptionSyntax)},${getEncodedUri(publishTopicException)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete a Publish Topic Exception object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + aclProfileName - The name of the ACL Profile.
    # + publishTopicExceptionSyntax - The syntax of the topic for the exception to the default action taken.
    # + publishTopicException - The topic for the exception to the default action taken. May include wildcard characters.
    # + headers - Headers to be sent with the request 
    # + return - The request metadata. 
    remote isolated function deleteMsgVpnAclProfilePublishTopicException(string msgVpnName, string aclProfileName, string publishTopicExceptionSyntax, string publishTopicException, map<string|string[]> headers = {}) returns SempMetaOnlyResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/aclProfiles/${getEncodedUri(aclProfileName)}/publishTopicExceptions/${getEncodedUri(publishTopicExceptionSyntax)},${getEncodedUri(publishTopicException)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Get a list of Subscribe Topic Exception objects.
    #
    # + msgVpnName - The name of the Message VPN.
    # + aclProfileName - The name of the ACL Profile.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The list of Subscribe Topic Exception objects' attributes, and the request metadata. 
    #
    # # Deprecated
    @deprecated
    remote isolated function getMsgVpnAclProfileSubscribeExceptions(string msgVpnName, string aclProfileName, map<string|string[]> headers = {}, *GetMsgVpnAclProfileSubscribeExceptionsQueries queries) returns MsgVpnAclProfileSubscribeExceptionsResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/aclProfiles/${getEncodedUri(aclProfileName)}/subscribeExceptions`;
        map<Encoding> queryParamEncoding = {"where": {style: FORM, explode: false}, "select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create a Subscribe Topic Exception object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + aclProfileName - The name of the ACL Profile.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Subscribe Topic Exception object's attributes. 
    # + return - The Subscribe Topic Exception object's attributes after being created, and the request metadata. 
    #
    # # Deprecated
    @deprecated
    remote isolated function createMsgVpnAclProfileSubscribeException(string msgVpnName, string aclProfileName, MsgVpnAclProfileSubscribeException payload, map<string|string[]> headers = {}, *CreateMsgVpnAclProfileSubscribeExceptionQueries queries) returns MsgVpnAclProfileSubscribeExceptionResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/aclProfiles/${getEncodedUri(aclProfileName)}/subscribeExceptions`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get a Subscribe Topic Exception object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + aclProfileName - The name of the ACL Profile.
    # + topicSyntax - The syntax of the topic for the exception to the default action taken.
    # + subscribeExceptionTopic - The topic for the exception to the default action taken. May include wildcard characters.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The Subscribe Topic Exception object's attributes, and the request metadata. 
    #
    # # Deprecated
    @deprecated
    remote isolated function getMsgVpnAclProfileSubscribeException(string msgVpnName, string aclProfileName, string topicSyntax, string subscribeExceptionTopic, map<string|string[]> headers = {}, *GetMsgVpnAclProfileSubscribeExceptionQueries queries) returns MsgVpnAclProfileSubscribeExceptionResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/aclProfiles/${getEncodedUri(aclProfileName)}/subscribeExceptions/${getEncodedUri(topicSyntax)},${getEncodedUri(subscribeExceptionTopic)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete a Subscribe Topic Exception object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + aclProfileName - The name of the ACL Profile.
    # + topicSyntax - The syntax of the topic for the exception to the default action taken.
    # + subscribeExceptionTopic - The topic for the exception to the default action taken. May include wildcard characters.
    # + headers - Headers to be sent with the request 
    # + return - The request metadata. 
    #
    # # Deprecated
    @deprecated
    remote isolated function deleteMsgVpnAclProfileSubscribeException(string msgVpnName, string aclProfileName, string topicSyntax, string subscribeExceptionTopic, map<string|string[]> headers = {}) returns SempMetaOnlyResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/aclProfiles/${getEncodedUri(aclProfileName)}/subscribeExceptions/${getEncodedUri(topicSyntax)},${getEncodedUri(subscribeExceptionTopic)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Get a list of Subscribe Share Name Exception objects.
    #
    # + msgVpnName - The name of the Message VPN.
    # + aclProfileName - The name of the ACL Profile.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The list of Subscribe Share Name Exception objects' attributes, and the request metadata. 
    remote isolated function getMsgVpnAclProfileSubscribeShareNameExceptions(string msgVpnName, string aclProfileName, map<string|string[]> headers = {}, *GetMsgVpnAclProfileSubscribeShareNameExceptionsQueries queries) returns MsgVpnAclProfileSubscribeShareNameExceptionsResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/aclProfiles/${getEncodedUri(aclProfileName)}/subscribeShareNameExceptions`;
        map<Encoding> queryParamEncoding = {"where": {style: FORM, explode: false}, "select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create a Subscribe Share Name Exception object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + aclProfileName - The name of the ACL Profile.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Subscribe Share Name Exception object's attributes. 
    # + return - The Subscribe Share Name Exception object's attributes after being created, and the request metadata. 
    remote isolated function createMsgVpnAclProfileSubscribeShareNameException(string msgVpnName, string aclProfileName, MsgVpnAclProfileSubscribeShareNameException payload, map<string|string[]> headers = {}, *CreateMsgVpnAclProfileSubscribeShareNameExceptionQueries queries) returns MsgVpnAclProfileSubscribeShareNameExceptionResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/aclProfiles/${getEncodedUri(aclProfileName)}/subscribeShareNameExceptions`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get a Subscribe Share Name Exception object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + aclProfileName - The name of the ACL Profile.
    # + subscribeShareNameExceptionSyntax - The syntax of the subscribe share name for the exception to the default action taken.
    # + subscribeShareNameException - The subscribe share name exception to the default action taken. May include wildcard characters.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The Subscribe Share Name Exception object's attributes, and the request metadata. 
    remote isolated function getMsgVpnAclProfileSubscribeShareNameException(string msgVpnName, string aclProfileName, string subscribeShareNameExceptionSyntax, string subscribeShareNameException, map<string|string[]> headers = {}, *GetMsgVpnAclProfileSubscribeShareNameExceptionQueries queries) returns MsgVpnAclProfileSubscribeShareNameExceptionResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/aclProfiles/${getEncodedUri(aclProfileName)}/subscribeShareNameExceptions/${getEncodedUri(subscribeShareNameExceptionSyntax)},${getEncodedUri(subscribeShareNameException)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete a Subscribe Share Name Exception object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + aclProfileName - The name of the ACL Profile.
    # + subscribeShareNameExceptionSyntax - The syntax of the subscribe share name for the exception to the default action taken.
    # + subscribeShareNameException - The subscribe share name exception to the default action taken. May include wildcard characters.
    # + headers - Headers to be sent with the request 
    # + return - The request metadata. 
    remote isolated function deleteMsgVpnAclProfileSubscribeShareNameException(string msgVpnName, string aclProfileName, string subscribeShareNameExceptionSyntax, string subscribeShareNameException, map<string|string[]> headers = {}) returns SempMetaOnlyResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/aclProfiles/${getEncodedUri(aclProfileName)}/subscribeShareNameExceptions/${getEncodedUri(subscribeShareNameExceptionSyntax)},${getEncodedUri(subscribeShareNameException)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Get a list of Subscribe Topic Exception objects.
    #
    # + msgVpnName - The name of the Message VPN.
    # + aclProfileName - The name of the ACL Profile.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The list of Subscribe Topic Exception objects' attributes, and the request metadata. 
    remote isolated function getMsgVpnAclProfileSubscribeTopicExceptions(string msgVpnName, string aclProfileName, map<string|string[]> headers = {}, *GetMsgVpnAclProfileSubscribeTopicExceptionsQueries queries) returns MsgVpnAclProfileSubscribeTopicExceptionsResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/aclProfiles/${getEncodedUri(aclProfileName)}/subscribeTopicExceptions`;
        map<Encoding> queryParamEncoding = {"where": {style: FORM, explode: false}, "select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create a Subscribe Topic Exception object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + aclProfileName - The name of the ACL Profile.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Subscribe Topic Exception object's attributes. 
    # + return - The Subscribe Topic Exception object's attributes after being created, and the request metadata. 
    remote isolated function createMsgVpnAclProfileSubscribeTopicException(string msgVpnName, string aclProfileName, MsgVpnAclProfileSubscribeTopicException payload, map<string|string[]> headers = {}, *CreateMsgVpnAclProfileSubscribeTopicExceptionQueries queries) returns MsgVpnAclProfileSubscribeTopicExceptionResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/aclProfiles/${getEncodedUri(aclProfileName)}/subscribeTopicExceptions`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get a Subscribe Topic Exception object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + aclProfileName - The name of the ACL Profile.
    # + subscribeTopicExceptionSyntax - The syntax of the topic for the exception to the default action taken.
    # + subscribeTopicException - The topic for the exception to the default action taken. May include wildcard characters.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The Subscribe Topic Exception object's attributes, and the request metadata. 
    remote isolated function getMsgVpnAclProfileSubscribeTopicException(string msgVpnName, string aclProfileName, string subscribeTopicExceptionSyntax, string subscribeTopicException, map<string|string[]> headers = {}, *GetMsgVpnAclProfileSubscribeTopicExceptionQueries queries) returns MsgVpnAclProfileSubscribeTopicExceptionResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/aclProfiles/${getEncodedUri(aclProfileName)}/subscribeTopicExceptions/${getEncodedUri(subscribeTopicExceptionSyntax)},${getEncodedUri(subscribeTopicException)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete a Subscribe Topic Exception object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + aclProfileName - The name of the ACL Profile.
    # + subscribeTopicExceptionSyntax - The syntax of the topic for the exception to the default action taken.
    # + subscribeTopicException - The topic for the exception to the default action taken. May include wildcard characters.
    # + headers - Headers to be sent with the request 
    # + return - The request metadata. 
    remote isolated function deleteMsgVpnAclProfileSubscribeTopicException(string msgVpnName, string aclProfileName, string subscribeTopicExceptionSyntax, string subscribeTopicException, map<string|string[]> headers = {}) returns SempMetaOnlyResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/aclProfiles/${getEncodedUri(aclProfileName)}/subscribeTopicExceptions/${getEncodedUri(subscribeTopicExceptionSyntax)},${getEncodedUri(subscribeTopicException)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Get a list of Realm objects.
    #
    # + msgVpnName - The name of the Message VPN.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The list of Realm objects' attributes, and the request metadata. 
    remote isolated function getMsgVpnAuthenticationKerberosRealms(string msgVpnName, map<string|string[]> headers = {}, *GetMsgVpnAuthenticationKerberosRealmsQueries queries) returns MsgVpnAuthenticationKerberosRealmsResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/authenticationKerberosRealms`;
        map<Encoding> queryParamEncoding = {"where": {style: FORM, explode: false}, "select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create a Realm object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Realm object's attributes. 
    # + return - The Realm object's attributes after being created, and the request metadata. 
    remote isolated function createMsgVpnAuthenticationKerberosRealm(string msgVpnName, MsgVpnAuthenticationKerberosRealm payload, map<string|string[]> headers = {}, *CreateMsgVpnAuthenticationKerberosRealmQueries queries) returns MsgVpnAuthenticationKerberosRealmResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/authenticationKerberosRealms`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get a Realm object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + kerberosRealmName - The Realm Name. Must start with "@", typically all uppercase.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The Realm object's attributes, and the request metadata. 
    remote isolated function getMsgVpnAuthenticationKerberosRealm(string msgVpnName, string kerberosRealmName, map<string|string[]> headers = {}, *GetMsgVpnAuthenticationKerberosRealmQueries queries) returns MsgVpnAuthenticationKerberosRealmResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/authenticationKerberosRealms/${getEncodedUri(kerberosRealmName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Replace a Realm object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + kerberosRealmName - The Realm Name. Must start with "@", typically all uppercase.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Realm object's attributes. 
    # + return - The Realm object's attributes after being replaced, and the request metadata. 
    remote isolated function replaceMsgVpnAuthenticationKerberosRealm(string msgVpnName, string kerberosRealmName, MsgVpnAuthenticationKerberosRealm payload, map<string|string[]> headers = {}, *ReplaceMsgVpnAuthenticationKerberosRealmQueries queries) returns MsgVpnAuthenticationKerberosRealmResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/authenticationKerberosRealms/${getEncodedUri(kerberosRealmName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Delete a Realm object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + kerberosRealmName - The Realm Name. Must start with "@", typically all uppercase.
    # + headers - Headers to be sent with the request 
    # + return - The request metadata. 
    remote isolated function deleteMsgVpnAuthenticationKerberosRealm(string msgVpnName, string kerberosRealmName, map<string|string[]> headers = {}) returns SempMetaOnlyResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/authenticationKerberosRealms/${getEncodedUri(kerberosRealmName)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Update a Realm object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + kerberosRealmName - The Realm Name. Must start with "@", typically all uppercase.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Realm object's attributes. 
    # + return - The Realm object's attributes after being updated, and the request metadata. 
    remote isolated function updateMsgVpnAuthenticationKerberosRealm(string msgVpnName, string kerberosRealmName, MsgVpnAuthenticationKerberosRealm payload, map<string|string[]> headers = {}, *UpdateMsgVpnAuthenticationKerberosRealmQueries queries) returns MsgVpnAuthenticationKerberosRealmResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/authenticationKerberosRealms/${getEncodedUri(kerberosRealmName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Get a list of OAuth Profile objects.
    #
    # + msgVpnName - The name of the Message VPN.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The list of OAuth Profile objects' attributes, and the request metadata. 
    remote isolated function getMsgVpnAuthenticationOauthProfiles(string msgVpnName, map<string|string[]> headers = {}, *GetMsgVpnAuthenticationOauthProfilesQueries queries) returns MsgVpnAuthenticationOauthProfilesResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/authenticationOauthProfiles`;
        map<Encoding> queryParamEncoding = {"where": {style: FORM, explode: false}, "select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create an OAuth Profile object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The OAuth Profile object's attributes. 
    # + return - The OAuth Profile object's attributes after being created, and the request metadata. 
    remote isolated function createMsgVpnAuthenticationOauthProfile(string msgVpnName, MsgVpnAuthenticationOauthProfile payload, map<string|string[]> headers = {}, *CreateMsgVpnAuthenticationOauthProfileQueries queries) returns MsgVpnAuthenticationOauthProfileResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/authenticationOauthProfiles`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get an OAuth Profile object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + oauthProfileName - The name of the OAuth profile.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The OAuth Profile object's attributes, and the request metadata. 
    remote isolated function getMsgVpnAuthenticationOauthProfile(string msgVpnName, string oauthProfileName, map<string|string[]> headers = {}, *GetMsgVpnAuthenticationOauthProfileQueries queries) returns MsgVpnAuthenticationOauthProfileResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/authenticationOauthProfiles/${getEncodedUri(oauthProfileName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Replace an OAuth Profile object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + oauthProfileName - The name of the OAuth profile.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The OAuth Profile object's attributes. 
    # + return - The OAuth Profile object's attributes after being replaced, and the request metadata. 
    remote isolated function replaceMsgVpnAuthenticationOauthProfile(string msgVpnName, string oauthProfileName, MsgVpnAuthenticationOauthProfile payload, map<string|string[]> headers = {}, *ReplaceMsgVpnAuthenticationOauthProfileQueries queries) returns MsgVpnAuthenticationOauthProfileResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/authenticationOauthProfiles/${getEncodedUri(oauthProfileName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Delete an OAuth Profile object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + oauthProfileName - The name of the OAuth profile.
    # + headers - Headers to be sent with the request 
    # + return - The request metadata. 
    remote isolated function deleteMsgVpnAuthenticationOauthProfile(string msgVpnName, string oauthProfileName, map<string|string[]> headers = {}) returns SempMetaOnlyResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/authenticationOauthProfiles/${getEncodedUri(oauthProfileName)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Update an OAuth Profile object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + oauthProfileName - The name of the OAuth profile.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The OAuth Profile object's attributes. 
    # + return - The OAuth Profile object's attributes after being updated, and the request metadata. 
    remote isolated function updateMsgVpnAuthenticationOauthProfile(string msgVpnName, string oauthProfileName, MsgVpnAuthenticationOauthProfile payload, map<string|string[]> headers = {}, *UpdateMsgVpnAuthenticationOauthProfileQueries queries) returns MsgVpnAuthenticationOauthProfileResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/authenticationOauthProfiles/${getEncodedUri(oauthProfileName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Get a list of Required Claim objects.
    #
    # + msgVpnName - The name of the Message VPN.
    # + oauthProfileName - The name of the OAuth profile.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The list of Required Claim objects' attributes, and the request metadata. 
    remote isolated function getMsgVpnAuthenticationOauthProfileClientRequiredClaims(string msgVpnName, string oauthProfileName, map<string|string[]> headers = {}, *GetMsgVpnAuthenticationOauthProfileClientRequiredClaimsQueries queries) returns MsgVpnAuthenticationOauthProfileClientRequiredClaimsResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/authenticationOauthProfiles/${getEncodedUri(oauthProfileName)}/clientRequiredClaims`;
        map<Encoding> queryParamEncoding = {"where": {style: FORM, explode: false}, "select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create a Required Claim object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + oauthProfileName - The name of the OAuth profile.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Required Claim object's attributes. 
    # + return - The Required Claim object's attributes after being created, and the request metadata. 
    remote isolated function createMsgVpnAuthenticationOauthProfileClientRequiredClaim(string msgVpnName, string oauthProfileName, MsgVpnAuthenticationOauthProfileClientRequiredClaim payload, map<string|string[]> headers = {}, *CreateMsgVpnAuthenticationOauthProfileClientRequiredClaimQueries queries) returns MsgVpnAuthenticationOauthProfileClientRequiredClaimResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/authenticationOauthProfiles/${getEncodedUri(oauthProfileName)}/clientRequiredClaims`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get a Required Claim object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + oauthProfileName - The name of the OAuth profile.
    # + clientRequiredClaimName - The name of the ID token claim to verify.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The Required Claim object's attributes, and the request metadata. 
    remote isolated function getMsgVpnAuthenticationOauthProfileClientRequiredClaim(string msgVpnName, string oauthProfileName, string clientRequiredClaimName, map<string|string[]> headers = {}, *GetMsgVpnAuthenticationOauthProfileClientRequiredClaimQueries queries) returns MsgVpnAuthenticationOauthProfileClientRequiredClaimResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/authenticationOauthProfiles/${getEncodedUri(oauthProfileName)}/clientRequiredClaims/${getEncodedUri(clientRequiredClaimName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete a Required Claim object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + oauthProfileName - The name of the OAuth profile.
    # + clientRequiredClaimName - The name of the ID token claim to verify.
    # + headers - Headers to be sent with the request 
    # + return - The request metadata. 
    remote isolated function deleteMsgVpnAuthenticationOauthProfileClientRequiredClaim(string msgVpnName, string oauthProfileName, string clientRequiredClaimName, map<string|string[]> headers = {}) returns SempMetaOnlyResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/authenticationOauthProfiles/${getEncodedUri(oauthProfileName)}/clientRequiredClaims/${getEncodedUri(clientRequiredClaimName)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Get a list of Required Claim objects.
    #
    # + msgVpnName - The name of the Message VPN.
    # + oauthProfileName - The name of the OAuth profile.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The list of Required Claim objects' attributes, and the request metadata. 
    remote isolated function getMsgVpnAuthenticationOauthProfileResourceServerRequiredClaims(string msgVpnName, string oauthProfileName, map<string|string[]> headers = {}, *GetMsgVpnAuthenticationOauthProfileResourceServerRequiredClaimsQueries queries) returns MsgVpnAuthenticationOauthProfileResourceServerRequiredClaimsResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/authenticationOauthProfiles/${getEncodedUri(oauthProfileName)}/resourceServerRequiredClaims`;
        map<Encoding> queryParamEncoding = {"where": {style: FORM, explode: false}, "select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create a Required Claim object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + oauthProfileName - The name of the OAuth profile.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Required Claim object's attributes. 
    # + return - The Required Claim object's attributes after being created, and the request metadata. 
    remote isolated function createMsgVpnAuthenticationOauthProfileResourceServerRequiredClaim(string msgVpnName, string oauthProfileName, MsgVpnAuthenticationOauthProfileResourceServerRequiredClaim payload, map<string|string[]> headers = {}, *CreateMsgVpnAuthenticationOauthProfileResourceServerRequiredClaimQueries queries) returns MsgVpnAuthenticationOauthProfileResourceServerRequiredClaimResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/authenticationOauthProfiles/${getEncodedUri(oauthProfileName)}/resourceServerRequiredClaims`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get a Required Claim object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + oauthProfileName - The name of the OAuth profile.
    # + resourceServerRequiredClaimName - The name of the access token claim to verify.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The Required Claim object's attributes, and the request metadata. 
    remote isolated function getMsgVpnAuthenticationOauthProfileResourceServerRequiredClaim(string msgVpnName, string oauthProfileName, string resourceServerRequiredClaimName, map<string|string[]> headers = {}, *GetMsgVpnAuthenticationOauthProfileResourceServerRequiredClaimQueries queries) returns MsgVpnAuthenticationOauthProfileResourceServerRequiredClaimResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/authenticationOauthProfiles/${getEncodedUri(oauthProfileName)}/resourceServerRequiredClaims/${getEncodedUri(resourceServerRequiredClaimName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete a Required Claim object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + oauthProfileName - The name of the OAuth profile.
    # + resourceServerRequiredClaimName - The name of the access token claim to verify.
    # + headers - Headers to be sent with the request 
    # + return - The request metadata. 
    remote isolated function deleteMsgVpnAuthenticationOauthProfileResourceServerRequiredClaim(string msgVpnName, string oauthProfileName, string resourceServerRequiredClaimName, map<string|string[]> headers = {}) returns SempMetaOnlyResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/authenticationOauthProfiles/${getEncodedUri(oauthProfileName)}/resourceServerRequiredClaims/${getEncodedUri(resourceServerRequiredClaimName)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Get a list of OAuth Provider objects.
    #
    # + msgVpnName - The name of the Message VPN.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The list of OAuth Provider objects' attributes, and the request metadata. 
    #
    # # Deprecated
    @deprecated
    remote isolated function getMsgVpnAuthenticationOauthProviders(string msgVpnName, map<string|string[]> headers = {}, *GetMsgVpnAuthenticationOauthProvidersQueries queries) returns MsgVpnAuthenticationOauthProvidersResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/authenticationOauthProviders`;
        map<Encoding> queryParamEncoding = {"where": {style: FORM, explode: false}, "select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create an OAuth Provider object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The OAuth Provider object's attributes. 
    # + return - The OAuth Provider object's attributes after being created, and the request metadata. 
    #
    # # Deprecated
    @deprecated
    remote isolated function createMsgVpnAuthenticationOauthProvider(string msgVpnName, MsgVpnAuthenticationOauthProvider payload, map<string|string[]> headers = {}, *CreateMsgVpnAuthenticationOauthProviderQueries queries) returns MsgVpnAuthenticationOauthProviderResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/authenticationOauthProviders`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get an OAuth Provider object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + oauthProviderName - The name of the OAuth Provider.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The OAuth Provider object's attributes, and the request metadata. 
    #
    # # Deprecated
    @deprecated
    remote isolated function getMsgVpnAuthenticationOauthProvider(string msgVpnName, string oauthProviderName, map<string|string[]> headers = {}, *GetMsgVpnAuthenticationOauthProviderQueries queries) returns MsgVpnAuthenticationOauthProviderResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/authenticationOauthProviders/${getEncodedUri(oauthProviderName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Replace an OAuth Provider object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + oauthProviderName - The name of the OAuth Provider.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The OAuth Provider object's attributes. 
    # + return - The OAuth Provider object's attributes after being replaced, and the request metadata. 
    #
    # # Deprecated
    @deprecated
    remote isolated function replaceMsgVpnAuthenticationOauthProvider(string msgVpnName, string oauthProviderName, MsgVpnAuthenticationOauthProvider payload, map<string|string[]> headers = {}, *ReplaceMsgVpnAuthenticationOauthProviderQueries queries) returns MsgVpnAuthenticationOauthProviderResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/authenticationOauthProviders/${getEncodedUri(oauthProviderName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Delete an OAuth Provider object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + oauthProviderName - The name of the OAuth Provider.
    # + headers - Headers to be sent with the request 
    # + return - The request metadata. 
    #
    # # Deprecated
    @deprecated
    remote isolated function deleteMsgVpnAuthenticationOauthProvider(string msgVpnName, string oauthProviderName, map<string|string[]> headers = {}) returns SempMetaOnlyResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/authenticationOauthProviders/${getEncodedUri(oauthProviderName)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Update an OAuth Provider object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + oauthProviderName - The name of the OAuth Provider.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The OAuth Provider object's attributes. 
    # + return - The OAuth Provider object's attributes after being updated, and the request metadata. 
    #
    # # Deprecated
    @deprecated
    remote isolated function updateMsgVpnAuthenticationOauthProvider(string msgVpnName, string oauthProviderName, MsgVpnAuthenticationOauthProvider payload, map<string|string[]> headers = {}, *UpdateMsgVpnAuthenticationOauthProviderQueries queries) returns MsgVpnAuthenticationOauthProviderResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/authenticationOauthProviders/${getEncodedUri(oauthProviderName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Get a list of Authorization Group objects.
    #
    # + msgVpnName - The name of the Message VPN.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The list of Authorization Group objects' attributes, and the request metadata. 
    remote isolated function getMsgVpnAuthorizationGroups(string msgVpnName, map<string|string[]> headers = {}, *GetMsgVpnAuthorizationGroupsQueries queries) returns MsgVpnAuthorizationGroupsResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/authorizationGroups`;
        map<Encoding> queryParamEncoding = {"where": {style: FORM, explode: false}, "select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create an Authorization Group object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Authorization Group object's attributes. 
    # + return - The Authorization Group object's attributes after being created, and the request metadata. 
    remote isolated function createMsgVpnAuthorizationGroup(string msgVpnName, MsgVpnAuthorizationGroup payload, map<string|string[]> headers = {}, *CreateMsgVpnAuthorizationGroupQueries queries) returns MsgVpnAuthorizationGroupResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/authorizationGroups`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get an Authorization Group object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + authorizationGroupName - The name of the Authorization Group. For LDAP groups, special care is needed if the group name contains special characters such as '#', '+', ';', '=' as the value of the group name returned from the LDAP server might prepend those characters with '\'. For example a group name called 'test#,lab,com' will be returned from the LDAP server as 'test\#,lab,com'.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The Authorization Group object's attributes, and the request metadata. 
    remote isolated function getMsgVpnAuthorizationGroup(string msgVpnName, string authorizationGroupName, map<string|string[]> headers = {}, *GetMsgVpnAuthorizationGroupQueries queries) returns MsgVpnAuthorizationGroupResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/authorizationGroups/${getEncodedUri(authorizationGroupName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Replace an Authorization Group object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + authorizationGroupName - The name of the Authorization Group. For LDAP groups, special care is needed if the group name contains special characters such as '#', '+', ';', '=' as the value of the group name returned from the LDAP server might prepend those characters with '\'. For example a group name called 'test#,lab,com' will be returned from the LDAP server as 'test\#,lab,com'.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Authorization Group object's attributes. 
    # + return - The Authorization Group object's attributes after being replaced, and the request metadata. 
    remote isolated function replaceMsgVpnAuthorizationGroup(string msgVpnName, string authorizationGroupName, MsgVpnAuthorizationGroup payload, map<string|string[]> headers = {}, *ReplaceMsgVpnAuthorizationGroupQueries queries) returns MsgVpnAuthorizationGroupResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/authorizationGroups/${getEncodedUri(authorizationGroupName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Delete an Authorization Group object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + authorizationGroupName - The name of the Authorization Group. For LDAP groups, special care is needed if the group name contains special characters such as '#', '+', ';', '=' as the value of the group name returned from the LDAP server might prepend those characters with '\'. For example a group name called 'test#,lab,com' will be returned from the LDAP server as 'test\#,lab,com'.
    # + headers - Headers to be sent with the request 
    # + return - The request metadata. 
    remote isolated function deleteMsgVpnAuthorizationGroup(string msgVpnName, string authorizationGroupName, map<string|string[]> headers = {}) returns SempMetaOnlyResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/authorizationGroups/${getEncodedUri(authorizationGroupName)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Update an Authorization Group object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + authorizationGroupName - The name of the Authorization Group. For LDAP groups, special care is needed if the group name contains special characters such as '#', '+', ';', '=' as the value of the group name returned from the LDAP server might prepend those characters with '\'. For example a group name called 'test#,lab,com' will be returned from the LDAP server as 'test\#,lab,com'.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Authorization Group object's attributes. 
    # + return - The Authorization Group object's attributes after being updated, and the request metadata. 
    remote isolated function updateMsgVpnAuthorizationGroup(string msgVpnName, string authorizationGroupName, MsgVpnAuthorizationGroup payload, map<string|string[]> headers = {}, *UpdateMsgVpnAuthorizationGroupQueries queries) returns MsgVpnAuthorizationGroupResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/authorizationGroups/${getEncodedUri(authorizationGroupName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Get a list of Bridge objects.
    #
    # + msgVpnName - The name of the Message VPN.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The list of Bridge objects' attributes, and the request metadata. 
    remote isolated function getMsgVpnBridges(string msgVpnName, map<string|string[]> headers = {}, *GetMsgVpnBridgesQueries queries) returns MsgVpnBridgesResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/bridges`;
        map<Encoding> queryParamEncoding = {"where": {style: FORM, explode: false}, "select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create a Bridge object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Bridge object's attributes. 
    # + return - The Bridge object's attributes after being created, and the request metadata. 
    remote isolated function createMsgVpnBridge(string msgVpnName, MsgVpnBridge payload, map<string|string[]> headers = {}, *CreateMsgVpnBridgeQueries queries) returns MsgVpnBridgeResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/bridges`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get a Bridge object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + bridgeName - The name of the Bridge.
    # + bridgeVirtualRouter - The virtual router of the Bridge.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The Bridge object's attributes, and the request metadata. 
    remote isolated function getMsgVpnBridge(string msgVpnName, string bridgeName, string bridgeVirtualRouter, map<string|string[]> headers = {}, *GetMsgVpnBridgeQueries queries) returns MsgVpnBridgeResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/bridges/${getEncodedUri(bridgeName)},${getEncodedUri(bridgeVirtualRouter)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Replace a Bridge object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + bridgeName - The name of the Bridge.
    # + bridgeVirtualRouter - The virtual router of the Bridge.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Bridge object's attributes. 
    # + return - The Bridge object's attributes after being replaced, and the request metadata. 
    remote isolated function replaceMsgVpnBridge(string msgVpnName, string bridgeName, string bridgeVirtualRouter, MsgVpnBridge payload, map<string|string[]> headers = {}, *ReplaceMsgVpnBridgeQueries queries) returns MsgVpnBridgeResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/bridges/${getEncodedUri(bridgeName)},${getEncodedUri(bridgeVirtualRouter)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Delete a Bridge object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + bridgeName - The name of the Bridge.
    # + bridgeVirtualRouter - The virtual router of the Bridge.
    # + headers - Headers to be sent with the request 
    # + return - The request metadata. 
    remote isolated function deleteMsgVpnBridge(string msgVpnName, string bridgeName, string bridgeVirtualRouter, map<string|string[]> headers = {}) returns SempMetaOnlyResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/bridges/${getEncodedUri(bridgeName)},${getEncodedUri(bridgeVirtualRouter)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Update a Bridge object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + bridgeName - The name of the Bridge.
    # + bridgeVirtualRouter - The virtual router of the Bridge.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Bridge object's attributes. 
    # + return - The Bridge object's attributes after being updated, and the request metadata. 
    remote isolated function updateMsgVpnBridge(string msgVpnName, string bridgeName, string bridgeVirtualRouter, MsgVpnBridge payload, map<string|string[]> headers = {}, *UpdateMsgVpnBridgeQueries queries) returns MsgVpnBridgeResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/bridges/${getEncodedUri(bridgeName)},${getEncodedUri(bridgeVirtualRouter)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Get a list of Remote Message VPN objects.
    #
    # + msgVpnName - The name of the Message VPN.
    # + bridgeName - The name of the Bridge.
    # + bridgeVirtualRouter - The virtual router of the Bridge.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The list of Remote Message VPN objects' attributes, and the request metadata. 
    remote isolated function getMsgVpnBridgeRemoteMsgVpns(string msgVpnName, string bridgeName, string bridgeVirtualRouter, map<string|string[]> headers = {}, *GetMsgVpnBridgeRemoteMsgVpnsQueries queries) returns MsgVpnBridgeRemoteMsgVpnsResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/bridges/${getEncodedUri(bridgeName)},${getEncodedUri(bridgeVirtualRouter)}/remoteMsgVpns`;
        map<Encoding> queryParamEncoding = {"where": {style: FORM, explode: false}, "select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create a Remote Message VPN object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + bridgeName - The name of the Bridge.
    # + bridgeVirtualRouter - The virtual router of the Bridge.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Remote Message VPN object's attributes. 
    # + return - The Remote Message VPN object's attributes after being created, and the request metadata. 
    remote isolated function createMsgVpnBridgeRemoteMsgVpn(string msgVpnName, string bridgeName, string bridgeVirtualRouter, MsgVpnBridgeRemoteMsgVpn payload, map<string|string[]> headers = {}, *CreateMsgVpnBridgeRemoteMsgVpnQueries queries) returns MsgVpnBridgeRemoteMsgVpnResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/bridges/${getEncodedUri(bridgeName)},${getEncodedUri(bridgeVirtualRouter)}/remoteMsgVpns`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get a Remote Message VPN object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + bridgeName - The name of the Bridge.
    # + bridgeVirtualRouter - The virtual router of the Bridge.
    # + remoteMsgVpnName - The name of the remote Message VPN.
    # + remoteMsgVpnLocation - The location of the remote Message VPN as either an FQDN with port, IP address with port, or virtual router name (starting with "v:").
    # + remoteMsgVpnInterface - The physical interface on the local Message VPN host for connecting to the remote Message VPN. By default, an interface is chosen automatically (recommended), but if specified, `remoteMsgVpnLocation` must not be a virtual router name.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The Remote Message VPN object's attributes, and the request metadata. 
    remote isolated function getMsgVpnBridgeRemoteMsgVpn(string msgVpnName, string bridgeName, string bridgeVirtualRouter, string remoteMsgVpnName, string remoteMsgVpnLocation, string remoteMsgVpnInterface, map<string|string[]> headers = {}, *GetMsgVpnBridgeRemoteMsgVpnQueries queries) returns MsgVpnBridgeRemoteMsgVpnResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/bridges/${getEncodedUri(bridgeName)},${getEncodedUri(bridgeVirtualRouter)}/remoteMsgVpns/${getEncodedUri(remoteMsgVpnName)},${getEncodedUri(remoteMsgVpnLocation)},${getEncodedUri(remoteMsgVpnInterface)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Replace a Remote Message VPN object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + bridgeName - The name of the Bridge.
    # + bridgeVirtualRouter - The virtual router of the Bridge.
    # + remoteMsgVpnName - The name of the remote Message VPN.
    # + remoteMsgVpnLocation - The location of the remote Message VPN as either an FQDN with port, IP address with port, or virtual router name (starting with "v:").
    # + remoteMsgVpnInterface - The physical interface on the local Message VPN host for connecting to the remote Message VPN. By default, an interface is chosen automatically (recommended), but if specified, `remoteMsgVpnLocation` must not be a virtual router name.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Remote Message VPN object's attributes. 
    # + return - The Remote Message VPN object's attributes after being replaced, and the request metadata. 
    remote isolated function replaceMsgVpnBridgeRemoteMsgVpn(string msgVpnName, string bridgeName, string bridgeVirtualRouter, string remoteMsgVpnName, string remoteMsgVpnLocation, string remoteMsgVpnInterface, MsgVpnBridgeRemoteMsgVpn payload, map<string|string[]> headers = {}, *ReplaceMsgVpnBridgeRemoteMsgVpnQueries queries) returns MsgVpnBridgeRemoteMsgVpnResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/bridges/${getEncodedUri(bridgeName)},${getEncodedUri(bridgeVirtualRouter)}/remoteMsgVpns/${getEncodedUri(remoteMsgVpnName)},${getEncodedUri(remoteMsgVpnLocation)},${getEncodedUri(remoteMsgVpnInterface)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Delete a Remote Message VPN object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + bridgeName - The name of the Bridge.
    # + bridgeVirtualRouter - The virtual router of the Bridge.
    # + remoteMsgVpnName - The name of the remote Message VPN.
    # + remoteMsgVpnLocation - The location of the remote Message VPN as either an FQDN with port, IP address with port, or virtual router name (starting with "v:").
    # + remoteMsgVpnInterface - The physical interface on the local Message VPN host for connecting to the remote Message VPN. By default, an interface is chosen automatically (recommended), but if specified, `remoteMsgVpnLocation` must not be a virtual router name.
    # + headers - Headers to be sent with the request 
    # + return - The request metadata. 
    remote isolated function deleteMsgVpnBridgeRemoteMsgVpn(string msgVpnName, string bridgeName, string bridgeVirtualRouter, string remoteMsgVpnName, string remoteMsgVpnLocation, string remoteMsgVpnInterface, map<string|string[]> headers = {}) returns SempMetaOnlyResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/bridges/${getEncodedUri(bridgeName)},${getEncodedUri(bridgeVirtualRouter)}/remoteMsgVpns/${getEncodedUri(remoteMsgVpnName)},${getEncodedUri(remoteMsgVpnLocation)},${getEncodedUri(remoteMsgVpnInterface)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Update a Remote Message VPN object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + bridgeName - The name of the Bridge.
    # + bridgeVirtualRouter - The virtual router of the Bridge.
    # + remoteMsgVpnName - The name of the remote Message VPN.
    # + remoteMsgVpnLocation - The location of the remote Message VPN as either an FQDN with port, IP address with port, or virtual router name (starting with "v:").
    # + remoteMsgVpnInterface - The physical interface on the local Message VPN host for connecting to the remote Message VPN. By default, an interface is chosen automatically (recommended), but if specified, `remoteMsgVpnLocation` must not be a virtual router name.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Remote Message VPN object's attributes. 
    # + return - The Remote Message VPN object's attributes after being updated, and the request metadata. 
    remote isolated function updateMsgVpnBridgeRemoteMsgVpn(string msgVpnName, string bridgeName, string bridgeVirtualRouter, string remoteMsgVpnName, string remoteMsgVpnLocation, string remoteMsgVpnInterface, MsgVpnBridgeRemoteMsgVpn payload, map<string|string[]> headers = {}, *UpdateMsgVpnBridgeRemoteMsgVpnQueries queries) returns MsgVpnBridgeRemoteMsgVpnResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/bridges/${getEncodedUri(bridgeName)},${getEncodedUri(bridgeVirtualRouter)}/remoteMsgVpns/${getEncodedUri(remoteMsgVpnName)},${getEncodedUri(remoteMsgVpnLocation)},${getEncodedUri(remoteMsgVpnInterface)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Get a list of Remote Subscription objects.
    #
    # + msgVpnName - The name of the Message VPN.
    # + bridgeName - The name of the Bridge.
    # + bridgeVirtualRouter - The virtual router of the Bridge.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The list of Remote Subscription objects' attributes, and the request metadata. 
    remote isolated function getMsgVpnBridgeRemoteSubscriptions(string msgVpnName, string bridgeName, string bridgeVirtualRouter, map<string|string[]> headers = {}, *GetMsgVpnBridgeRemoteSubscriptionsQueries queries) returns MsgVpnBridgeRemoteSubscriptionsResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/bridges/${getEncodedUri(bridgeName)},${getEncodedUri(bridgeVirtualRouter)}/remoteSubscriptions`;
        map<Encoding> queryParamEncoding = {"where": {style: FORM, explode: false}, "select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create a Remote Subscription object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + bridgeName - The name of the Bridge.
    # + bridgeVirtualRouter - The virtual router of the Bridge.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Remote Subscription object's attributes. 
    # + return - The Remote Subscription object's attributes after being created, and the request metadata. 
    remote isolated function createMsgVpnBridgeRemoteSubscription(string msgVpnName, string bridgeName, string bridgeVirtualRouter, MsgVpnBridgeRemoteSubscription payload, map<string|string[]> headers = {}, *CreateMsgVpnBridgeRemoteSubscriptionQueries queries) returns MsgVpnBridgeRemoteSubscriptionResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/bridges/${getEncodedUri(bridgeName)},${getEncodedUri(bridgeVirtualRouter)}/remoteSubscriptions`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get a Remote Subscription object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + bridgeName - The name of the Bridge.
    # + bridgeVirtualRouter - The virtual router of the Bridge.
    # + remoteSubscriptionTopic - The topic of the Bridge remote subscription.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The Remote Subscription object's attributes, and the request metadata. 
    remote isolated function getMsgVpnBridgeRemoteSubscription(string msgVpnName, string bridgeName, string bridgeVirtualRouter, string remoteSubscriptionTopic, map<string|string[]> headers = {}, *GetMsgVpnBridgeRemoteSubscriptionQueries queries) returns MsgVpnBridgeRemoteSubscriptionResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/bridges/${getEncodedUri(bridgeName)},${getEncodedUri(bridgeVirtualRouter)}/remoteSubscriptions/${getEncodedUri(remoteSubscriptionTopic)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete a Remote Subscription object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + bridgeName - The name of the Bridge.
    # + bridgeVirtualRouter - The virtual router of the Bridge.
    # + remoteSubscriptionTopic - The topic of the Bridge remote subscription.
    # + headers - Headers to be sent with the request 
    # + return - The request metadata. 
    remote isolated function deleteMsgVpnBridgeRemoteSubscription(string msgVpnName, string bridgeName, string bridgeVirtualRouter, string remoteSubscriptionTopic, map<string|string[]> headers = {}) returns SempMetaOnlyResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/bridges/${getEncodedUri(bridgeName)},${getEncodedUri(bridgeVirtualRouter)}/remoteSubscriptions/${getEncodedUri(remoteSubscriptionTopic)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Get a list of Trusted Common Name objects.
    #
    # + msgVpnName - The name of the Message VPN.
    # + bridgeName - The name of the Bridge.
    # + bridgeVirtualRouter - The virtual router of the Bridge.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The list of Trusted Common Name objects' attributes, and the request metadata. 
    #
    # # Deprecated
    @deprecated
    remote isolated function getMsgVpnBridgeTlsTrustedCommonNames(string msgVpnName, string bridgeName, string bridgeVirtualRouter, map<string|string[]> headers = {}, *GetMsgVpnBridgeTlsTrustedCommonNamesQueries queries) returns MsgVpnBridgeTlsTrustedCommonNamesResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/bridges/${getEncodedUri(bridgeName)},${getEncodedUri(bridgeVirtualRouter)}/tlsTrustedCommonNames`;
        map<Encoding> queryParamEncoding = {"where": {style: FORM, explode: false}, "select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create a Trusted Common Name object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + bridgeName - The name of the Bridge.
    # + bridgeVirtualRouter - The virtual router of the Bridge.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Trusted Common Name object's attributes. 
    # + return - The Trusted Common Name object's attributes after being created, and the request metadata. 
    #
    # # Deprecated
    @deprecated
    remote isolated function createMsgVpnBridgeTlsTrustedCommonName(string msgVpnName, string bridgeName, string bridgeVirtualRouter, MsgVpnBridgeTlsTrustedCommonName payload, map<string|string[]> headers = {}, *CreateMsgVpnBridgeTlsTrustedCommonNameQueries queries) returns MsgVpnBridgeTlsTrustedCommonNameResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/bridges/${getEncodedUri(bridgeName)},${getEncodedUri(bridgeVirtualRouter)}/tlsTrustedCommonNames`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get a Trusted Common Name object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + bridgeName - The name of the Bridge.
    # + bridgeVirtualRouter - The virtual router of the Bridge.
    # + tlsTrustedCommonName - The expected trusted common name of the remote certificate.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The Trusted Common Name object's attributes, and the request metadata. 
    #
    # # Deprecated
    @deprecated
    remote isolated function getMsgVpnBridgeTlsTrustedCommonName(string msgVpnName, string bridgeName, string bridgeVirtualRouter, string tlsTrustedCommonName, map<string|string[]> headers = {}, *GetMsgVpnBridgeTlsTrustedCommonNameQueries queries) returns MsgVpnBridgeTlsTrustedCommonNameResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/bridges/${getEncodedUri(bridgeName)},${getEncodedUri(bridgeVirtualRouter)}/tlsTrustedCommonNames/${getEncodedUri(tlsTrustedCommonName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete a Trusted Common Name object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + bridgeName - The name of the Bridge.
    # + bridgeVirtualRouter - The virtual router of the Bridge.
    # + tlsTrustedCommonName - The expected trusted common name of the remote certificate.
    # + headers - Headers to be sent with the request 
    # + return - The request metadata. 
    #
    # # Deprecated
    @deprecated
    remote isolated function deleteMsgVpnBridgeTlsTrustedCommonName(string msgVpnName, string bridgeName, string bridgeVirtualRouter, string tlsTrustedCommonName, map<string|string[]> headers = {}) returns SempMetaOnlyResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/bridges/${getEncodedUri(bridgeName)},${getEncodedUri(bridgeVirtualRouter)}/tlsTrustedCommonNames/${getEncodedUri(tlsTrustedCommonName)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Get a list of Certificate Matching Rule objects.
    #
    # + msgVpnName - The name of the Message VPN.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The list of Certificate Matching Rule objects' attributes, and the request metadata. 
    remote isolated function getMsgVpnCertMatchingRules(string msgVpnName, map<string|string[]> headers = {}, *GetMsgVpnCertMatchingRulesQueries queries) returns MsgVpnCertMatchingRulesResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/certMatchingRules`;
        map<Encoding> queryParamEncoding = {"where": {style: FORM, explode: false}, "select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create a Certificate Matching Rule object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Certificate Matching Rule object's attributes. 
    # + return - The Certificate Matching Rule object's attributes after being created, and the request metadata. 
    remote isolated function createMsgVpnCertMatchingRule(string msgVpnName, MsgVpnCertMatchingRule payload, map<string|string[]> headers = {}, *CreateMsgVpnCertMatchingRuleQueries queries) returns MsgVpnCertMatchingRuleResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/certMatchingRules`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get a Certificate Matching Rule object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + ruleName - The name of the rule.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The Certificate Matching Rule object's attributes, and the request metadata. 
    remote isolated function getMsgVpnCertMatchingRule(string msgVpnName, string ruleName, map<string|string[]> headers = {}, *GetMsgVpnCertMatchingRuleQueries queries) returns MsgVpnCertMatchingRuleResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/certMatchingRules/${getEncodedUri(ruleName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Replace a Certificate Matching Rule object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + ruleName - The name of the rule.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Certificate Matching Rule object's attributes. 
    # + return - The Certificate Matching Rule object's attributes after being replaced, and the request metadata. 
    remote isolated function replaceMsgVpnCertMatchingRule(string msgVpnName, string ruleName, MsgVpnCertMatchingRule payload, map<string|string[]> headers = {}, *ReplaceMsgVpnCertMatchingRuleQueries queries) returns MsgVpnCertMatchingRuleResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/certMatchingRules/${getEncodedUri(ruleName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Delete a Certificate Matching Rule object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + ruleName - The name of the rule.
    # + headers - Headers to be sent with the request 
    # + return - The request metadata. 
    remote isolated function deleteMsgVpnCertMatchingRule(string msgVpnName, string ruleName, map<string|string[]> headers = {}) returns SempMetaOnlyResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/certMatchingRules/${getEncodedUri(ruleName)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Update a Certificate Matching Rule object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + ruleName - The name of the rule.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Certificate Matching Rule object's attributes. 
    # + return - The Certificate Matching Rule object's attributes after being updated, and the request metadata. 
    remote isolated function updateMsgVpnCertMatchingRule(string msgVpnName, string ruleName, MsgVpnCertMatchingRule payload, map<string|string[]> headers = {}, *UpdateMsgVpnCertMatchingRuleQueries queries) returns MsgVpnCertMatchingRuleResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/certMatchingRules/${getEncodedUri(ruleName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Get a list of Certificate Matching Rule Attribute Filter objects.
    #
    # + msgVpnName - The name of the Message VPN.
    # + ruleName - The name of the rule.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The list of Certificate Matching Rule Attribute Filter objects' attributes, and the request metadata. 
    remote isolated function getMsgVpnCertMatchingRuleAttributeFilters(string msgVpnName, string ruleName, map<string|string[]> headers = {}, *GetMsgVpnCertMatchingRuleAttributeFiltersQueries queries) returns MsgVpnCertMatchingRuleAttributeFiltersResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/certMatchingRules/${getEncodedUri(ruleName)}/attributeFilters`;
        map<Encoding> queryParamEncoding = {"where": {style: FORM, explode: false}, "select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create a Certificate Matching Rule Attribute Filter object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + ruleName - The name of the rule.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Certificate Matching Rule Attribute Filter object's attributes. 
    # + return - The Certificate Matching Rule Attribute Filter object's attributes after being created, and the request metadata. 
    remote isolated function createMsgVpnCertMatchingRuleAttributeFilter(string msgVpnName, string ruleName, MsgVpnCertMatchingRuleAttributeFilter payload, map<string|string[]> headers = {}, *CreateMsgVpnCertMatchingRuleAttributeFilterQueries queries) returns MsgVpnCertMatchingRuleAttributeFilterResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/certMatchingRules/${getEncodedUri(ruleName)}/attributeFilters`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get a Certificate Matching Rule Attribute Filter object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + ruleName - The name of the rule.
    # + filterName - The name of the filter.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The Certificate Matching Rule Attribute Filter object's attributes, and the request metadata. 
    remote isolated function getMsgVpnCertMatchingRuleAttributeFilter(string msgVpnName, string ruleName, string filterName, map<string|string[]> headers = {}, *GetMsgVpnCertMatchingRuleAttributeFilterQueries queries) returns MsgVpnCertMatchingRuleAttributeFilterResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/certMatchingRules/${getEncodedUri(ruleName)}/attributeFilters/${getEncodedUri(filterName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Replace a Certificate Matching Rule Attribute Filter object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + ruleName - The name of the rule.
    # + filterName - The name of the filter.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Certificate Matching Rule Attribute Filter object's attributes. 
    # + return - The Certificate Matching Rule Attribute Filter object's attributes after being replaced, and the request metadata. 
    remote isolated function replaceMsgVpnCertMatchingRuleAttributeFilter(string msgVpnName, string ruleName, string filterName, MsgVpnCertMatchingRuleAttributeFilter payload, map<string|string[]> headers = {}, *ReplaceMsgVpnCertMatchingRuleAttributeFilterQueries queries) returns MsgVpnCertMatchingRuleAttributeFilterResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/certMatchingRules/${getEncodedUri(ruleName)}/attributeFilters/${getEncodedUri(filterName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Delete a Certificate Matching Rule Attribute Filter object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + ruleName - The name of the rule.
    # + filterName - The name of the filter.
    # + headers - Headers to be sent with the request 
    # + return - The request metadata. 
    remote isolated function deleteMsgVpnCertMatchingRuleAttributeFilter(string msgVpnName, string ruleName, string filterName, map<string|string[]> headers = {}) returns SempMetaOnlyResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/certMatchingRules/${getEncodedUri(ruleName)}/attributeFilters/${getEncodedUri(filterName)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Update a Certificate Matching Rule Attribute Filter object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + ruleName - The name of the rule.
    # + filterName - The name of the filter.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Certificate Matching Rule Attribute Filter object's attributes. 
    # + return - The Certificate Matching Rule Attribute Filter object's attributes after being updated, and the request metadata. 
    remote isolated function updateMsgVpnCertMatchingRuleAttributeFilter(string msgVpnName, string ruleName, string filterName, MsgVpnCertMatchingRuleAttributeFilter payload, map<string|string[]> headers = {}, *UpdateMsgVpnCertMatchingRuleAttributeFilterQueries queries) returns MsgVpnCertMatchingRuleAttributeFilterResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/certMatchingRules/${getEncodedUri(ruleName)}/attributeFilters/${getEncodedUri(filterName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Get a list of Certificate Matching Rule Condition objects.
    #
    # + msgVpnName - The name of the Message VPN.
    # + ruleName - The name of the rule.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The list of Certificate Matching Rule Condition objects' attributes, and the request metadata. 
    remote isolated function getMsgVpnCertMatchingRuleConditions(string msgVpnName, string ruleName, map<string|string[]> headers = {}, *GetMsgVpnCertMatchingRuleConditionsQueries queries) returns MsgVpnCertMatchingRuleConditionsResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/certMatchingRules/${getEncodedUri(ruleName)}/conditions`;
        map<Encoding> queryParamEncoding = {"where": {style: FORM, explode: false}, "select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create a Certificate Matching Rule Condition object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + ruleName - The name of the rule.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Certificate Matching Rule Condition object's attributes. 
    # + return - The Certificate Matching Rule Condition object's attributes after being created, and the request metadata. 
    remote isolated function createMsgVpnCertMatchingRuleCondition(string msgVpnName, string ruleName, MsgVpnCertMatchingRuleCondition payload, map<string|string[]> headers = {}, *CreateMsgVpnCertMatchingRuleConditionQueries queries) returns MsgVpnCertMatchingRuleConditionResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/certMatchingRules/${getEncodedUri(ruleName)}/conditions`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get a Certificate Matching Rule Condition object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + ruleName - The name of the rule.
    # + 'source - Certificate field to be compared with the Attribute.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The Certificate Matching Rule Condition object's attributes, and the request metadata. 
    remote isolated function getMsgVpnCertMatchingRuleCondition(string msgVpnName, string ruleName, string 'source, map<string|string[]> headers = {}, *GetMsgVpnCertMatchingRuleConditionQueries queries) returns MsgVpnCertMatchingRuleConditionResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/certMatchingRules/${getEncodedUri(ruleName)}/conditions/${getEncodedUri('source)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete a Certificate Matching Rule Condition object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + ruleName - The name of the rule.
    # + 'source - Certificate field to be compared with the Attribute.
    # + headers - Headers to be sent with the request 
    # + return - The request metadata. 
    remote isolated function deleteMsgVpnCertMatchingRuleCondition(string msgVpnName, string ruleName, string 'source, map<string|string[]> headers = {}) returns SempMetaOnlyResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/certMatchingRules/${getEncodedUri(ruleName)}/conditions/${getEncodedUri('source)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Get a list of Client Profile objects.
    #
    # + msgVpnName - The name of the Message VPN.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The list of Client Profile objects' attributes, and the request metadata. 
    remote isolated function getMsgVpnClientProfiles(string msgVpnName, map<string|string[]> headers = {}, *GetMsgVpnClientProfilesQueries queries) returns MsgVpnClientProfilesResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/clientProfiles`;
        map<Encoding> queryParamEncoding = {"where": {style: FORM, explode: false}, "select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create a Client Profile object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Client Profile object's attributes. 
    # + return - The Client Profile object's attributes after being created, and the request metadata. 
    remote isolated function createMsgVpnClientProfile(string msgVpnName, MsgVpnClientProfile payload, map<string|string[]> headers = {}, *CreateMsgVpnClientProfileQueries queries) returns MsgVpnClientProfileResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/clientProfiles`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get a Client Profile object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + clientProfileName - The name of the Client Profile.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The Client Profile object's attributes, and the request metadata. 
    remote isolated function getMsgVpnClientProfile(string msgVpnName, string clientProfileName, map<string|string[]> headers = {}, *GetMsgVpnClientProfileQueries queries) returns MsgVpnClientProfileResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/clientProfiles/${getEncodedUri(clientProfileName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Replace a Client Profile object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + clientProfileName - The name of the Client Profile.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Client Profile object's attributes. 
    # + return - The Client Profile object's attributes after being replaced, and the request metadata. 
    remote isolated function replaceMsgVpnClientProfile(string msgVpnName, string clientProfileName, MsgVpnClientProfile payload, map<string|string[]> headers = {}, *ReplaceMsgVpnClientProfileQueries queries) returns MsgVpnClientProfileResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/clientProfiles/${getEncodedUri(clientProfileName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Delete a Client Profile object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + clientProfileName - The name of the Client Profile.
    # + headers - Headers to be sent with the request 
    # + return - The request metadata. 
    remote isolated function deleteMsgVpnClientProfile(string msgVpnName, string clientProfileName, map<string|string[]> headers = {}) returns SempMetaOnlyResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/clientProfiles/${getEncodedUri(clientProfileName)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Update a Client Profile object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + clientProfileName - The name of the Client Profile.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Client Profile object's attributes. 
    # + return - The Client Profile object's attributes after being updated, and the request metadata. 
    remote isolated function updateMsgVpnClientProfile(string msgVpnName, string clientProfileName, MsgVpnClientProfile payload, map<string|string[]> headers = {}, *UpdateMsgVpnClientProfileQueries queries) returns MsgVpnClientProfileResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/clientProfiles/${getEncodedUri(clientProfileName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Get a list of Client Username objects.
    #
    # + msgVpnName - The name of the Message VPN.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The list of Client Username objects' attributes, and the request metadata. 
    remote isolated function getMsgVpnClientUsernames(string msgVpnName, map<string|string[]> headers = {}, *GetMsgVpnClientUsernamesQueries queries) returns MsgVpnClientUsernamesResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/clientUsernames`;
        map<Encoding> queryParamEncoding = {"where": {style: FORM, explode: false}, "select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create a Client Username object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Client Username object's attributes. 
    # + return - The Client Username object's attributes after being created, and the request metadata. 
    remote isolated function createMsgVpnClientUsername(string msgVpnName, MsgVpnClientUsername payload, map<string|string[]> headers = {}, *CreateMsgVpnClientUsernameQueries queries) returns MsgVpnClientUsernameResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/clientUsernames`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get a Client Username object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + clientUsername - The name of the Client Username.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The Client Username object's attributes, and the request metadata. 
    remote isolated function getMsgVpnClientUsername(string msgVpnName, string clientUsername, map<string|string[]> headers = {}, *GetMsgVpnClientUsernameQueries queries) returns MsgVpnClientUsernameResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/clientUsernames/${getEncodedUri(clientUsername)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Replace a Client Username object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + clientUsername - The name of the Client Username.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Client Username object's attributes. 
    # + return - The Client Username object's attributes after being replaced, and the request metadata. 
    remote isolated function replaceMsgVpnClientUsername(string msgVpnName, string clientUsername, MsgVpnClientUsername payload, map<string|string[]> headers = {}, *ReplaceMsgVpnClientUsernameQueries queries) returns MsgVpnClientUsernameResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/clientUsernames/${getEncodedUri(clientUsername)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Delete a Client Username object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + clientUsername - The name of the Client Username.
    # + headers - Headers to be sent with the request 
    # + return - The request metadata. 
    remote isolated function deleteMsgVpnClientUsername(string msgVpnName, string clientUsername, map<string|string[]> headers = {}) returns SempMetaOnlyResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/clientUsernames/${getEncodedUri(clientUsername)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Update a Client Username object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + clientUsername - The name of the Client Username.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Client Username object's attributes. 
    # + return - The Client Username object's attributes after being updated, and the request metadata. 
    remote isolated function updateMsgVpnClientUsername(string msgVpnName, string clientUsername, MsgVpnClientUsername payload, map<string|string[]> headers = {}, *UpdateMsgVpnClientUsernameQueries queries) returns MsgVpnClientUsernameResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/clientUsernames/${getEncodedUri(clientUsername)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Get a list of Client Username Attribute objects.
    #
    # + msgVpnName - The name of the Message VPN.
    # + clientUsername - The name of the Client Username.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The list of Client Username Attribute objects' attributes, and the request metadata. 
    remote isolated function getMsgVpnClientUsernameAttributes(string msgVpnName, string clientUsername, map<string|string[]> headers = {}, *GetMsgVpnClientUsernameAttributesQueries queries) returns MsgVpnClientUsernameAttributesResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/clientUsernames/${getEncodedUri(clientUsername)}/attributes`;
        map<Encoding> queryParamEncoding = {"where": {style: FORM, explode: false}, "select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create a Client Username Attribute object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + clientUsername - The name of the Client Username.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Client Username Attribute object's attributes. 
    # + return - The Client Username Attribute object's attributes after being created, and the request metadata. 
    remote isolated function createMsgVpnClientUsernameAttribute(string msgVpnName, string clientUsername, MsgVpnClientUsernameAttribute payload, map<string|string[]> headers = {}, *CreateMsgVpnClientUsernameAttributeQueries queries) returns MsgVpnClientUsernameAttributeResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/clientUsernames/${getEncodedUri(clientUsername)}/attributes`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get a Client Username Attribute object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + clientUsername - The name of the Client Username.
    # + attributeName - The name of the Attribute.
    # + attributeValue - The value of the Attribute.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The Client Username Attribute object's attributes, and the request metadata. 
    remote isolated function getMsgVpnClientUsernameAttribute(string msgVpnName, string clientUsername, string attributeName, string attributeValue, map<string|string[]> headers = {}, *GetMsgVpnClientUsernameAttributeQueries queries) returns MsgVpnClientUsernameAttributeResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/clientUsernames/${getEncodedUri(clientUsername)}/attributes/${getEncodedUri(attributeName)},${getEncodedUri(attributeValue)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete a Client Username Attribute object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + clientUsername - The name of the Client Username.
    # + attributeName - The name of the Attribute.
    # + attributeValue - The value of the Attribute.
    # + headers - Headers to be sent with the request 
    # + return - The request metadata. 
    remote isolated function deleteMsgVpnClientUsernameAttribute(string msgVpnName, string clientUsername, string attributeName, string attributeValue, map<string|string[]> headers = {}) returns SempMetaOnlyResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/clientUsernames/${getEncodedUri(clientUsername)}/attributes/${getEncodedUri(attributeName)},${getEncodedUri(attributeValue)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Get a list of Distributed Cache objects.
    #
    # + msgVpnName - The name of the Message VPN.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The list of Distributed Cache objects' attributes, and the request metadata. 
    remote isolated function getMsgVpnDistributedCaches(string msgVpnName, map<string|string[]> headers = {}, *GetMsgVpnDistributedCachesQueries queries) returns MsgVpnDistributedCachesResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/distributedCaches`;
        map<Encoding> queryParamEncoding = {"where": {style: FORM, explode: false}, "select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create a Distributed Cache object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Distributed Cache object's attributes. 
    # + return - The Distributed Cache object's attributes after being created, and the request metadata. 
    remote isolated function createMsgVpnDistributedCache(string msgVpnName, MsgVpnDistributedCache payload, map<string|string[]> headers = {}, *CreateMsgVpnDistributedCacheQueries queries) returns MsgVpnDistributedCacheResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/distributedCaches`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get a Distributed Cache object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + cacheName - The name of the Distributed Cache.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The Distributed Cache object's attributes, and the request metadata. 
    remote isolated function getMsgVpnDistributedCache(string msgVpnName, string cacheName, map<string|string[]> headers = {}, *GetMsgVpnDistributedCacheQueries queries) returns MsgVpnDistributedCacheResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/distributedCaches/${getEncodedUri(cacheName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Replace a Distributed Cache object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + cacheName - The name of the Distributed Cache.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Distributed Cache object's attributes. 
    # + return - The Distributed Cache object's attributes after being replaced, and the request metadata. 
    remote isolated function replaceMsgVpnDistributedCache(string msgVpnName, string cacheName, MsgVpnDistributedCache payload, map<string|string[]> headers = {}, *ReplaceMsgVpnDistributedCacheQueries queries) returns MsgVpnDistributedCacheResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/distributedCaches/${getEncodedUri(cacheName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Delete a Distributed Cache object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + cacheName - The name of the Distributed Cache.
    # + headers - Headers to be sent with the request 
    # + return - The request metadata. 
    remote isolated function deleteMsgVpnDistributedCache(string msgVpnName, string cacheName, map<string|string[]> headers = {}) returns SempMetaOnlyResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/distributedCaches/${getEncodedUri(cacheName)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Update a Distributed Cache object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + cacheName - The name of the Distributed Cache.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Distributed Cache object's attributes. 
    # + return - The Distributed Cache object's attributes after being updated, and the request metadata. 
    remote isolated function updateMsgVpnDistributedCache(string msgVpnName, string cacheName, MsgVpnDistributedCache payload, map<string|string[]> headers = {}, *UpdateMsgVpnDistributedCacheQueries queries) returns MsgVpnDistributedCacheResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/distributedCaches/${getEncodedUri(cacheName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Get a list of Cache Cluster objects.
    #
    # + msgVpnName - The name of the Message VPN.
    # + cacheName - The name of the Distributed Cache.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The list of Cache Cluster objects' attributes, and the request metadata. 
    remote isolated function getMsgVpnDistributedCacheClusters(string msgVpnName, string cacheName, map<string|string[]> headers = {}, *GetMsgVpnDistributedCacheClustersQueries queries) returns MsgVpnDistributedCacheClustersResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/distributedCaches/${getEncodedUri(cacheName)}/clusters`;
        map<Encoding> queryParamEncoding = {"where": {style: FORM, explode: false}, "select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create a Cache Cluster object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + cacheName - The name of the Distributed Cache.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Cache Cluster object's attributes. 
    # + return - The Cache Cluster object's attributes after being created, and the request metadata. 
    remote isolated function createMsgVpnDistributedCacheCluster(string msgVpnName, string cacheName, MsgVpnDistributedCacheCluster payload, map<string|string[]> headers = {}, *CreateMsgVpnDistributedCacheClusterQueries queries) returns MsgVpnDistributedCacheClusterResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/distributedCaches/${getEncodedUri(cacheName)}/clusters`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get a Cache Cluster object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + cacheName - The name of the Distributed Cache.
    # + clusterName - The name of the Cache Cluster.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The Cache Cluster object's attributes, and the request metadata. 
    remote isolated function getMsgVpnDistributedCacheCluster(string msgVpnName, string cacheName, string clusterName, map<string|string[]> headers = {}, *GetMsgVpnDistributedCacheClusterQueries queries) returns MsgVpnDistributedCacheClusterResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/distributedCaches/${getEncodedUri(cacheName)}/clusters/${getEncodedUri(clusterName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Replace a Cache Cluster object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + cacheName - The name of the Distributed Cache.
    # + clusterName - The name of the Cache Cluster.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Cache Cluster object's attributes. 
    # + return - The Cache Cluster object's attributes after being replaced, and the request metadata. 
    remote isolated function replaceMsgVpnDistributedCacheCluster(string msgVpnName, string cacheName, string clusterName, MsgVpnDistributedCacheCluster payload, map<string|string[]> headers = {}, *ReplaceMsgVpnDistributedCacheClusterQueries queries) returns MsgVpnDistributedCacheClusterResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/distributedCaches/${getEncodedUri(cacheName)}/clusters/${getEncodedUri(clusterName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Delete a Cache Cluster object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + cacheName - The name of the Distributed Cache.
    # + clusterName - The name of the Cache Cluster.
    # + headers - Headers to be sent with the request 
    # + return - The request metadata. 
    remote isolated function deleteMsgVpnDistributedCacheCluster(string msgVpnName, string cacheName, string clusterName, map<string|string[]> headers = {}) returns SempMetaOnlyResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/distributedCaches/${getEncodedUri(cacheName)}/clusters/${getEncodedUri(clusterName)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Update a Cache Cluster object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + cacheName - The name of the Distributed Cache.
    # + clusterName - The name of the Cache Cluster.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Cache Cluster object's attributes. 
    # + return - The Cache Cluster object's attributes after being updated, and the request metadata. 
    remote isolated function updateMsgVpnDistributedCacheCluster(string msgVpnName, string cacheName, string clusterName, MsgVpnDistributedCacheCluster payload, map<string|string[]> headers = {}, *UpdateMsgVpnDistributedCacheClusterQueries queries) returns MsgVpnDistributedCacheClusterResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/distributedCaches/${getEncodedUri(cacheName)}/clusters/${getEncodedUri(clusterName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Get a list of Home Cache Cluster objects.
    #
    # + msgVpnName - The name of the Message VPN.
    # + cacheName - The name of the Distributed Cache.
    # + clusterName - The name of the Cache Cluster.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The list of Home Cache Cluster objects' attributes, and the request metadata. 
    remote isolated function getMsgVpnDistributedCacheClusterGlobalCachingHomeClusters(string msgVpnName, string cacheName, string clusterName, map<string|string[]> headers = {}, *GetMsgVpnDistributedCacheClusterGlobalCachingHomeClustersQueries queries) returns MsgVpnDistributedCacheClusterGlobalCachingHomeClustersResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/distributedCaches/${getEncodedUri(cacheName)}/clusters/${getEncodedUri(clusterName)}/globalCachingHomeClusters`;
        map<Encoding> queryParamEncoding = {"where": {style: FORM, explode: false}, "select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create a Home Cache Cluster object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + cacheName - The name of the Distributed Cache.
    # + clusterName - The name of the Cache Cluster.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Home Cache Cluster object's attributes. 
    # + return - The Home Cache Cluster object's attributes after being created, and the request metadata. 
    remote isolated function createMsgVpnDistributedCacheClusterGlobalCachingHomeCluster(string msgVpnName, string cacheName, string clusterName, MsgVpnDistributedCacheClusterGlobalCachingHomeCluster payload, map<string|string[]> headers = {}, *CreateMsgVpnDistributedCacheClusterGlobalCachingHomeClusterQueries queries) returns MsgVpnDistributedCacheClusterGlobalCachingHomeClusterResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/distributedCaches/${getEncodedUri(cacheName)}/clusters/${getEncodedUri(clusterName)}/globalCachingHomeClusters`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get a Home Cache Cluster object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + cacheName - The name of the Distributed Cache.
    # + clusterName - The name of the Cache Cluster.
    # + homeClusterName - The name of the remote Home Cache Cluster.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The Home Cache Cluster object's attributes, and the request metadata. 
    remote isolated function getMsgVpnDistributedCacheClusterGlobalCachingHomeCluster(string msgVpnName, string cacheName, string clusterName, string homeClusterName, map<string|string[]> headers = {}, *GetMsgVpnDistributedCacheClusterGlobalCachingHomeClusterQueries queries) returns MsgVpnDistributedCacheClusterGlobalCachingHomeClusterResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/distributedCaches/${getEncodedUri(cacheName)}/clusters/${getEncodedUri(clusterName)}/globalCachingHomeClusters/${getEncodedUri(homeClusterName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete a Home Cache Cluster object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + cacheName - The name of the Distributed Cache.
    # + clusterName - The name of the Cache Cluster.
    # + homeClusterName - The name of the remote Home Cache Cluster.
    # + headers - Headers to be sent with the request 
    # + return - The request metadata. 
    remote isolated function deleteMsgVpnDistributedCacheClusterGlobalCachingHomeCluster(string msgVpnName, string cacheName, string clusterName, string homeClusterName, map<string|string[]> headers = {}) returns SempMetaOnlyResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/distributedCaches/${getEncodedUri(cacheName)}/clusters/${getEncodedUri(clusterName)}/globalCachingHomeClusters/${getEncodedUri(homeClusterName)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Get a list of Topic Prefix objects.
    #
    # + msgVpnName - The name of the Message VPN.
    # + cacheName - The name of the Distributed Cache.
    # + clusterName - The name of the Cache Cluster.
    # + homeClusterName - The name of the remote Home Cache Cluster.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The list of Topic Prefix objects' attributes, and the request metadata. 
    remote isolated function getMsgVpnDistributedCacheClusterGlobalCachingHomeClusterTopicPrefixes(string msgVpnName, string cacheName, string clusterName, string homeClusterName, map<string|string[]> headers = {}, *GetMsgVpnDistributedCacheClusterGlobalCachingHomeClusterTopicPrefixesQueries queries) returns MsgVpnDistributedCacheClusterGlobalCachingHomeClusterTopicPrefixesResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/distributedCaches/${getEncodedUri(cacheName)}/clusters/${getEncodedUri(clusterName)}/globalCachingHomeClusters/${getEncodedUri(homeClusterName)}/topicPrefixes`;
        map<Encoding> queryParamEncoding = {"where": {style: FORM, explode: false}, "select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create a Topic Prefix object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + cacheName - The name of the Distributed Cache.
    # + clusterName - The name of the Cache Cluster.
    # + homeClusterName - The name of the remote Home Cache Cluster.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Topic Prefix object's attributes. 
    # + return - The Topic Prefix object's attributes after being created, and the request metadata. 
    remote isolated function createMsgVpnDistributedCacheClusterGlobalCachingHomeClusterTopicPrefix(string msgVpnName, string cacheName, string clusterName, string homeClusterName, MsgVpnDistributedCacheClusterGlobalCachingHomeClusterTopicPrefix payload, map<string|string[]> headers = {}, *CreateMsgVpnDistributedCacheClusterGlobalCachingHomeClusterTopicPrefixQueries queries) returns MsgVpnDistributedCacheClusterGlobalCachingHomeClusterTopicPrefixResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/distributedCaches/${getEncodedUri(cacheName)}/clusters/${getEncodedUri(clusterName)}/globalCachingHomeClusters/${getEncodedUri(homeClusterName)}/topicPrefixes`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get a Topic Prefix object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + cacheName - The name of the Distributed Cache.
    # + clusterName - The name of the Cache Cluster.
    # + homeClusterName - The name of the remote Home Cache Cluster.
    # + topicPrefix - A topic prefix for global topics available from the remote Home Cache Cluster. A wildcard (/>) is implied at the end of the prefix.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The Topic Prefix object's attributes, and the request metadata. 
    remote isolated function getMsgVpnDistributedCacheClusterGlobalCachingHomeClusterTopicPrefix(string msgVpnName, string cacheName, string clusterName, string homeClusterName, string topicPrefix, map<string|string[]> headers = {}, *GetMsgVpnDistributedCacheClusterGlobalCachingHomeClusterTopicPrefixQueries queries) returns MsgVpnDistributedCacheClusterGlobalCachingHomeClusterTopicPrefixResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/distributedCaches/${getEncodedUri(cacheName)}/clusters/${getEncodedUri(clusterName)}/globalCachingHomeClusters/${getEncodedUri(homeClusterName)}/topicPrefixes/${getEncodedUri(topicPrefix)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete a Topic Prefix object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + cacheName - The name of the Distributed Cache.
    # + clusterName - The name of the Cache Cluster.
    # + homeClusterName - The name of the remote Home Cache Cluster.
    # + topicPrefix - A topic prefix for global topics available from the remote Home Cache Cluster. A wildcard (/>) is implied at the end of the prefix.
    # + headers - Headers to be sent with the request 
    # + return - The request metadata. 
    remote isolated function deleteMsgVpnDistributedCacheClusterGlobalCachingHomeClusterTopicPrefix(string msgVpnName, string cacheName, string clusterName, string homeClusterName, string topicPrefix, map<string|string[]> headers = {}) returns SempMetaOnlyResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/distributedCaches/${getEncodedUri(cacheName)}/clusters/${getEncodedUri(clusterName)}/globalCachingHomeClusters/${getEncodedUri(homeClusterName)}/topicPrefixes/${getEncodedUri(topicPrefix)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Get a list of Cache Instance objects.
    #
    # + msgVpnName - The name of the Message VPN.
    # + cacheName - The name of the Distributed Cache.
    # + clusterName - The name of the Cache Cluster.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The list of Cache Instance objects' attributes, and the request metadata. 
    remote isolated function getMsgVpnDistributedCacheClusterInstances(string msgVpnName, string cacheName, string clusterName, map<string|string[]> headers = {}, *GetMsgVpnDistributedCacheClusterInstancesQueries queries) returns MsgVpnDistributedCacheClusterInstancesResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/distributedCaches/${getEncodedUri(cacheName)}/clusters/${getEncodedUri(clusterName)}/instances`;
        map<Encoding> queryParamEncoding = {"where": {style: FORM, explode: false}, "select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create a Cache Instance object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + cacheName - The name of the Distributed Cache.
    # + clusterName - The name of the Cache Cluster.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Cache Instance object's attributes. 
    # + return - The Cache Instance object's attributes after being created, and the request metadata. 
    remote isolated function createMsgVpnDistributedCacheClusterInstance(string msgVpnName, string cacheName, string clusterName, MsgVpnDistributedCacheClusterInstance payload, map<string|string[]> headers = {}, *CreateMsgVpnDistributedCacheClusterInstanceQueries queries) returns MsgVpnDistributedCacheClusterInstanceResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/distributedCaches/${getEncodedUri(cacheName)}/clusters/${getEncodedUri(clusterName)}/instances`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get a Cache Instance object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + cacheName - The name of the Distributed Cache.
    # + clusterName - The name of the Cache Cluster.
    # + instanceName - The name of the Cache Instance.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The Cache Instance object's attributes, and the request metadata. 
    remote isolated function getMsgVpnDistributedCacheClusterInstance(string msgVpnName, string cacheName, string clusterName, string instanceName, map<string|string[]> headers = {}, *GetMsgVpnDistributedCacheClusterInstanceQueries queries) returns MsgVpnDistributedCacheClusterInstanceResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/distributedCaches/${getEncodedUri(cacheName)}/clusters/${getEncodedUri(clusterName)}/instances/${getEncodedUri(instanceName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Replace a Cache Instance object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + cacheName - The name of the Distributed Cache.
    # + clusterName - The name of the Cache Cluster.
    # + instanceName - The name of the Cache Instance.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Cache Instance object's attributes. 
    # + return - The Cache Instance object's attributes after being replaced, and the request metadata. 
    remote isolated function replaceMsgVpnDistributedCacheClusterInstance(string msgVpnName, string cacheName, string clusterName, string instanceName, MsgVpnDistributedCacheClusterInstance payload, map<string|string[]> headers = {}, *ReplaceMsgVpnDistributedCacheClusterInstanceQueries queries) returns MsgVpnDistributedCacheClusterInstanceResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/distributedCaches/${getEncodedUri(cacheName)}/clusters/${getEncodedUri(clusterName)}/instances/${getEncodedUri(instanceName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Delete a Cache Instance object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + cacheName - The name of the Distributed Cache.
    # + clusterName - The name of the Cache Cluster.
    # + instanceName - The name of the Cache Instance.
    # + headers - Headers to be sent with the request 
    # + return - The request metadata. 
    remote isolated function deleteMsgVpnDistributedCacheClusterInstance(string msgVpnName, string cacheName, string clusterName, string instanceName, map<string|string[]> headers = {}) returns SempMetaOnlyResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/distributedCaches/${getEncodedUri(cacheName)}/clusters/${getEncodedUri(clusterName)}/instances/${getEncodedUri(instanceName)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Update a Cache Instance object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + cacheName - The name of the Distributed Cache.
    # + clusterName - The name of the Cache Cluster.
    # + instanceName - The name of the Cache Instance.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Cache Instance object's attributes. 
    # + return - The Cache Instance object's attributes after being updated, and the request metadata. 
    remote isolated function updateMsgVpnDistributedCacheClusterInstance(string msgVpnName, string cacheName, string clusterName, string instanceName, MsgVpnDistributedCacheClusterInstance payload, map<string|string[]> headers = {}, *UpdateMsgVpnDistributedCacheClusterInstanceQueries queries) returns MsgVpnDistributedCacheClusterInstanceResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/distributedCaches/${getEncodedUri(cacheName)}/clusters/${getEncodedUri(clusterName)}/instances/${getEncodedUri(instanceName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Get a list of Topic objects.
    #
    # + msgVpnName - The name of the Message VPN.
    # + cacheName - The name of the Distributed Cache.
    # + clusterName - The name of the Cache Cluster.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The list of Topic objects' attributes, and the request metadata. 
    remote isolated function getMsgVpnDistributedCacheClusterTopics(string msgVpnName, string cacheName, string clusterName, map<string|string[]> headers = {}, *GetMsgVpnDistributedCacheClusterTopicsQueries queries) returns MsgVpnDistributedCacheClusterTopicsResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/distributedCaches/${getEncodedUri(cacheName)}/clusters/${getEncodedUri(clusterName)}/topics`;
        map<Encoding> queryParamEncoding = {"where": {style: FORM, explode: false}, "select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create a Topic object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + cacheName - The name of the Distributed Cache.
    # + clusterName - The name of the Cache Cluster.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Topic object's attributes. 
    # + return - The Topic object's attributes after being created, and the request metadata. 
    remote isolated function createMsgVpnDistributedCacheClusterTopic(string msgVpnName, string cacheName, string clusterName, MsgVpnDistributedCacheClusterTopic payload, map<string|string[]> headers = {}, *CreateMsgVpnDistributedCacheClusterTopicQueries queries) returns MsgVpnDistributedCacheClusterTopicResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/distributedCaches/${getEncodedUri(cacheName)}/clusters/${getEncodedUri(clusterName)}/topics`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get a Topic object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + cacheName - The name of the Distributed Cache.
    # + clusterName - The name of the Cache Cluster.
    # + topic - The value of the Topic in the form a/b/c.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The Topic object's attributes, and the request metadata. 
    remote isolated function getMsgVpnDistributedCacheClusterTopic(string msgVpnName, string cacheName, string clusterName, string topic, map<string|string[]> headers = {}, *GetMsgVpnDistributedCacheClusterTopicQueries queries) returns MsgVpnDistributedCacheClusterTopicResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/distributedCaches/${getEncodedUri(cacheName)}/clusters/${getEncodedUri(clusterName)}/topics/${getEncodedUri(topic)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete a Topic object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + cacheName - The name of the Distributed Cache.
    # + clusterName - The name of the Cache Cluster.
    # + topic - The value of the Topic in the form a/b/c.
    # + headers - Headers to be sent with the request 
    # + return - The request metadata. 
    remote isolated function deleteMsgVpnDistributedCacheClusterTopic(string msgVpnName, string cacheName, string clusterName, string topic, map<string|string[]> headers = {}) returns SempMetaOnlyResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/distributedCaches/${getEncodedUri(cacheName)}/clusters/${getEncodedUri(clusterName)}/topics/${getEncodedUri(topic)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Get a list of DMR Bridge objects.
    #
    # + msgVpnName - The name of the Message VPN.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The list of DMR Bridge objects' attributes, and the request metadata. 
    remote isolated function getMsgVpnDmrBridges(string msgVpnName, map<string|string[]> headers = {}, *GetMsgVpnDmrBridgesQueries queries) returns MsgVpnDmrBridgesResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/dmrBridges`;
        map<Encoding> queryParamEncoding = {"where": {style: FORM, explode: false}, "select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create a DMR Bridge object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The DMR Bridge object's attributes. 
    # + return - The DMR Bridge object's attributes after being created, and the request metadata. 
    remote isolated function createMsgVpnDmrBridge(string msgVpnName, MsgVpnDmrBridge payload, map<string|string[]> headers = {}, *CreateMsgVpnDmrBridgeQueries queries) returns MsgVpnDmrBridgeResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/dmrBridges`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get a DMR Bridge object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + remoteNodeName - The name of the node at the remote end of the DMR Bridge.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The DMR Bridge object's attributes, and the request metadata. 
    remote isolated function getMsgVpnDmrBridge(string msgVpnName, string remoteNodeName, map<string|string[]> headers = {}, *GetMsgVpnDmrBridgeQueries queries) returns MsgVpnDmrBridgeResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/dmrBridges/${getEncodedUri(remoteNodeName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Replace a DMR Bridge object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + remoteNodeName - The name of the node at the remote end of the DMR Bridge.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The DMR Bridge object's attributes. 
    # + return - The DMR Bridge object's attributes after being replaced, and the request metadata. 
    remote isolated function replaceMsgVpnDmrBridge(string msgVpnName, string remoteNodeName, MsgVpnDmrBridge payload, map<string|string[]> headers = {}, *ReplaceMsgVpnDmrBridgeQueries queries) returns MsgVpnDmrBridgeResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/dmrBridges/${getEncodedUri(remoteNodeName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Delete a DMR Bridge object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + remoteNodeName - The name of the node at the remote end of the DMR Bridge.
    # + headers - Headers to be sent with the request 
    # + return - The request metadata. 
    remote isolated function deleteMsgVpnDmrBridge(string msgVpnName, string remoteNodeName, map<string|string[]> headers = {}) returns SempMetaOnlyResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/dmrBridges/${getEncodedUri(remoteNodeName)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Update a DMR Bridge object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + remoteNodeName - The name of the node at the remote end of the DMR Bridge.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The DMR Bridge object's attributes. 
    # + return - The DMR Bridge object's attributes after being updated, and the request metadata. 
    remote isolated function updateMsgVpnDmrBridge(string msgVpnName, string remoteNodeName, MsgVpnDmrBridge payload, map<string|string[]> headers = {}, *UpdateMsgVpnDmrBridgeQueries queries) returns MsgVpnDmrBridgeResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/dmrBridges/${getEncodedUri(remoteNodeName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Get a list of JNDI Connection Factory objects.
    #
    # + msgVpnName - The name of the Message VPN.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The list of JNDI Connection Factory objects' attributes, and the request metadata. 
    remote isolated function getMsgVpnJndiConnectionFactories(string msgVpnName, map<string|string[]> headers = {}, *GetMsgVpnJndiConnectionFactoriesQueries queries) returns MsgVpnJndiConnectionFactoriesResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/jndiConnectionFactories`;
        map<Encoding> queryParamEncoding = {"where": {style: FORM, explode: false}, "select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create a JNDI Connection Factory object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The JNDI Connection Factory object's attributes. 
    # + return - The JNDI Connection Factory object's attributes after being created, and the request metadata. 
    remote isolated function createMsgVpnJndiConnectionFactory(string msgVpnName, MsgVpnJndiConnectionFactory payload, map<string|string[]> headers = {}, *CreateMsgVpnJndiConnectionFactoryQueries queries) returns MsgVpnJndiConnectionFactoryResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/jndiConnectionFactories`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get a JNDI Connection Factory object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + connectionFactoryName - The name of the JMS Connection Factory.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The JNDI Connection Factory object's attributes, and the request metadata. 
    remote isolated function getMsgVpnJndiConnectionFactory(string msgVpnName, string connectionFactoryName, map<string|string[]> headers = {}, *GetMsgVpnJndiConnectionFactoryQueries queries) returns MsgVpnJndiConnectionFactoryResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/jndiConnectionFactories/${getEncodedUri(connectionFactoryName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Replace a JNDI Connection Factory object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + connectionFactoryName - The name of the JMS Connection Factory.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The JNDI Connection Factory object's attributes. 
    # + return - The JNDI Connection Factory object's attributes after being replaced, and the request metadata. 
    remote isolated function replaceMsgVpnJndiConnectionFactory(string msgVpnName, string connectionFactoryName, MsgVpnJndiConnectionFactory payload, map<string|string[]> headers = {}, *ReplaceMsgVpnJndiConnectionFactoryQueries queries) returns MsgVpnJndiConnectionFactoryResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/jndiConnectionFactories/${getEncodedUri(connectionFactoryName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Delete a JNDI Connection Factory object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + connectionFactoryName - The name of the JMS Connection Factory.
    # + headers - Headers to be sent with the request 
    # + return - The request metadata. 
    remote isolated function deleteMsgVpnJndiConnectionFactory(string msgVpnName, string connectionFactoryName, map<string|string[]> headers = {}) returns SempMetaOnlyResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/jndiConnectionFactories/${getEncodedUri(connectionFactoryName)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Update a JNDI Connection Factory object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + connectionFactoryName - The name of the JMS Connection Factory.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The JNDI Connection Factory object's attributes. 
    # + return - The JNDI Connection Factory object's attributes after being updated, and the request metadata. 
    remote isolated function updateMsgVpnJndiConnectionFactory(string msgVpnName, string connectionFactoryName, MsgVpnJndiConnectionFactory payload, map<string|string[]> headers = {}, *UpdateMsgVpnJndiConnectionFactoryQueries queries) returns MsgVpnJndiConnectionFactoryResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/jndiConnectionFactories/${getEncodedUri(connectionFactoryName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Get a list of JNDI Queue objects.
    #
    # + msgVpnName - The name of the Message VPN.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The list of JNDI Queue objects' attributes, and the request metadata. 
    remote isolated function getMsgVpnJndiQueues(string msgVpnName, map<string|string[]> headers = {}, *GetMsgVpnJndiQueuesQueries queries) returns MsgVpnJndiQueuesResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/jndiQueues`;
        map<Encoding> queryParamEncoding = {"where": {style: FORM, explode: false}, "select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create a JNDI Queue object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The JNDI Queue object's attributes. 
    # + return - The JNDI Queue object's attributes after being created, and the request metadata. 
    remote isolated function createMsgVpnJndiQueue(string msgVpnName, MsgVpnJndiQueue payload, map<string|string[]> headers = {}, *CreateMsgVpnJndiQueueQueries queries) returns MsgVpnJndiQueueResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/jndiQueues`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get a JNDI Queue object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + queueName - The JNDI name of the JMS Queue.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The JNDI Queue object's attributes, and the request metadata. 
    remote isolated function getMsgVpnJndiQueue(string msgVpnName, string queueName, map<string|string[]> headers = {}, *GetMsgVpnJndiQueueQueries queries) returns MsgVpnJndiQueueResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/jndiQueues/${getEncodedUri(queueName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Replace a JNDI Queue object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + queueName - The JNDI name of the JMS Queue.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The JNDI Queue object's attributes. 
    # + return - The JNDI Queue object's attributes after being replaced, and the request metadata. 
    remote isolated function replaceMsgVpnJndiQueue(string msgVpnName, string queueName, MsgVpnJndiQueue payload, map<string|string[]> headers = {}, *ReplaceMsgVpnJndiQueueQueries queries) returns MsgVpnJndiQueueResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/jndiQueues/${getEncodedUri(queueName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Delete a JNDI Queue object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + queueName - The JNDI name of the JMS Queue.
    # + headers - Headers to be sent with the request 
    # + return - The request metadata. 
    remote isolated function deleteMsgVpnJndiQueue(string msgVpnName, string queueName, map<string|string[]> headers = {}) returns SempMetaOnlyResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/jndiQueues/${getEncodedUri(queueName)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Update a JNDI Queue object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + queueName - The JNDI name of the JMS Queue.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The JNDI Queue object's attributes. 
    # + return - The JNDI Queue object's attributes after being updated, and the request metadata. 
    remote isolated function updateMsgVpnJndiQueue(string msgVpnName, string queueName, MsgVpnJndiQueue payload, map<string|string[]> headers = {}, *UpdateMsgVpnJndiQueueQueries queries) returns MsgVpnJndiQueueResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/jndiQueues/${getEncodedUri(queueName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Get a list of JNDI Topic objects.
    #
    # + msgVpnName - The name of the Message VPN.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The list of JNDI Topic objects' attributes, and the request metadata. 
    remote isolated function getMsgVpnJndiTopics(string msgVpnName, map<string|string[]> headers = {}, *GetMsgVpnJndiTopicsQueries queries) returns MsgVpnJndiTopicsResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/jndiTopics`;
        map<Encoding> queryParamEncoding = {"where": {style: FORM, explode: false}, "select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create a JNDI Topic object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The JNDI Topic object's attributes. 
    # + return - The JNDI Topic object's attributes after being created, and the request metadata. 
    remote isolated function createMsgVpnJndiTopic(string msgVpnName, MsgVpnJndiTopic payload, map<string|string[]> headers = {}, *CreateMsgVpnJndiTopicQueries queries) returns MsgVpnJndiTopicResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/jndiTopics`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get a JNDI Topic object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + topicName - The JNDI name of the JMS Topic.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The JNDI Topic object's attributes, and the request metadata. 
    remote isolated function getMsgVpnJndiTopic(string msgVpnName, string topicName, map<string|string[]> headers = {}, *GetMsgVpnJndiTopicQueries queries) returns MsgVpnJndiTopicResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/jndiTopics/${getEncodedUri(topicName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Replace a JNDI Topic object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + topicName - The JNDI name of the JMS Topic.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The JNDI Topic object's attributes. 
    # + return - The JNDI Topic object's attributes after being replaced, and the request metadata. 
    remote isolated function replaceMsgVpnJndiTopic(string msgVpnName, string topicName, MsgVpnJndiTopic payload, map<string|string[]> headers = {}, *ReplaceMsgVpnJndiTopicQueries queries) returns MsgVpnJndiTopicResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/jndiTopics/${getEncodedUri(topicName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Delete a JNDI Topic object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + topicName - The JNDI name of the JMS Topic.
    # + headers - Headers to be sent with the request 
    # + return - The request metadata. 
    remote isolated function deleteMsgVpnJndiTopic(string msgVpnName, string topicName, map<string|string[]> headers = {}) returns SempMetaOnlyResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/jndiTopics/${getEncodedUri(topicName)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Update a JNDI Topic object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + topicName - The JNDI name of the JMS Topic.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The JNDI Topic object's attributes. 
    # + return - The JNDI Topic object's attributes after being updated, and the request metadata. 
    remote isolated function updateMsgVpnJndiTopic(string msgVpnName, string topicName, MsgVpnJndiTopic payload, map<string|string[]> headers = {}, *UpdateMsgVpnJndiTopicQueries queries) returns MsgVpnJndiTopicResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/jndiTopics/${getEncodedUri(topicName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Get a list of Kafka Receiver objects.
    #
    # + msgVpnName - The name of the Message VPN.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The list of Kafka Receiver objects' attributes, and the request metadata. 
    remote isolated function getMsgVpnKafkaReceivers(string msgVpnName, map<string|string[]> headers = {}, *GetMsgVpnKafkaReceiversQueries queries) returns MsgVpnKafkaReceiversResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/kafkaReceivers`;
        map<Encoding> queryParamEncoding = {"where": {style: FORM, explode: false}, "select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create a Kafka Receiver object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Kafka Receiver object's attributes. 
    # + return - The Kafka Receiver object's attributes after being created, and the request metadata. 
    remote isolated function createMsgVpnKafkaReceiver(string msgVpnName, MsgVpnKafkaReceiver payload, map<string|string[]> headers = {}, *CreateMsgVpnKafkaReceiverQueries queries) returns MsgVpnKafkaReceiverResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/kafkaReceivers`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get a Kafka Receiver object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + kafkaReceiverName - The name of the Kafka Receiver.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The Kafka Receiver object's attributes, and the request metadata. 
    remote isolated function getMsgVpnKafkaReceiver(string msgVpnName, string kafkaReceiverName, map<string|string[]> headers = {}, *GetMsgVpnKafkaReceiverQueries queries) returns MsgVpnKafkaReceiverResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/kafkaReceivers/${getEncodedUri(kafkaReceiverName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Replace a Kafka Receiver object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + kafkaReceiverName - The name of the Kafka Receiver.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Kafka Receiver object's attributes. 
    # + return - The Kafka Receiver object's attributes after being replaced, and the request metadata. 
    remote isolated function replaceMsgVpnKafkaReceiver(string msgVpnName, string kafkaReceiverName, MsgVpnKafkaReceiver payload, map<string|string[]> headers = {}, *ReplaceMsgVpnKafkaReceiverQueries queries) returns MsgVpnKafkaReceiverResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/kafkaReceivers/${getEncodedUri(kafkaReceiverName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Delete a Kafka Receiver object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + kafkaReceiverName - The name of the Kafka Receiver.
    # + headers - Headers to be sent with the request 
    # + return - The request metadata. 
    remote isolated function deleteMsgVpnKafkaReceiver(string msgVpnName, string kafkaReceiverName, map<string|string[]> headers = {}) returns SempMetaOnlyResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/kafkaReceivers/${getEncodedUri(kafkaReceiverName)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Update a Kafka Receiver object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + kafkaReceiverName - The name of the Kafka Receiver.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Kafka Receiver object's attributes. 
    # + return - The Kafka Receiver object's attributes after being updated, and the request metadata. 
    remote isolated function updateMsgVpnKafkaReceiver(string msgVpnName, string kafkaReceiverName, MsgVpnKafkaReceiver payload, map<string|string[]> headers = {}, *UpdateMsgVpnKafkaReceiverQueries queries) returns MsgVpnKafkaReceiverResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/kafkaReceivers/${getEncodedUri(kafkaReceiverName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Get a list of Topic Binding objects.
    #
    # + msgVpnName - The name of the Message VPN.
    # + kafkaReceiverName - The name of the Kafka Receiver.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The list of Topic Binding objects' attributes, and the request metadata. 
    remote isolated function getMsgVpnKafkaReceiverTopicBindings(string msgVpnName, string kafkaReceiverName, map<string|string[]> headers = {}, *GetMsgVpnKafkaReceiverTopicBindingsQueries queries) returns MsgVpnKafkaReceiverTopicBindingsResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/kafkaReceivers/${getEncodedUri(kafkaReceiverName)}/topicBindings`;
        map<Encoding> queryParamEncoding = {"where": {style: FORM, explode: false}, "select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create a Topic Binding object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + kafkaReceiverName - The name of the Kafka Receiver.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Topic Binding object's attributes. 
    # + return - The Topic Binding object's attributes after being created, and the request metadata. 
    remote isolated function createMsgVpnKafkaReceiverTopicBinding(string msgVpnName, string kafkaReceiverName, MsgVpnKafkaReceiverTopicBinding payload, map<string|string[]> headers = {}, *CreateMsgVpnKafkaReceiverTopicBindingQueries queries) returns MsgVpnKafkaReceiverTopicBindingResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/kafkaReceivers/${getEncodedUri(kafkaReceiverName)}/topicBindings`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get a Topic Binding object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + kafkaReceiverName - The name of the Kafka Receiver.
    # + topicName - The name of the Topic or a POSIX.2 regular expression starting with '^'.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The Topic Binding object's attributes, and the request metadata. 
    remote isolated function getMsgVpnKafkaReceiverTopicBinding(string msgVpnName, string kafkaReceiverName, string topicName, map<string|string[]> headers = {}, *GetMsgVpnKafkaReceiverTopicBindingQueries queries) returns MsgVpnKafkaReceiverTopicBindingResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/kafkaReceivers/${getEncodedUri(kafkaReceiverName)}/topicBindings/${getEncodedUri(topicName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Replace a Topic Binding object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + kafkaReceiverName - The name of the Kafka Receiver.
    # + topicName - The name of the Topic or a POSIX.2 regular expression starting with '^'.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Topic Binding object's attributes. 
    # + return - The Topic Binding object's attributes after being replaced, and the request metadata. 
    remote isolated function replaceMsgVpnKafkaReceiverTopicBinding(string msgVpnName, string kafkaReceiverName, string topicName, MsgVpnKafkaReceiverTopicBinding payload, map<string|string[]> headers = {}, *ReplaceMsgVpnKafkaReceiverTopicBindingQueries queries) returns MsgVpnKafkaReceiverTopicBindingResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/kafkaReceivers/${getEncodedUri(kafkaReceiverName)}/topicBindings/${getEncodedUri(topicName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Delete a Topic Binding object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + kafkaReceiverName - The name of the Kafka Receiver.
    # + topicName - The name of the Topic or a POSIX.2 regular expression starting with '^'.
    # + headers - Headers to be sent with the request 
    # + return - The request metadata. 
    remote isolated function deleteMsgVpnKafkaReceiverTopicBinding(string msgVpnName, string kafkaReceiverName, string topicName, map<string|string[]> headers = {}) returns SempMetaOnlyResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/kafkaReceivers/${getEncodedUri(kafkaReceiverName)}/topicBindings/${getEncodedUri(topicName)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Update a Topic Binding object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + kafkaReceiverName - The name of the Kafka Receiver.
    # + topicName - The name of the Topic or a POSIX.2 regular expression starting with '^'.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Topic Binding object's attributes. 
    # + return - The Topic Binding object's attributes after being updated, and the request metadata. 
    remote isolated function updateMsgVpnKafkaReceiverTopicBinding(string msgVpnName, string kafkaReceiverName, string topicName, MsgVpnKafkaReceiverTopicBinding payload, map<string|string[]> headers = {}, *UpdateMsgVpnKafkaReceiverTopicBindingQueries queries) returns MsgVpnKafkaReceiverTopicBindingResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/kafkaReceivers/${getEncodedUri(kafkaReceiverName)}/topicBindings/${getEncodedUri(topicName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Get a list of Kafka Sender objects.
    #
    # + msgVpnName - The name of the Message VPN.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The list of Kafka Sender objects' attributes, and the request metadata. 
    remote isolated function getMsgVpnKafkaSenders(string msgVpnName, map<string|string[]> headers = {}, *GetMsgVpnKafkaSendersQueries queries) returns MsgVpnKafkaSendersResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/kafkaSenders`;
        map<Encoding> queryParamEncoding = {"where": {style: FORM, explode: false}, "select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create a Kafka Sender object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Kafka Sender object's attributes. 
    # + return - The Kafka Sender object's attributes after being created, and the request metadata. 
    remote isolated function createMsgVpnKafkaSender(string msgVpnName, MsgVpnKafkaSender payload, map<string|string[]> headers = {}, *CreateMsgVpnKafkaSenderQueries queries) returns MsgVpnKafkaSenderResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/kafkaSenders`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get a Kafka Sender object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + kafkaSenderName - The name of the Kafka Sender.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The Kafka Sender object's attributes, and the request metadata. 
    remote isolated function getMsgVpnKafkaSender(string msgVpnName, string kafkaSenderName, map<string|string[]> headers = {}, *GetMsgVpnKafkaSenderQueries queries) returns MsgVpnKafkaSenderResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/kafkaSenders/${getEncodedUri(kafkaSenderName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Replace a Kafka Sender object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + kafkaSenderName - The name of the Kafka Sender.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Kafka Sender object's attributes. 
    # + return - The Kafka Sender object's attributes after being replaced, and the request metadata. 
    remote isolated function replaceMsgVpnKafkaSender(string msgVpnName, string kafkaSenderName, MsgVpnKafkaSender payload, map<string|string[]> headers = {}, *ReplaceMsgVpnKafkaSenderQueries queries) returns MsgVpnKafkaSenderResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/kafkaSenders/${getEncodedUri(kafkaSenderName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Delete a Kafka Sender object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + kafkaSenderName - The name of the Kafka Sender.
    # + headers - Headers to be sent with the request 
    # + return - The request metadata. 
    remote isolated function deleteMsgVpnKafkaSender(string msgVpnName, string kafkaSenderName, map<string|string[]> headers = {}) returns SempMetaOnlyResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/kafkaSenders/${getEncodedUri(kafkaSenderName)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Update a Kafka Sender object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + kafkaSenderName - The name of the Kafka Sender.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Kafka Sender object's attributes. 
    # + return - The Kafka Sender object's attributes after being updated, and the request metadata. 
    remote isolated function updateMsgVpnKafkaSender(string msgVpnName, string kafkaSenderName, MsgVpnKafkaSender payload, map<string|string[]> headers = {}, *UpdateMsgVpnKafkaSenderQueries queries) returns MsgVpnKafkaSenderResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/kafkaSenders/${getEncodedUri(kafkaSenderName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Get a list of Queue Binding objects.
    #
    # + msgVpnName - The name of the Message VPN.
    # + kafkaSenderName - The name of the Kafka Sender.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The list of Queue Binding objects' attributes, and the request metadata. 
    remote isolated function getMsgVpnKafkaSenderQueueBindings(string msgVpnName, string kafkaSenderName, map<string|string[]> headers = {}, *GetMsgVpnKafkaSenderQueueBindingsQueries queries) returns MsgVpnKafkaSenderQueueBindingsResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/kafkaSenders/${getEncodedUri(kafkaSenderName)}/queueBindings`;
        map<Encoding> queryParamEncoding = {"where": {style: FORM, explode: false}, "select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create a Queue Binding object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + kafkaSenderName - The name of the Kafka Sender.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Queue Binding object's attributes. 
    # + return - The Queue Binding object's attributes after being created, and the request metadata. 
    remote isolated function createMsgVpnKafkaSenderQueueBinding(string msgVpnName, string kafkaSenderName, MsgVpnKafkaSenderQueueBinding payload, map<string|string[]> headers = {}, *CreateMsgVpnKafkaSenderQueueBindingQueries queries) returns MsgVpnKafkaSenderQueueBindingResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/kafkaSenders/${getEncodedUri(kafkaSenderName)}/queueBindings`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get a Queue Binding object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + kafkaSenderName - The name of the Kafka Sender.
    # + queueName - The name of the Queue.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The Queue Binding object's attributes, and the request metadata. 
    remote isolated function getMsgVpnKafkaSenderQueueBinding(string msgVpnName, string kafkaSenderName, string queueName, map<string|string[]> headers = {}, *GetMsgVpnKafkaSenderQueueBindingQueries queries) returns MsgVpnKafkaSenderQueueBindingResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/kafkaSenders/${getEncodedUri(kafkaSenderName)}/queueBindings/${getEncodedUri(queueName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Replace a Queue Binding object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + kafkaSenderName - The name of the Kafka Sender.
    # + queueName - The name of the Queue.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Queue Binding object's attributes. 
    # + return - The Queue Binding object's attributes after being replaced, and the request metadata. 
    remote isolated function replaceMsgVpnKafkaSenderQueueBinding(string msgVpnName, string kafkaSenderName, string queueName, MsgVpnKafkaSenderQueueBinding payload, map<string|string[]> headers = {}, *ReplaceMsgVpnKafkaSenderQueueBindingQueries queries) returns MsgVpnKafkaSenderQueueBindingResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/kafkaSenders/${getEncodedUri(kafkaSenderName)}/queueBindings/${getEncodedUri(queueName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Delete a Queue Binding object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + kafkaSenderName - The name of the Kafka Sender.
    # + queueName - The name of the Queue.
    # + headers - Headers to be sent with the request 
    # + return - The request metadata. 
    remote isolated function deleteMsgVpnKafkaSenderQueueBinding(string msgVpnName, string kafkaSenderName, string queueName, map<string|string[]> headers = {}) returns SempMetaOnlyResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/kafkaSenders/${getEncodedUri(kafkaSenderName)}/queueBindings/${getEncodedUri(queueName)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Update a Queue Binding object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + kafkaSenderName - The name of the Kafka Sender.
    # + queueName - The name of the Queue.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Queue Binding object's attributes. 
    # + return - The Queue Binding object's attributes after being updated, and the request metadata. 
    remote isolated function updateMsgVpnKafkaSenderQueueBinding(string msgVpnName, string kafkaSenderName, string queueName, MsgVpnKafkaSenderQueueBinding payload, map<string|string[]> headers = {}, *UpdateMsgVpnKafkaSenderQueueBindingQueries queries) returns MsgVpnKafkaSenderQueueBindingResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/kafkaSenders/${getEncodedUri(kafkaSenderName)}/queueBindings/${getEncodedUri(queueName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Get a list of MQTT Retain Cache objects.
    #
    # + msgVpnName - The name of the Message VPN.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The list of MQTT Retain Cache objects' attributes, and the request metadata. 
    remote isolated function getMsgVpnMqttRetainCaches(string msgVpnName, map<string|string[]> headers = {}, *GetMsgVpnMqttRetainCachesQueries queries) returns MsgVpnMqttRetainCachesResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/mqttRetainCaches`;
        map<Encoding> queryParamEncoding = {"where": {style: FORM, explode: false}, "select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create an MQTT Retain Cache object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The MQTT Retain Cache object's attributes. 
    # + return - The MQTT Retain Cache object's attributes after being created, and the request metadata. 
    remote isolated function createMsgVpnMqttRetainCache(string msgVpnName, MsgVpnMqttRetainCache payload, map<string|string[]> headers = {}, *CreateMsgVpnMqttRetainCacheQueries queries) returns MsgVpnMqttRetainCacheResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/mqttRetainCaches`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get an MQTT Retain Cache object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + cacheName - The name of the MQTT Retain Cache.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The MQTT Retain Cache object's attributes, and the request metadata. 
    remote isolated function getMsgVpnMqttRetainCache(string msgVpnName, string cacheName, map<string|string[]> headers = {}, *GetMsgVpnMqttRetainCacheQueries queries) returns MsgVpnMqttRetainCacheResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/mqttRetainCaches/${getEncodedUri(cacheName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Replace an MQTT Retain Cache object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + cacheName - The name of the MQTT Retain Cache.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The MQTT Retain Cache object's attributes. 
    # + return - The MQTT Retain Cache object's attributes after being replaced, and the request metadata. 
    remote isolated function replaceMsgVpnMqttRetainCache(string msgVpnName, string cacheName, MsgVpnMqttRetainCache payload, map<string|string[]> headers = {}, *ReplaceMsgVpnMqttRetainCacheQueries queries) returns MsgVpnMqttRetainCacheResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/mqttRetainCaches/${getEncodedUri(cacheName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Delete an MQTT Retain Cache object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + cacheName - The name of the MQTT Retain Cache.
    # + headers - Headers to be sent with the request 
    # + return - The request metadata. 
    remote isolated function deleteMsgVpnMqttRetainCache(string msgVpnName, string cacheName, map<string|string[]> headers = {}) returns SempMetaOnlyResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/mqttRetainCaches/${getEncodedUri(cacheName)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Update an MQTT Retain Cache object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + cacheName - The name of the MQTT Retain Cache.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The MQTT Retain Cache object's attributes. 
    # + return - The MQTT Retain Cache object's attributes after being updated, and the request metadata. 
    remote isolated function updateMsgVpnMqttRetainCache(string msgVpnName, string cacheName, MsgVpnMqttRetainCache payload, map<string|string[]> headers = {}, *UpdateMsgVpnMqttRetainCacheQueries queries) returns MsgVpnMqttRetainCacheResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/mqttRetainCaches/${getEncodedUri(cacheName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Get a list of MQTT Session objects.
    #
    # + msgVpnName - The name of the Message VPN.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The list of MQTT Session objects' attributes, and the request metadata. 
    remote isolated function getMsgVpnMqttSessions(string msgVpnName, map<string|string[]> headers = {}, *GetMsgVpnMqttSessionsQueries queries) returns MsgVpnMqttSessionsResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/mqttSessions`;
        map<Encoding> queryParamEncoding = {"where": {style: FORM, explode: false}, "select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create an MQTT Session object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The MQTT Session object's attributes. 
    # + return - The MQTT Session object's attributes after being created, and the request metadata. 
    remote isolated function createMsgVpnMqttSession(string msgVpnName, MsgVpnMqttSession payload, map<string|string[]> headers = {}, *CreateMsgVpnMqttSessionQueries queries) returns MsgVpnMqttSessionResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/mqttSessions`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get an MQTT Session object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + mqttSessionClientId - The Client ID of the MQTT Session, which corresponds to the ClientId provided in the MQTT CONNECT packet.
    # + mqttSessionVirtualRouter - The virtual router of the MQTT Session.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The MQTT Session object's attributes, and the request metadata. 
    remote isolated function getMsgVpnMqttSession(string msgVpnName, string mqttSessionClientId, string mqttSessionVirtualRouter, map<string|string[]> headers = {}, *GetMsgVpnMqttSessionQueries queries) returns MsgVpnMqttSessionResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/mqttSessions/${getEncodedUri(mqttSessionClientId)},${getEncodedUri(mqttSessionVirtualRouter)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Replace an MQTT Session object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + mqttSessionClientId - The Client ID of the MQTT Session, which corresponds to the ClientId provided in the MQTT CONNECT packet.
    # + mqttSessionVirtualRouter - The virtual router of the MQTT Session.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The MQTT Session object's attributes. 
    # + return - The MQTT Session object's attributes after being replaced, and the request metadata. 
    remote isolated function replaceMsgVpnMqttSession(string msgVpnName, string mqttSessionClientId, string mqttSessionVirtualRouter, MsgVpnMqttSession payload, map<string|string[]> headers = {}, *ReplaceMsgVpnMqttSessionQueries queries) returns MsgVpnMqttSessionResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/mqttSessions/${getEncodedUri(mqttSessionClientId)},${getEncodedUri(mqttSessionVirtualRouter)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Delete an MQTT Session object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + mqttSessionClientId - The Client ID of the MQTT Session, which corresponds to the ClientId provided in the MQTT CONNECT packet.
    # + mqttSessionVirtualRouter - The virtual router of the MQTT Session.
    # + headers - Headers to be sent with the request 
    # + return - The request metadata. 
    remote isolated function deleteMsgVpnMqttSession(string msgVpnName, string mqttSessionClientId, string mqttSessionVirtualRouter, map<string|string[]> headers = {}) returns SempMetaOnlyResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/mqttSessions/${getEncodedUri(mqttSessionClientId)},${getEncodedUri(mqttSessionVirtualRouter)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Update an MQTT Session object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + mqttSessionClientId - The Client ID of the MQTT Session, which corresponds to the ClientId provided in the MQTT CONNECT packet.
    # + mqttSessionVirtualRouter - The virtual router of the MQTT Session.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The MQTT Session object's attributes. 
    # + return - The MQTT Session object's attributes after being updated, and the request metadata. 
    remote isolated function updateMsgVpnMqttSession(string msgVpnName, string mqttSessionClientId, string mqttSessionVirtualRouter, MsgVpnMqttSession payload, map<string|string[]> headers = {}, *UpdateMsgVpnMqttSessionQueries queries) returns MsgVpnMqttSessionResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/mqttSessions/${getEncodedUri(mqttSessionClientId)},${getEncodedUri(mqttSessionVirtualRouter)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Get a list of Subscription objects.
    #
    # + msgVpnName - The name of the Message VPN.
    # + mqttSessionClientId - The Client ID of the MQTT Session, which corresponds to the ClientId provided in the MQTT CONNECT packet.
    # + mqttSessionVirtualRouter - The virtual router of the MQTT Session.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The list of Subscription objects' attributes, and the request metadata. 
    remote isolated function getMsgVpnMqttSessionSubscriptions(string msgVpnName, string mqttSessionClientId, string mqttSessionVirtualRouter, map<string|string[]> headers = {}, *GetMsgVpnMqttSessionSubscriptionsQueries queries) returns MsgVpnMqttSessionSubscriptionsResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/mqttSessions/${getEncodedUri(mqttSessionClientId)},${getEncodedUri(mqttSessionVirtualRouter)}/subscriptions`;
        map<Encoding> queryParamEncoding = {"where": {style: FORM, explode: false}, "select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create a Subscription object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + mqttSessionClientId - The Client ID of the MQTT Session, which corresponds to the ClientId provided in the MQTT CONNECT packet.
    # + mqttSessionVirtualRouter - The virtual router of the MQTT Session.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Subscription object's attributes. 
    # + return - The Subscription object's attributes after being created, and the request metadata. 
    remote isolated function createMsgVpnMqttSessionSubscription(string msgVpnName, string mqttSessionClientId, string mqttSessionVirtualRouter, MsgVpnMqttSessionSubscription payload, map<string|string[]> headers = {}, *CreateMsgVpnMqttSessionSubscriptionQueries queries) returns MsgVpnMqttSessionSubscriptionResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/mqttSessions/${getEncodedUri(mqttSessionClientId)},${getEncodedUri(mqttSessionVirtualRouter)}/subscriptions`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get a Subscription object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + mqttSessionClientId - The Client ID of the MQTT Session, which corresponds to the ClientId provided in the MQTT CONNECT packet.
    # + mqttSessionVirtualRouter - The virtual router of the MQTT Session.
    # + subscriptionTopic - The MQTT subscription topic.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The Subscription object's attributes, and the request metadata. 
    remote isolated function getMsgVpnMqttSessionSubscription(string msgVpnName, string mqttSessionClientId, string mqttSessionVirtualRouter, string subscriptionTopic, map<string|string[]> headers = {}, *GetMsgVpnMqttSessionSubscriptionQueries queries) returns MsgVpnMqttSessionSubscriptionResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/mqttSessions/${getEncodedUri(mqttSessionClientId)},${getEncodedUri(mqttSessionVirtualRouter)}/subscriptions/${getEncodedUri(subscriptionTopic)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Replace a Subscription object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + mqttSessionClientId - The Client ID of the MQTT Session, which corresponds to the ClientId provided in the MQTT CONNECT packet.
    # + mqttSessionVirtualRouter - The virtual router of the MQTT Session.
    # + subscriptionTopic - The MQTT subscription topic.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Subscription object's attributes. 
    # + return - The Subscription object's attributes after being replaced, and the request metadata. 
    remote isolated function replaceMsgVpnMqttSessionSubscription(string msgVpnName, string mqttSessionClientId, string mqttSessionVirtualRouter, string subscriptionTopic, MsgVpnMqttSessionSubscription payload, map<string|string[]> headers = {}, *ReplaceMsgVpnMqttSessionSubscriptionQueries queries) returns MsgVpnMqttSessionSubscriptionResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/mqttSessions/${getEncodedUri(mqttSessionClientId)},${getEncodedUri(mqttSessionVirtualRouter)}/subscriptions/${getEncodedUri(subscriptionTopic)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Delete a Subscription object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + mqttSessionClientId - The Client ID of the MQTT Session, which corresponds to the ClientId provided in the MQTT CONNECT packet.
    # + mqttSessionVirtualRouter - The virtual router of the MQTT Session.
    # + subscriptionTopic - The MQTT subscription topic.
    # + headers - Headers to be sent with the request 
    # + return - The request metadata. 
    remote isolated function deleteMsgVpnMqttSessionSubscription(string msgVpnName, string mqttSessionClientId, string mqttSessionVirtualRouter, string subscriptionTopic, map<string|string[]> headers = {}) returns SempMetaOnlyResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/mqttSessions/${getEncodedUri(mqttSessionClientId)},${getEncodedUri(mqttSessionVirtualRouter)}/subscriptions/${getEncodedUri(subscriptionTopic)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Update a Subscription object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + mqttSessionClientId - The Client ID of the MQTT Session, which corresponds to the ClientId provided in the MQTT CONNECT packet.
    # + mqttSessionVirtualRouter - The virtual router of the MQTT Session.
    # + subscriptionTopic - The MQTT subscription topic.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Subscription object's attributes. 
    # + return - The Subscription object's attributes after being updated, and the request metadata. 
    remote isolated function updateMsgVpnMqttSessionSubscription(string msgVpnName, string mqttSessionClientId, string mqttSessionVirtualRouter, string subscriptionTopic, MsgVpnMqttSessionSubscription payload, map<string|string[]> headers = {}, *UpdateMsgVpnMqttSessionSubscriptionQueries queries) returns MsgVpnMqttSessionSubscriptionResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/mqttSessions/${getEncodedUri(mqttSessionClientId)},${getEncodedUri(mqttSessionVirtualRouter)}/subscriptions/${getEncodedUri(subscriptionTopic)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Get a list of Proxy objects.
    #
    # + msgVpnName - The name of the Message VPN.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The list of Proxy objects' attributes, and the request metadata. 
    remote isolated function getMsgVpnProxies(string msgVpnName, map<string|string[]> headers = {}, *GetMsgVpnProxiesQueries queries) returns MsgVpnProxiesResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/proxies`;
        map<Encoding> queryParamEncoding = {"where": {style: FORM, explode: false}, "select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create a Proxy object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Proxy object's attributes. 
    # + return - The Proxy object's attributes after being created, and the request metadata. 
    remote isolated function createMsgVpnProxy(string msgVpnName, MsgVpnProxy payload, map<string|string[]> headers = {}, *CreateMsgVpnProxyQueries queries) returns MsgVpnProxyResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/proxies`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get a Proxy object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + proxyName - The name of the proxy.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The Proxy object's attributes, and the request metadata. 
    remote isolated function getMsgVpnProxy(string msgVpnName, string proxyName, map<string|string[]> headers = {}, *GetMsgVpnProxyQueries queries) returns MsgVpnProxyResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/proxies/${getEncodedUri(proxyName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Replace a Proxy object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + proxyName - The name of the proxy.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Proxy object's attributes. 
    # + return - The Proxy object's attributes after being replaced, and the request metadata. 
    remote isolated function replaceMsgVpnProxy(string msgVpnName, string proxyName, MsgVpnProxy payload, map<string|string[]> headers = {}, *ReplaceMsgVpnProxyQueries queries) returns MsgVpnProxyResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/proxies/${getEncodedUri(proxyName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Delete a Proxy object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + proxyName - The name of the proxy.
    # + headers - Headers to be sent with the request 
    # + return - The request metadata. 
    remote isolated function deleteMsgVpnProxy(string msgVpnName, string proxyName, map<string|string[]> headers = {}) returns SempMetaOnlyResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/proxies/${getEncodedUri(proxyName)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Update a Proxy object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + proxyName - The name of the proxy.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Proxy object's attributes. 
    # + return - The Proxy object's attributes after being updated, and the request metadata. 
    remote isolated function updateMsgVpnProxy(string msgVpnName, string proxyName, MsgVpnProxy payload, map<string|string[]> headers = {}, *UpdateMsgVpnProxyQueries queries) returns MsgVpnProxyResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/proxies/${getEncodedUri(proxyName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Get a list of Queue Template objects.
    #
    # + msgVpnName - The name of the Message VPN.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The list of Queue Template objects' attributes, and the request metadata. 
    remote isolated function getMsgVpnQueueTemplates(string msgVpnName, map<string|string[]> headers = {}, *GetMsgVpnQueueTemplatesQueries queries) returns MsgVpnQueueTemplatesResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/queueTemplates`;
        map<Encoding> queryParamEncoding = {"where": {style: FORM, explode: false}, "select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create a Queue Template object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Queue Template object's attributes. 
    # + return - The Queue Template object's attributes after being created, and the request metadata. 
    remote isolated function createMsgVpnQueueTemplate(string msgVpnName, MsgVpnQueueTemplate payload, map<string|string[]> headers = {}, *CreateMsgVpnQueueTemplateQueries queries) returns MsgVpnQueueTemplateResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/queueTemplates`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get a Queue Template object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + queueTemplateName - The name of the Queue Template.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The Queue Template object's attributes, and the request metadata. 
    remote isolated function getMsgVpnQueueTemplate(string msgVpnName, string queueTemplateName, map<string|string[]> headers = {}, *GetMsgVpnQueueTemplateQueries queries) returns MsgVpnQueueTemplateResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/queueTemplates/${getEncodedUri(queueTemplateName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Replace a Queue Template object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + queueTemplateName - The name of the Queue Template.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Queue Template object's attributes. 
    # + return - The Queue Template object's attributes after being replaced, and the request metadata. 
    remote isolated function replaceMsgVpnQueueTemplate(string msgVpnName, string queueTemplateName, MsgVpnQueueTemplate payload, map<string|string[]> headers = {}, *ReplaceMsgVpnQueueTemplateQueries queries) returns MsgVpnQueueTemplateResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/queueTemplates/${getEncodedUri(queueTemplateName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Delete a Queue Template object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + queueTemplateName - The name of the Queue Template.
    # + headers - Headers to be sent with the request 
    # + return - The request metadata. 
    remote isolated function deleteMsgVpnQueueTemplate(string msgVpnName, string queueTemplateName, map<string|string[]> headers = {}) returns SempMetaOnlyResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/queueTemplates/${getEncodedUri(queueTemplateName)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Update a Queue Template object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + queueTemplateName - The name of the Queue Template.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Queue Template object's attributes. 
    # + return - The Queue Template object's attributes after being updated, and the request metadata. 
    remote isolated function updateMsgVpnQueueTemplate(string msgVpnName, string queueTemplateName, MsgVpnQueueTemplate payload, map<string|string[]> headers = {}, *UpdateMsgVpnQueueTemplateQueries queries) returns MsgVpnQueueTemplateResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/queueTemplates/${getEncodedUri(queueTemplateName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Get a list of Queue objects.
    #
    # + msgVpnName - The name of the Message VPN.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The list of Queue objects' attributes, and the request metadata. 
    remote isolated function getMsgVpnQueues(string msgVpnName, map<string|string[]> headers = {}, *GetMsgVpnQueuesQueries queries) returns MsgVpnQueuesResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/queues`;
        map<Encoding> queryParamEncoding = {"where": {style: FORM, explode: false}, "select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create a Queue object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Queue object's attributes. 
    # + return - The Queue object's attributes after being created, and the request metadata. 
    remote isolated function createMsgVpnQueue(string msgVpnName, MsgVpnQueue payload, map<string|string[]> headers = {}, *CreateMsgVpnQueueQueries queries) returns MsgVpnQueueResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/queues`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get a Queue object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + queueName - The name of the Queue.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The Queue object's attributes, and the request metadata. 
    remote isolated function getMsgVpnQueue(string msgVpnName, string queueName, map<string|string[]> headers = {}, *GetMsgVpnQueueQueries queries) returns MsgVpnQueueResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/queues/${getEncodedUri(queueName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Replace a Queue object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + queueName - The name of the Queue.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Queue object's attributes. 
    # + return - The Queue object's attributes after being replaced, and the request metadata. 
    remote isolated function replaceMsgVpnQueue(string msgVpnName, string queueName, MsgVpnQueue payload, map<string|string[]> headers = {}, *ReplaceMsgVpnQueueQueries queries) returns MsgVpnQueueResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/queues/${getEncodedUri(queueName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Delete a Queue object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + queueName - The name of the Queue.
    # + headers - Headers to be sent with the request 
    # + return - The request metadata. 
    remote isolated function deleteMsgVpnQueue(string msgVpnName, string queueName, map<string|string[]> headers = {}) returns SempMetaOnlyResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/queues/${getEncodedUri(queueName)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Update a Queue object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + queueName - The name of the Queue.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Queue object's attributes. 
    # + return - The Queue object's attributes after being updated, and the request metadata. 
    remote isolated function updateMsgVpnQueue(string msgVpnName, string queueName, MsgVpnQueue payload, map<string|string[]> headers = {}, *UpdateMsgVpnQueueQueries queries) returns MsgVpnQueueResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/queues/${getEncodedUri(queueName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Get a list of Queue Subscription objects.
    #
    # + msgVpnName - The name of the Message VPN.
    # + queueName - The name of the Queue.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The list of Queue Subscription objects' attributes, and the request metadata. 
    remote isolated function getMsgVpnQueueSubscriptions(string msgVpnName, string queueName, map<string|string[]> headers = {}, *GetMsgVpnQueueSubscriptionsQueries queries) returns MsgVpnQueueSubscriptionsResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/queues/${getEncodedUri(queueName)}/subscriptions`;
        map<Encoding> queryParamEncoding = {"where": {style: FORM, explode: false}, "select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create a Queue Subscription object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + queueName - The name of the Queue.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Queue Subscription object's attributes. 
    # + return - The Queue Subscription object's attributes after being created, and the request metadata. 
    remote isolated function createMsgVpnQueueSubscription(string msgVpnName, string queueName, MsgVpnQueueSubscription payload, map<string|string[]> headers = {}, *CreateMsgVpnQueueSubscriptionQueries queries) returns MsgVpnQueueSubscriptionResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/queues/${getEncodedUri(queueName)}/subscriptions`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get a Queue Subscription object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + queueName - The name of the Queue.
    # + subscriptionTopic - The topic of the Subscription.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The Queue Subscription object's attributes, and the request metadata. 
    remote isolated function getMsgVpnQueueSubscription(string msgVpnName, string queueName, string subscriptionTopic, map<string|string[]> headers = {}, *GetMsgVpnQueueSubscriptionQueries queries) returns MsgVpnQueueSubscriptionResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/queues/${getEncodedUri(queueName)}/subscriptions/${getEncodedUri(subscriptionTopic)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete a Queue Subscription object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + queueName - The name of the Queue.
    # + subscriptionTopic - The topic of the Subscription.
    # + headers - Headers to be sent with the request 
    # + return - The request metadata. 
    remote isolated function deleteMsgVpnQueueSubscription(string msgVpnName, string queueName, string subscriptionTopic, map<string|string[]> headers = {}) returns SempMetaOnlyResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/queues/${getEncodedUri(queueName)}/subscriptions/${getEncodedUri(subscriptionTopic)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Get a list of Replay Log objects.
    #
    # + msgVpnName - The name of the Message VPN.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The list of Replay Log objects' attributes, and the request metadata. 
    remote isolated function getMsgVpnReplayLogs(string msgVpnName, map<string|string[]> headers = {}, *GetMsgVpnReplayLogsQueries queries) returns MsgVpnReplayLogsResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/replayLogs`;
        map<Encoding> queryParamEncoding = {"where": {style: FORM, explode: false}, "select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create a Replay Log object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Replay Log object's attributes. 
    # + return - The Replay Log object's attributes after being created, and the request metadata. 
    remote isolated function createMsgVpnReplayLog(string msgVpnName, MsgVpnReplayLog payload, map<string|string[]> headers = {}, *CreateMsgVpnReplayLogQueries queries) returns MsgVpnReplayLogResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/replayLogs`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get a Replay Log object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + replayLogName - The name of the Replay Log.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The Replay Log object's attributes, and the request metadata. 
    remote isolated function getMsgVpnReplayLog(string msgVpnName, string replayLogName, map<string|string[]> headers = {}, *GetMsgVpnReplayLogQueries queries) returns MsgVpnReplayLogResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/replayLogs/${getEncodedUri(replayLogName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Replace a Replay Log object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + replayLogName - The name of the Replay Log.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Replay Log object's attributes. 
    # + return - The Replay Log object's attributes after being replaced, and the request metadata. 
    remote isolated function replaceMsgVpnReplayLog(string msgVpnName, string replayLogName, MsgVpnReplayLog payload, map<string|string[]> headers = {}, *ReplaceMsgVpnReplayLogQueries queries) returns MsgVpnReplayLogResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/replayLogs/${getEncodedUri(replayLogName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Delete a Replay Log object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + replayLogName - The name of the Replay Log.
    # + headers - Headers to be sent with the request 
    # + return - The request metadata. 
    remote isolated function deleteMsgVpnReplayLog(string msgVpnName, string replayLogName, map<string|string[]> headers = {}) returns SempMetaOnlyResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/replayLogs/${getEncodedUri(replayLogName)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Update a Replay Log object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + replayLogName - The name of the Replay Log.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Replay Log object's attributes. 
    # + return - The Replay Log object's attributes after being updated, and the request metadata. 
    remote isolated function updateMsgVpnReplayLog(string msgVpnName, string replayLogName, MsgVpnReplayLog payload, map<string|string[]> headers = {}, *UpdateMsgVpnReplayLogQueries queries) returns MsgVpnReplayLogResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/replayLogs/${getEncodedUri(replayLogName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Get a list of Topic Filter Subscription objects.
    #
    # + msgVpnName - The name of the Message VPN.
    # + replayLogName - The name of the Replay Log.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The list of Topic Filter Subscription objects' attributes, and the request metadata. 
    remote isolated function getMsgVpnReplayLogTopicFilterSubscriptions(string msgVpnName, string replayLogName, map<string|string[]> headers = {}, *GetMsgVpnReplayLogTopicFilterSubscriptionsQueries queries) returns MsgVpnReplayLogTopicFilterSubscriptionsResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/replayLogs/${getEncodedUri(replayLogName)}/topicFilterSubscriptions`;
        map<Encoding> queryParamEncoding = {"where": {style: FORM, explode: false}, "select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create a Topic Filter Subscription object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + replayLogName - The name of the Replay Log.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Topic Filter Subscription object's attributes. 
    # + return - The Topic Filter Subscription object's attributes after being created, and the request metadata. 
    remote isolated function createMsgVpnReplayLogTopicFilterSubscription(string msgVpnName, string replayLogName, MsgVpnReplayLogTopicFilterSubscription payload, map<string|string[]> headers = {}, *CreateMsgVpnReplayLogTopicFilterSubscriptionQueries queries) returns MsgVpnReplayLogTopicFilterSubscriptionResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/replayLogs/${getEncodedUri(replayLogName)}/topicFilterSubscriptions`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get a Topic Filter Subscription object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + replayLogName - The name of the Replay Log.
    # + topicFilterSubscription - The topic of the Subscription.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The Topic Filter Subscription object's attributes, and the request metadata. 
    remote isolated function getMsgVpnReplayLogTopicFilterSubscription(string msgVpnName, string replayLogName, string topicFilterSubscription, map<string|string[]> headers = {}, *GetMsgVpnReplayLogTopicFilterSubscriptionQueries queries) returns MsgVpnReplayLogTopicFilterSubscriptionResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/replayLogs/${getEncodedUri(replayLogName)}/topicFilterSubscriptions/${getEncodedUri(topicFilterSubscription)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete a Topic Filter Subscription object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + replayLogName - The name of the Replay Log.
    # + topicFilterSubscription - The topic of the Subscription.
    # + headers - Headers to be sent with the request 
    # + return - The request metadata. 
    remote isolated function deleteMsgVpnReplayLogTopicFilterSubscription(string msgVpnName, string replayLogName, string topicFilterSubscription, map<string|string[]> headers = {}) returns SempMetaOnlyResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/replayLogs/${getEncodedUri(replayLogName)}/topicFilterSubscriptions/${getEncodedUri(topicFilterSubscription)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Get a list of Replicated Topic objects.
    #
    # + msgVpnName - The name of the Message VPN.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The list of Replicated Topic objects' attributes, and the request metadata. 
    remote isolated function getMsgVpnReplicatedTopics(string msgVpnName, map<string|string[]> headers = {}, *GetMsgVpnReplicatedTopicsQueries queries) returns MsgVpnReplicatedTopicsResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/replicatedTopics`;
        map<Encoding> queryParamEncoding = {"where": {style: FORM, explode: false}, "select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create a Replicated Topic object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Replicated Topic object's attributes. 
    # + return - The Replicated Topic object's attributes after being created, and the request metadata. 
    remote isolated function createMsgVpnReplicatedTopic(string msgVpnName, MsgVpnReplicatedTopic payload, map<string|string[]> headers = {}, *CreateMsgVpnReplicatedTopicQueries queries) returns MsgVpnReplicatedTopicResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/replicatedTopics`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get a Replicated Topic object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + replicatedTopic - The topic for applying replication. Published messages matching this topic will be replicated to the standby site.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The Replicated Topic object's attributes, and the request metadata. 
    remote isolated function getMsgVpnReplicatedTopic(string msgVpnName, string replicatedTopic, map<string|string[]> headers = {}, *GetMsgVpnReplicatedTopicQueries queries) returns MsgVpnReplicatedTopicResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/replicatedTopics/${getEncodedUri(replicatedTopic)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Replace a Replicated Topic object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + replicatedTopic - The topic for applying replication. Published messages matching this topic will be replicated to the standby site.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Replicated Topic object's attributes. 
    # + return - The Replicated Topic object's attributes after being replaced, and the request metadata. 
    remote isolated function replaceMsgVpnReplicatedTopic(string msgVpnName, string replicatedTopic, MsgVpnReplicatedTopic payload, map<string|string[]> headers = {}, *ReplaceMsgVpnReplicatedTopicQueries queries) returns MsgVpnReplicatedTopicResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/replicatedTopics/${getEncodedUri(replicatedTopic)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Delete a Replicated Topic object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + replicatedTopic - The topic for applying replication. Published messages matching this topic will be replicated to the standby site.
    # + headers - Headers to be sent with the request 
    # + return - The request metadata. 
    remote isolated function deleteMsgVpnReplicatedTopic(string msgVpnName, string replicatedTopic, map<string|string[]> headers = {}) returns SempMetaOnlyResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/replicatedTopics/${getEncodedUri(replicatedTopic)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Update a Replicated Topic object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + replicatedTopic - The topic for applying replication. Published messages matching this topic will be replicated to the standby site.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Replicated Topic object's attributes. 
    # + return - The Replicated Topic object's attributes after being updated, and the request metadata. 
    remote isolated function updateMsgVpnReplicatedTopic(string msgVpnName, string replicatedTopic, MsgVpnReplicatedTopic payload, map<string|string[]> headers = {}, *UpdateMsgVpnReplicatedTopicQueries queries) returns MsgVpnReplicatedTopicResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/replicatedTopics/${getEncodedUri(replicatedTopic)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Get a list of REST Delivery Point objects.
    #
    # + msgVpnName - The name of the Message VPN.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The list of REST Delivery Point objects' attributes, and the request metadata. 
    remote isolated function getMsgVpnRestDeliveryPoints(string msgVpnName, map<string|string[]> headers = {}, *GetMsgVpnRestDeliveryPointsQueries queries) returns MsgVpnRestDeliveryPointsResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/restDeliveryPoints`;
        map<Encoding> queryParamEncoding = {"where": {style: FORM, explode: false}, "select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create a REST Delivery Point object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The REST Delivery Point object's attributes. 
    # + return - The REST Delivery Point object's attributes after being created, and the request metadata. 
    remote isolated function createMsgVpnRestDeliveryPoint(string msgVpnName, MsgVpnRestDeliveryPoint payload, map<string|string[]> headers = {}, *CreateMsgVpnRestDeliveryPointQueries queries) returns MsgVpnRestDeliveryPointResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/restDeliveryPoints`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get a REST Delivery Point object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + restDeliveryPointName - The name of the REST Delivery Point.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The REST Delivery Point object's attributes, and the request metadata. 
    remote isolated function getMsgVpnRestDeliveryPoint(string msgVpnName, string restDeliveryPointName, map<string|string[]> headers = {}, *GetMsgVpnRestDeliveryPointQueries queries) returns MsgVpnRestDeliveryPointResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/restDeliveryPoints/${getEncodedUri(restDeliveryPointName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Replace a REST Delivery Point object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + restDeliveryPointName - The name of the REST Delivery Point.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The REST Delivery Point object's attributes. 
    # + return - The REST Delivery Point object's attributes after being replaced, and the request metadata. 
    remote isolated function replaceMsgVpnRestDeliveryPoint(string msgVpnName, string restDeliveryPointName, MsgVpnRestDeliveryPoint payload, map<string|string[]> headers = {}, *ReplaceMsgVpnRestDeliveryPointQueries queries) returns MsgVpnRestDeliveryPointResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/restDeliveryPoints/${getEncodedUri(restDeliveryPointName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Delete a REST Delivery Point object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + restDeliveryPointName - The name of the REST Delivery Point.
    # + headers - Headers to be sent with the request 
    # + return - The request metadata. 
    remote isolated function deleteMsgVpnRestDeliveryPoint(string msgVpnName, string restDeliveryPointName, map<string|string[]> headers = {}) returns SempMetaOnlyResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/restDeliveryPoints/${getEncodedUri(restDeliveryPointName)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Update a REST Delivery Point object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + restDeliveryPointName - The name of the REST Delivery Point.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The REST Delivery Point object's attributes. 
    # + return - The REST Delivery Point object's attributes after being updated, and the request metadata. 
    remote isolated function updateMsgVpnRestDeliveryPoint(string msgVpnName, string restDeliveryPointName, MsgVpnRestDeliveryPoint payload, map<string|string[]> headers = {}, *UpdateMsgVpnRestDeliveryPointQueries queries) returns MsgVpnRestDeliveryPointResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/restDeliveryPoints/${getEncodedUri(restDeliveryPointName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Get a list of Queue Binding objects.
    #
    # + msgVpnName - The name of the Message VPN.
    # + restDeliveryPointName - The name of the REST Delivery Point.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The list of Queue Binding objects' attributes, and the request metadata. 
    remote isolated function getMsgVpnRestDeliveryPointQueueBindings(string msgVpnName, string restDeliveryPointName, map<string|string[]> headers = {}, *GetMsgVpnRestDeliveryPointQueueBindingsQueries queries) returns MsgVpnRestDeliveryPointQueueBindingsResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/restDeliveryPoints/${getEncodedUri(restDeliveryPointName)}/queueBindings`;
        map<Encoding> queryParamEncoding = {"where": {style: FORM, explode: false}, "select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create a Queue Binding object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + restDeliveryPointName - The name of the REST Delivery Point.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Queue Binding object's attributes. 
    # + return - The Queue Binding object's attributes after being created, and the request metadata. 
    remote isolated function createMsgVpnRestDeliveryPointQueueBinding(string msgVpnName, string restDeliveryPointName, MsgVpnRestDeliveryPointQueueBinding payload, map<string|string[]> headers = {}, *CreateMsgVpnRestDeliveryPointQueueBindingQueries queries) returns MsgVpnRestDeliveryPointQueueBindingResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/restDeliveryPoints/${getEncodedUri(restDeliveryPointName)}/queueBindings`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get a Queue Binding object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + restDeliveryPointName - The name of the REST Delivery Point.
    # + queueBindingName - The name of a queue in the Message VPN.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The Queue Binding object's attributes, and the request metadata. 
    remote isolated function getMsgVpnRestDeliveryPointQueueBinding(string msgVpnName, string restDeliveryPointName, string queueBindingName, map<string|string[]> headers = {}, *GetMsgVpnRestDeliveryPointQueueBindingQueries queries) returns MsgVpnRestDeliveryPointQueueBindingResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/restDeliveryPoints/${getEncodedUri(restDeliveryPointName)}/queueBindings/${getEncodedUri(queueBindingName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Replace a Queue Binding object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + restDeliveryPointName - The name of the REST Delivery Point.
    # + queueBindingName - The name of a queue in the Message VPN.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Queue Binding object's attributes. 
    # + return - The Queue Binding object's attributes after being replaced, and the request metadata. 
    remote isolated function replaceMsgVpnRestDeliveryPointQueueBinding(string msgVpnName, string restDeliveryPointName, string queueBindingName, MsgVpnRestDeliveryPointQueueBinding payload, map<string|string[]> headers = {}, *ReplaceMsgVpnRestDeliveryPointQueueBindingQueries queries) returns MsgVpnRestDeliveryPointQueueBindingResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/restDeliveryPoints/${getEncodedUri(restDeliveryPointName)}/queueBindings/${getEncodedUri(queueBindingName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Delete a Queue Binding object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + restDeliveryPointName - The name of the REST Delivery Point.
    # + queueBindingName - The name of a queue in the Message VPN.
    # + headers - Headers to be sent with the request 
    # + return - The request metadata. 
    remote isolated function deleteMsgVpnRestDeliveryPointQueueBinding(string msgVpnName, string restDeliveryPointName, string queueBindingName, map<string|string[]> headers = {}) returns SempMetaOnlyResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/restDeliveryPoints/${getEncodedUri(restDeliveryPointName)}/queueBindings/${getEncodedUri(queueBindingName)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Update a Queue Binding object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + restDeliveryPointName - The name of the REST Delivery Point.
    # + queueBindingName - The name of a queue in the Message VPN.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Queue Binding object's attributes. 
    # + return - The Queue Binding object's attributes after being updated, and the request metadata. 
    remote isolated function updateMsgVpnRestDeliveryPointQueueBinding(string msgVpnName, string restDeliveryPointName, string queueBindingName, MsgVpnRestDeliveryPointQueueBinding payload, map<string|string[]> headers = {}, *UpdateMsgVpnRestDeliveryPointQueueBindingQueries queries) returns MsgVpnRestDeliveryPointQueueBindingResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/restDeliveryPoints/${getEncodedUri(restDeliveryPointName)}/queueBindings/${getEncodedUri(queueBindingName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Get a list of Protected Request Header objects.
    #
    # + msgVpnName - The name of the Message VPN.
    # + restDeliveryPointName - The name of the REST Delivery Point.
    # + queueBindingName - The name of a queue in the Message VPN.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The list of Protected Request Header objects' attributes, and the request metadata. 
    remote isolated function getMsgVpnRestDeliveryPointQueueBindingProtectedRequestHeaders(string msgVpnName, string restDeliveryPointName, string queueBindingName, map<string|string[]> headers = {}, *GetMsgVpnRestDeliveryPointQueueBindingProtectedRequestHeadersQueries queries) returns MsgVpnRestDeliveryPointQueueBindingProtectedRequestHeadersResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/restDeliveryPoints/${getEncodedUri(restDeliveryPointName)}/queueBindings/${getEncodedUri(queueBindingName)}/protectedRequestHeaders`;
        map<Encoding> queryParamEncoding = {"where": {style: FORM, explode: false}, "select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create a Protected Request Header object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + restDeliveryPointName - The name of the REST Delivery Point.
    # + queueBindingName - The name of a queue in the Message VPN.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Protected Request Header object's attributes. 
    # + return - The Protected Request Header object's attributes after being created, and the request metadata. 
    remote isolated function createMsgVpnRestDeliveryPointQueueBindingProtectedRequestHeader(string msgVpnName, string restDeliveryPointName, string queueBindingName, MsgVpnRestDeliveryPointQueueBindingProtectedRequestHeader payload, map<string|string[]> headers = {}, *CreateMsgVpnRestDeliveryPointQueueBindingProtectedRequestHeaderQueries queries) returns MsgVpnRestDeliveryPointQueueBindingProtectedRequestHeaderResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/restDeliveryPoints/${getEncodedUri(restDeliveryPointName)}/queueBindings/${getEncodedUri(queueBindingName)}/protectedRequestHeaders`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get a Protected Request Header object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + restDeliveryPointName - The name of the REST Delivery Point.
    # + queueBindingName - The name of a queue in the Message VPN.
    # + headerName - The name of the protected HTTP request header.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The Protected Request Header object's attributes, and the request metadata. 
    remote isolated function getMsgVpnRestDeliveryPointQueueBindingProtectedRequestHeader(string msgVpnName, string restDeliveryPointName, string queueBindingName, string headerName, map<string|string[]> headers = {}, *GetMsgVpnRestDeliveryPointQueueBindingProtectedRequestHeaderQueries queries) returns MsgVpnRestDeliveryPointQueueBindingProtectedRequestHeaderResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/restDeliveryPoints/${getEncodedUri(restDeliveryPointName)}/queueBindings/${getEncodedUri(queueBindingName)}/protectedRequestHeaders/${getEncodedUri(headerName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Replace a Protected Request Header object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + restDeliveryPointName - The name of the REST Delivery Point.
    # + queueBindingName - The name of a queue in the Message VPN.
    # + headerName - The name of the protected HTTP request header.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Protected Request Header object's attributes. 
    # + return - The Protected Request Header object's attributes after being replaced, and the request metadata. 
    remote isolated function replaceMsgVpnRestDeliveryPointQueueBindingProtectedRequestHeader(string msgVpnName, string restDeliveryPointName, string queueBindingName, string headerName, MsgVpnRestDeliveryPointQueueBindingProtectedRequestHeader payload, map<string|string[]> headers = {}, *ReplaceMsgVpnRestDeliveryPointQueueBindingProtectedRequestHeaderQueries queries) returns MsgVpnRestDeliveryPointQueueBindingProtectedRequestHeaderResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/restDeliveryPoints/${getEncodedUri(restDeliveryPointName)}/queueBindings/${getEncodedUri(queueBindingName)}/protectedRequestHeaders/${getEncodedUri(headerName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Delete a Protected Request Header object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + restDeliveryPointName - The name of the REST Delivery Point.
    # + queueBindingName - The name of a queue in the Message VPN.
    # + headerName - The name of the protected HTTP request header.
    # + headers - Headers to be sent with the request 
    # + return - The request metadata. 
    remote isolated function deleteMsgVpnRestDeliveryPointQueueBindingProtectedRequestHeader(string msgVpnName, string restDeliveryPointName, string queueBindingName, string headerName, map<string|string[]> headers = {}) returns SempMetaOnlyResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/restDeliveryPoints/${getEncodedUri(restDeliveryPointName)}/queueBindings/${getEncodedUri(queueBindingName)}/protectedRequestHeaders/${getEncodedUri(headerName)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Update a Protected Request Header object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + restDeliveryPointName - The name of the REST Delivery Point.
    # + queueBindingName - The name of a queue in the Message VPN.
    # + headerName - The name of the protected HTTP request header.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Protected Request Header object's attributes. 
    # + return - The Protected Request Header object's attributes after being updated, and the request metadata. 
    remote isolated function updateMsgVpnRestDeliveryPointQueueBindingProtectedRequestHeader(string msgVpnName, string restDeliveryPointName, string queueBindingName, string headerName, MsgVpnRestDeliveryPointQueueBindingProtectedRequestHeader payload, map<string|string[]> headers = {}, *UpdateMsgVpnRestDeliveryPointQueueBindingProtectedRequestHeaderQueries queries) returns MsgVpnRestDeliveryPointQueueBindingProtectedRequestHeaderResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/restDeliveryPoints/${getEncodedUri(restDeliveryPointName)}/queueBindings/${getEncodedUri(queueBindingName)}/protectedRequestHeaders/${getEncodedUri(headerName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Get a list of Request Header objects.
    #
    # + msgVpnName - The name of the Message VPN.
    # + restDeliveryPointName - The name of the REST Delivery Point.
    # + queueBindingName - The name of a queue in the Message VPN.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The list of Request Header objects' attributes, and the request metadata. 
    remote isolated function getMsgVpnRestDeliveryPointQueueBindingRequestHeaders(string msgVpnName, string restDeliveryPointName, string queueBindingName, map<string|string[]> headers = {}, *GetMsgVpnRestDeliveryPointQueueBindingRequestHeadersQueries queries) returns MsgVpnRestDeliveryPointQueueBindingRequestHeadersResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/restDeliveryPoints/${getEncodedUri(restDeliveryPointName)}/queueBindings/${getEncodedUri(queueBindingName)}/requestHeaders`;
        map<Encoding> queryParamEncoding = {"where": {style: FORM, explode: false}, "select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create a Request Header object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + restDeliveryPointName - The name of the REST Delivery Point.
    # + queueBindingName - The name of a queue in the Message VPN.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Request Header object's attributes. 
    # + return - The Request Header object's attributes after being created, and the request metadata. 
    remote isolated function createMsgVpnRestDeliveryPointQueueBindingRequestHeader(string msgVpnName, string restDeliveryPointName, string queueBindingName, MsgVpnRestDeliveryPointQueueBindingRequestHeader payload, map<string|string[]> headers = {}, *CreateMsgVpnRestDeliveryPointQueueBindingRequestHeaderQueries queries) returns MsgVpnRestDeliveryPointQueueBindingRequestHeaderResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/restDeliveryPoints/${getEncodedUri(restDeliveryPointName)}/queueBindings/${getEncodedUri(queueBindingName)}/requestHeaders`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get a Request Header object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + restDeliveryPointName - The name of the REST Delivery Point.
    # + queueBindingName - The name of a queue in the Message VPN.
    # + headerName - The name of the HTTP request header.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The Request Header object's attributes, and the request metadata. 
    remote isolated function getMsgVpnRestDeliveryPointQueueBindingRequestHeader(string msgVpnName, string restDeliveryPointName, string queueBindingName, string headerName, map<string|string[]> headers = {}, *GetMsgVpnRestDeliveryPointQueueBindingRequestHeaderQueries queries) returns MsgVpnRestDeliveryPointQueueBindingRequestHeaderResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/restDeliveryPoints/${getEncodedUri(restDeliveryPointName)}/queueBindings/${getEncodedUri(queueBindingName)}/requestHeaders/${getEncodedUri(headerName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Replace a Request Header object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + restDeliveryPointName - The name of the REST Delivery Point.
    # + queueBindingName - The name of a queue in the Message VPN.
    # + headerName - The name of the HTTP request header.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Request Header object's attributes. 
    # + return - The Request Header object's attributes after being replaced, and the request metadata. 
    remote isolated function replaceMsgVpnRestDeliveryPointQueueBindingRequestHeader(string msgVpnName, string restDeliveryPointName, string queueBindingName, string headerName, MsgVpnRestDeliveryPointQueueBindingRequestHeader payload, map<string|string[]> headers = {}, *ReplaceMsgVpnRestDeliveryPointQueueBindingRequestHeaderQueries queries) returns MsgVpnRestDeliveryPointQueueBindingRequestHeaderResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/restDeliveryPoints/${getEncodedUri(restDeliveryPointName)}/queueBindings/${getEncodedUri(queueBindingName)}/requestHeaders/${getEncodedUri(headerName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Delete a Request Header object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + restDeliveryPointName - The name of the REST Delivery Point.
    # + queueBindingName - The name of a queue in the Message VPN.
    # + headerName - The name of the HTTP request header.
    # + headers - Headers to be sent with the request 
    # + return - The request metadata. 
    remote isolated function deleteMsgVpnRestDeliveryPointQueueBindingRequestHeader(string msgVpnName, string restDeliveryPointName, string queueBindingName, string headerName, map<string|string[]> headers = {}) returns SempMetaOnlyResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/restDeliveryPoints/${getEncodedUri(restDeliveryPointName)}/queueBindings/${getEncodedUri(queueBindingName)}/requestHeaders/${getEncodedUri(headerName)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Update a Request Header object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + restDeliveryPointName - The name of the REST Delivery Point.
    # + queueBindingName - The name of a queue in the Message VPN.
    # + headerName - The name of the HTTP request header.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Request Header object's attributes. 
    # + return - The Request Header object's attributes after being updated, and the request metadata. 
    remote isolated function updateMsgVpnRestDeliveryPointQueueBindingRequestHeader(string msgVpnName, string restDeliveryPointName, string queueBindingName, string headerName, MsgVpnRestDeliveryPointQueueBindingRequestHeader payload, map<string|string[]> headers = {}, *UpdateMsgVpnRestDeliveryPointQueueBindingRequestHeaderQueries queries) returns MsgVpnRestDeliveryPointQueueBindingRequestHeaderResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/restDeliveryPoints/${getEncodedUri(restDeliveryPointName)}/queueBindings/${getEncodedUri(queueBindingName)}/requestHeaders/${getEncodedUri(headerName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Get a list of REST Consumer objects.
    #
    # + msgVpnName - The name of the Message VPN.
    # + restDeliveryPointName - The name of the REST Delivery Point.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The list of REST Consumer objects' attributes, and the request metadata. 
    remote isolated function getMsgVpnRestDeliveryPointRestConsumers(string msgVpnName, string restDeliveryPointName, map<string|string[]> headers = {}, *GetMsgVpnRestDeliveryPointRestConsumersQueries queries) returns MsgVpnRestDeliveryPointRestConsumersResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/restDeliveryPoints/${getEncodedUri(restDeliveryPointName)}/restConsumers`;
        map<Encoding> queryParamEncoding = {"where": {style: FORM, explode: false}, "select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create a REST Consumer object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + restDeliveryPointName - The name of the REST Delivery Point.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The REST Consumer object's attributes. 
    # + return - The REST Consumer object's attributes after being created, and the request metadata. 
    remote isolated function createMsgVpnRestDeliveryPointRestConsumer(string msgVpnName, string restDeliveryPointName, MsgVpnRestDeliveryPointRestConsumer payload, map<string|string[]> headers = {}, *CreateMsgVpnRestDeliveryPointRestConsumerQueries queries) returns MsgVpnRestDeliveryPointRestConsumerResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/restDeliveryPoints/${getEncodedUri(restDeliveryPointName)}/restConsumers`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get a REST Consumer object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + restDeliveryPointName - The name of the REST Delivery Point.
    # + restConsumerName - The name of the REST Consumer.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The REST Consumer object's attributes, and the request metadata. 
    remote isolated function getMsgVpnRestDeliveryPointRestConsumer(string msgVpnName, string restDeliveryPointName, string restConsumerName, map<string|string[]> headers = {}, *GetMsgVpnRestDeliveryPointRestConsumerQueries queries) returns MsgVpnRestDeliveryPointRestConsumerResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/restDeliveryPoints/${getEncodedUri(restDeliveryPointName)}/restConsumers/${getEncodedUri(restConsumerName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Replace a REST Consumer object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + restDeliveryPointName - The name of the REST Delivery Point.
    # + restConsumerName - The name of the REST Consumer.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The REST Consumer object's attributes. 
    # + return - The REST Consumer object's attributes after being replaced, and the request metadata. 
    remote isolated function replaceMsgVpnRestDeliveryPointRestConsumer(string msgVpnName, string restDeliveryPointName, string restConsumerName, MsgVpnRestDeliveryPointRestConsumer payload, map<string|string[]> headers = {}, *ReplaceMsgVpnRestDeliveryPointRestConsumerQueries queries) returns MsgVpnRestDeliveryPointRestConsumerResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/restDeliveryPoints/${getEncodedUri(restDeliveryPointName)}/restConsumers/${getEncodedUri(restConsumerName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Delete a REST Consumer object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + restDeliveryPointName - The name of the REST Delivery Point.
    # + restConsumerName - The name of the REST Consumer.
    # + headers - Headers to be sent with the request 
    # + return - The request metadata. 
    remote isolated function deleteMsgVpnRestDeliveryPointRestConsumer(string msgVpnName, string restDeliveryPointName, string restConsumerName, map<string|string[]> headers = {}) returns SempMetaOnlyResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/restDeliveryPoints/${getEncodedUri(restDeliveryPointName)}/restConsumers/${getEncodedUri(restConsumerName)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Update a REST Consumer object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + restDeliveryPointName - The name of the REST Delivery Point.
    # + restConsumerName - The name of the REST Consumer.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The REST Consumer object's attributes. 
    # + return - The REST Consumer object's attributes after being updated, and the request metadata. 
    remote isolated function updateMsgVpnRestDeliveryPointRestConsumer(string msgVpnName, string restDeliveryPointName, string restConsumerName, MsgVpnRestDeliveryPointRestConsumer payload, map<string|string[]> headers = {}, *UpdateMsgVpnRestDeliveryPointRestConsumerQueries queries) returns MsgVpnRestDeliveryPointRestConsumerResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/restDeliveryPoints/${getEncodedUri(restDeliveryPointName)}/restConsumers/${getEncodedUri(restConsumerName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Get a list of Claim objects.
    #
    # + msgVpnName - The name of the Message VPN.
    # + restDeliveryPointName - The name of the REST Delivery Point.
    # + restConsumerName - The name of the REST Consumer.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The list of Claim objects' attributes, and the request metadata. 
    remote isolated function getMsgVpnRestDeliveryPointRestConsumerOauthJwtClaims(string msgVpnName, string restDeliveryPointName, string restConsumerName, map<string|string[]> headers = {}, *GetMsgVpnRestDeliveryPointRestConsumerOauthJwtClaimsQueries queries) returns MsgVpnRestDeliveryPointRestConsumerOauthJwtClaimsResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/restDeliveryPoints/${getEncodedUri(restDeliveryPointName)}/restConsumers/${getEncodedUri(restConsumerName)}/oauthJwtClaims`;
        map<Encoding> queryParamEncoding = {"where": {style: FORM, explode: false}, "select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create a Claim object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + restDeliveryPointName - The name of the REST Delivery Point.
    # + restConsumerName - The name of the REST Consumer.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Claim object's attributes. 
    # + return - The Claim object's attributes after being created, and the request metadata. 
    remote isolated function createMsgVpnRestDeliveryPointRestConsumerOauthJwtClaim(string msgVpnName, string restDeliveryPointName, string restConsumerName, MsgVpnRestDeliveryPointRestConsumerOauthJwtClaim payload, map<string|string[]> headers = {}, *CreateMsgVpnRestDeliveryPointRestConsumerOauthJwtClaimQueries queries) returns MsgVpnRestDeliveryPointRestConsumerOauthJwtClaimResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/restDeliveryPoints/${getEncodedUri(restDeliveryPointName)}/restConsumers/${getEncodedUri(restConsumerName)}/oauthJwtClaims`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get a Claim object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + restDeliveryPointName - The name of the REST Delivery Point.
    # + restConsumerName - The name of the REST Consumer.
    # + oauthJwtClaimName - The name of the additional claim. Cannot be "exp", "iat", or "jti".
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The Claim object's attributes, and the request metadata. 
    remote isolated function getMsgVpnRestDeliveryPointRestConsumerOauthJwtClaim(string msgVpnName, string restDeliveryPointName, string restConsumerName, string oauthJwtClaimName, map<string|string[]> headers = {}, *GetMsgVpnRestDeliveryPointRestConsumerOauthJwtClaimQueries queries) returns MsgVpnRestDeliveryPointRestConsumerOauthJwtClaimResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/restDeliveryPoints/${getEncodedUri(restDeliveryPointName)}/restConsumers/${getEncodedUri(restConsumerName)}/oauthJwtClaims/${getEncodedUri(oauthJwtClaimName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete a Claim object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + restDeliveryPointName - The name of the REST Delivery Point.
    # + restConsumerName - The name of the REST Consumer.
    # + oauthJwtClaimName - The name of the additional claim. Cannot be "exp", "iat", or "jti".
    # + headers - Headers to be sent with the request 
    # + return - The request metadata. 
    remote isolated function deleteMsgVpnRestDeliveryPointRestConsumerOauthJwtClaim(string msgVpnName, string restDeliveryPointName, string restConsumerName, string oauthJwtClaimName, map<string|string[]> headers = {}) returns SempMetaOnlyResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/restDeliveryPoints/${getEncodedUri(restDeliveryPointName)}/restConsumers/${getEncodedUri(restConsumerName)}/oauthJwtClaims/${getEncodedUri(oauthJwtClaimName)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Get a list of Trusted Common Name objects.
    #
    # + msgVpnName - The name of the Message VPN.
    # + restDeliveryPointName - The name of the REST Delivery Point.
    # + restConsumerName - The name of the REST Consumer.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The list of Trusted Common Name objects' attributes, and the request metadata. 
    #
    # # Deprecated
    @deprecated
    remote isolated function getMsgVpnRestDeliveryPointRestConsumerTlsTrustedCommonNames(string msgVpnName, string restDeliveryPointName, string restConsumerName, map<string|string[]> headers = {}, *GetMsgVpnRestDeliveryPointRestConsumerTlsTrustedCommonNamesQueries queries) returns MsgVpnRestDeliveryPointRestConsumerTlsTrustedCommonNamesResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/restDeliveryPoints/${getEncodedUri(restDeliveryPointName)}/restConsumers/${getEncodedUri(restConsumerName)}/tlsTrustedCommonNames`;
        map<Encoding> queryParamEncoding = {"where": {style: FORM, explode: false}, "select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create a Trusted Common Name object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + restDeliveryPointName - The name of the REST Delivery Point.
    # + restConsumerName - The name of the REST Consumer.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Trusted Common Name object's attributes. 
    # + return - The Trusted Common Name object's attributes after being created, and the request metadata. 
    #
    # # Deprecated
    @deprecated
    remote isolated function createMsgVpnRestDeliveryPointRestConsumerTlsTrustedCommonName(string msgVpnName, string restDeliveryPointName, string restConsumerName, MsgVpnRestDeliveryPointRestConsumerTlsTrustedCommonName payload, map<string|string[]> headers = {}, *CreateMsgVpnRestDeliveryPointRestConsumerTlsTrustedCommonNameQueries queries) returns MsgVpnRestDeliveryPointRestConsumerTlsTrustedCommonNameResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/restDeliveryPoints/${getEncodedUri(restDeliveryPointName)}/restConsumers/${getEncodedUri(restConsumerName)}/tlsTrustedCommonNames`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get a Trusted Common Name object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + restDeliveryPointName - The name of the REST Delivery Point.
    # + restConsumerName - The name of the REST Consumer.
    # + tlsTrustedCommonName - The expected trusted common name of the remote certificate.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The Trusted Common Name object's attributes, and the request metadata. 
    #
    # # Deprecated
    @deprecated
    remote isolated function getMsgVpnRestDeliveryPointRestConsumerTlsTrustedCommonName(string msgVpnName, string restDeliveryPointName, string restConsumerName, string tlsTrustedCommonName, map<string|string[]> headers = {}, *GetMsgVpnRestDeliveryPointRestConsumerTlsTrustedCommonNameQueries queries) returns MsgVpnRestDeliveryPointRestConsumerTlsTrustedCommonNameResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/restDeliveryPoints/${getEncodedUri(restDeliveryPointName)}/restConsumers/${getEncodedUri(restConsumerName)}/tlsTrustedCommonNames/${getEncodedUri(tlsTrustedCommonName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete a Trusted Common Name object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + restDeliveryPointName - The name of the REST Delivery Point.
    # + restConsumerName - The name of the REST Consumer.
    # + tlsTrustedCommonName - The expected trusted common name of the remote certificate.
    # + headers - Headers to be sent with the request 
    # + return - The request metadata. 
    #
    # # Deprecated
    @deprecated
    remote isolated function deleteMsgVpnRestDeliveryPointRestConsumerTlsTrustedCommonName(string msgVpnName, string restDeliveryPointName, string restConsumerName, string tlsTrustedCommonName, map<string|string[]> headers = {}) returns SempMetaOnlyResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/restDeliveryPoints/${getEncodedUri(restDeliveryPointName)}/restConsumers/${getEncodedUri(restConsumerName)}/tlsTrustedCommonNames/${getEncodedUri(tlsTrustedCommonName)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Get a list of Sequenced Topic objects.
    #
    # + msgVpnName - The name of the Message VPN.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The list of Sequenced Topic objects' attributes, and the request metadata. 
    #
    # # Deprecated
    @deprecated
    remote isolated function getMsgVpnSequencedTopics(string msgVpnName, map<string|string[]> headers = {}, *GetMsgVpnSequencedTopicsQueries queries) returns MsgVpnSequencedTopicsResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/sequencedTopics`;
        map<Encoding> queryParamEncoding = {"where": {style: FORM, explode: false}, "select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create a Sequenced Topic object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Sequenced Topic object's attributes. 
    # + return - The Sequenced Topic object's attributes after being created, and the request metadata. 
    #
    # # Deprecated
    @deprecated
    remote isolated function createMsgVpnSequencedTopic(string msgVpnName, MsgVpnSequencedTopic payload, map<string|string[]> headers = {}, *CreateMsgVpnSequencedTopicQueries queries) returns MsgVpnSequencedTopicResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/sequencedTopics`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get a Sequenced Topic object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + sequencedTopic - Topic for applying sequence numbers.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The Sequenced Topic object's attributes, and the request metadata. 
    #
    # # Deprecated
    @deprecated
    remote isolated function getMsgVpnSequencedTopic(string msgVpnName, string sequencedTopic, map<string|string[]> headers = {}, *GetMsgVpnSequencedTopicQueries queries) returns MsgVpnSequencedTopicResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/sequencedTopics/${getEncodedUri(sequencedTopic)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete a Sequenced Topic object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + sequencedTopic - Topic for applying sequence numbers.
    # + headers - Headers to be sent with the request 
    # + return - The request metadata. 
    #
    # # Deprecated
    @deprecated
    remote isolated function deleteMsgVpnSequencedTopic(string msgVpnName, string sequencedTopic, map<string|string[]> headers = {}) returns SempMetaOnlyResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/sequencedTopics/${getEncodedUri(sequencedTopic)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Get a list of Telemetry Profile objects.
    #
    # + msgVpnName - The name of the Message VPN.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The list of Telemetry Profile objects' attributes, and the request metadata. 
    remote isolated function getMsgVpnTelemetryProfiles(string msgVpnName, map<string|string[]> headers = {}, *GetMsgVpnTelemetryProfilesQueries queries) returns MsgVpnTelemetryProfilesResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/telemetryProfiles`;
        map<Encoding> queryParamEncoding = {"where": {style: FORM, explode: false}, "select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create a Telemetry Profile object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Telemetry Profile object's attributes. 
    # + return - The Telemetry Profile object's attributes after being created, and the request metadata. 
    remote isolated function createMsgVpnTelemetryProfile(string msgVpnName, MsgVpnTelemetryProfile payload, map<string|string[]> headers = {}, *CreateMsgVpnTelemetryProfileQueries queries) returns MsgVpnTelemetryProfileResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/telemetryProfiles`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get a Telemetry Profile object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + telemetryProfileName - The name of the Telemetry Profile.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The Telemetry Profile object's attributes, and the request metadata. 
    remote isolated function getMsgVpnTelemetryProfile(string msgVpnName, string telemetryProfileName, map<string|string[]> headers = {}, *GetMsgVpnTelemetryProfileQueries queries) returns MsgVpnTelemetryProfileResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/telemetryProfiles/${getEncodedUri(telemetryProfileName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Replace a Telemetry Profile object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + telemetryProfileName - The name of the Telemetry Profile.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Telemetry Profile object's attributes. 
    # + return - The Telemetry Profile object's attributes after being replaced, and the request metadata. 
    remote isolated function replaceMsgVpnTelemetryProfile(string msgVpnName, string telemetryProfileName, MsgVpnTelemetryProfile payload, map<string|string[]> headers = {}, *ReplaceMsgVpnTelemetryProfileQueries queries) returns MsgVpnTelemetryProfileResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/telemetryProfiles/${getEncodedUri(telemetryProfileName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Delete a Telemetry Profile object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + telemetryProfileName - The name of the Telemetry Profile.
    # + headers - Headers to be sent with the request 
    # + return - The request metadata. 
    remote isolated function deleteMsgVpnTelemetryProfile(string msgVpnName, string telemetryProfileName, map<string|string[]> headers = {}) returns SempMetaOnlyResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/telemetryProfiles/${getEncodedUri(telemetryProfileName)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Update a Telemetry Profile object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + telemetryProfileName - The name of the Telemetry Profile.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Telemetry Profile object's attributes. 
    # + return - The Telemetry Profile object's attributes after being updated, and the request metadata. 
    remote isolated function updateMsgVpnTelemetryProfile(string msgVpnName, string telemetryProfileName, MsgVpnTelemetryProfile payload, map<string|string[]> headers = {}, *UpdateMsgVpnTelemetryProfileQueries queries) returns MsgVpnTelemetryProfileResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/telemetryProfiles/${getEncodedUri(telemetryProfileName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Get a list of Receiver ACL Connect Exception objects.
    #
    # + msgVpnName - The name of the Message VPN.
    # + telemetryProfileName - The name of the Telemetry Profile.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The list of Receiver ACL Connect Exception objects' attributes, and the request metadata. 
    remote isolated function getMsgVpnTelemetryProfileReceiverAclConnectExceptions(string msgVpnName, string telemetryProfileName, map<string|string[]> headers = {}, *GetMsgVpnTelemetryProfileReceiverAclConnectExceptionsQueries queries) returns MsgVpnTelemetryProfileReceiverAclConnectExceptionsResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/telemetryProfiles/${getEncodedUri(telemetryProfileName)}/receiverAclConnectExceptions`;
        map<Encoding> queryParamEncoding = {"where": {style: FORM, explode: false}, "select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create a Receiver ACL Connect Exception object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + telemetryProfileName - The name of the Telemetry Profile.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Receiver ACL Connect Exception object's attributes. 
    # + return - The Receiver ACL Connect Exception object's attributes after being created, and the request metadata. 
    remote isolated function createMsgVpnTelemetryProfileReceiverAclConnectException(string msgVpnName, string telemetryProfileName, MsgVpnTelemetryProfileReceiverAclConnectException payload, map<string|string[]> headers = {}, *CreateMsgVpnTelemetryProfileReceiverAclConnectExceptionQueries queries) returns MsgVpnTelemetryProfileReceiverAclConnectExceptionResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/telemetryProfiles/${getEncodedUri(telemetryProfileName)}/receiverAclConnectExceptions`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get a Receiver ACL Connect Exception object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + telemetryProfileName - The name of the Telemetry Profile.
    # + receiverAclConnectExceptionAddress - The IP address/netmask of the receiver connect exception in CIDR form.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The Receiver ACL Connect Exception object's attributes, and the request metadata. 
    remote isolated function getMsgVpnTelemetryProfileReceiverAclConnectException(string msgVpnName, string telemetryProfileName, string receiverAclConnectExceptionAddress, map<string|string[]> headers = {}, *GetMsgVpnTelemetryProfileReceiverAclConnectExceptionQueries queries) returns MsgVpnTelemetryProfileReceiverAclConnectExceptionResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/telemetryProfiles/${getEncodedUri(telemetryProfileName)}/receiverAclConnectExceptions/${getEncodedUri(receiverAclConnectExceptionAddress)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete a Receiver ACL Connect Exception object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + telemetryProfileName - The name of the Telemetry Profile.
    # + receiverAclConnectExceptionAddress - The IP address/netmask of the receiver connect exception in CIDR form.
    # + headers - Headers to be sent with the request 
    # + return - The request metadata. 
    remote isolated function deleteMsgVpnTelemetryProfileReceiverAclConnectException(string msgVpnName, string telemetryProfileName, string receiverAclConnectExceptionAddress, map<string|string[]> headers = {}) returns SempMetaOnlyResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/telemetryProfiles/${getEncodedUri(telemetryProfileName)}/receiverAclConnectExceptions/${getEncodedUri(receiverAclConnectExceptionAddress)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Get a list of Trace Filter objects.
    #
    # + msgVpnName - The name of the Message VPN.
    # + telemetryProfileName - The name of the Telemetry Profile.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The list of Trace Filter objects' attributes, and the request metadata. 
    remote isolated function getMsgVpnTelemetryProfileTraceFilters(string msgVpnName, string telemetryProfileName, map<string|string[]> headers = {}, *GetMsgVpnTelemetryProfileTraceFiltersQueries queries) returns MsgVpnTelemetryProfileTraceFiltersResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/telemetryProfiles/${getEncodedUri(telemetryProfileName)}/traceFilters`;
        map<Encoding> queryParamEncoding = {"where": {style: FORM, explode: false}, "select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create a Trace Filter object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + telemetryProfileName - The name of the Telemetry Profile.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Trace Filter object's attributes. 
    # + return - The Trace Filter object's attributes after being created, and the request metadata. 
    remote isolated function createMsgVpnTelemetryProfileTraceFilter(string msgVpnName, string telemetryProfileName, MsgVpnTelemetryProfileTraceFilter payload, map<string|string[]> headers = {}, *CreateMsgVpnTelemetryProfileTraceFilterQueries queries) returns MsgVpnTelemetryProfileTraceFilterResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/telemetryProfiles/${getEncodedUri(telemetryProfileName)}/traceFilters`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get a Trace Filter object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + telemetryProfileName - The name of the Telemetry Profile.
    # + traceFilterName - A name used to identify the trace filter. Consider a name that describes the subscriptions contained within the filter, such as the name of the application and/or the scenario in which the trace filter might be enabled, such as "appNameDebug".
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The Trace Filter object's attributes, and the request metadata. 
    remote isolated function getMsgVpnTelemetryProfileTraceFilter(string msgVpnName, string telemetryProfileName, string traceFilterName, map<string|string[]> headers = {}, *GetMsgVpnTelemetryProfileTraceFilterQueries queries) returns MsgVpnTelemetryProfileTraceFilterResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/telemetryProfiles/${getEncodedUri(telemetryProfileName)}/traceFilters/${getEncodedUri(traceFilterName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Replace a Trace Filter object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + telemetryProfileName - The name of the Telemetry Profile.
    # + traceFilterName - A name used to identify the trace filter. Consider a name that describes the subscriptions contained within the filter, such as the name of the application and/or the scenario in which the trace filter might be enabled, such as "appNameDebug".
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Trace Filter object's attributes. 
    # + return - The Trace Filter object's attributes after being replaced, and the request metadata. 
    remote isolated function replaceMsgVpnTelemetryProfileTraceFilter(string msgVpnName, string telemetryProfileName, string traceFilterName, MsgVpnTelemetryProfileTraceFilter payload, map<string|string[]> headers = {}, *ReplaceMsgVpnTelemetryProfileTraceFilterQueries queries) returns MsgVpnTelemetryProfileTraceFilterResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/telemetryProfiles/${getEncodedUri(telemetryProfileName)}/traceFilters/${getEncodedUri(traceFilterName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Delete a Trace Filter object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + telemetryProfileName - The name of the Telemetry Profile.
    # + traceFilterName - A name used to identify the trace filter. Consider a name that describes the subscriptions contained within the filter, such as the name of the application and/or the scenario in which the trace filter might be enabled, such as "appNameDebug".
    # + headers - Headers to be sent with the request 
    # + return - The request metadata. 
    remote isolated function deleteMsgVpnTelemetryProfileTraceFilter(string msgVpnName, string telemetryProfileName, string traceFilterName, map<string|string[]> headers = {}) returns SempMetaOnlyResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/telemetryProfiles/${getEncodedUri(telemetryProfileName)}/traceFilters/${getEncodedUri(traceFilterName)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Update a Trace Filter object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + telemetryProfileName - The name of the Telemetry Profile.
    # + traceFilterName - A name used to identify the trace filter. Consider a name that describes the subscriptions contained within the filter, such as the name of the application and/or the scenario in which the trace filter might be enabled, such as "appNameDebug".
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Trace Filter object's attributes. 
    # + return - The Trace Filter object's attributes after being updated, and the request metadata. 
    remote isolated function updateMsgVpnTelemetryProfileTraceFilter(string msgVpnName, string telemetryProfileName, string traceFilterName, MsgVpnTelemetryProfileTraceFilter payload, map<string|string[]> headers = {}, *UpdateMsgVpnTelemetryProfileTraceFilterQueries queries) returns MsgVpnTelemetryProfileTraceFilterResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/telemetryProfiles/${getEncodedUri(telemetryProfileName)}/traceFilters/${getEncodedUri(traceFilterName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Get a list of Telemetry Trace Filter Subscription objects.
    #
    # + msgVpnName - The name of the Message VPN.
    # + telemetryProfileName - The name of the Telemetry Profile.
    # + traceFilterName - A name used to identify the trace filter. Consider a name that describes the subscriptions contained within the filter, such as the name of the application and/or the scenario in which the trace filter might be enabled, such as "appNameDebug".
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The list of Telemetry Trace Filter Subscription objects' attributes, and the request metadata. 
    remote isolated function getMsgVpnTelemetryProfileTraceFilterSubscriptions(string msgVpnName, string telemetryProfileName, string traceFilterName, map<string|string[]> headers = {}, *GetMsgVpnTelemetryProfileTraceFilterSubscriptionsQueries queries) returns MsgVpnTelemetryProfileTraceFilterSubscriptionsResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/telemetryProfiles/${getEncodedUri(telemetryProfileName)}/traceFilters/${getEncodedUri(traceFilterName)}/subscriptions`;
        map<Encoding> queryParamEncoding = {"where": {style: FORM, explode: false}, "select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create a Telemetry Trace Filter Subscription object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + telemetryProfileName - The name of the Telemetry Profile.
    # + traceFilterName - A name used to identify the trace filter. Consider a name that describes the subscriptions contained within the filter, such as the name of the application and/or the scenario in which the trace filter might be enabled, such as "appNameDebug".
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Telemetry Trace Filter Subscription object's attributes. 
    # + return - The Telemetry Trace Filter Subscription object's attributes after being created, and the request metadata. 
    remote isolated function createMsgVpnTelemetryProfileTraceFilterSubscription(string msgVpnName, string telemetryProfileName, string traceFilterName, MsgVpnTelemetryProfileTraceFilterSubscription payload, map<string|string[]> headers = {}, *CreateMsgVpnTelemetryProfileTraceFilterSubscriptionQueries queries) returns MsgVpnTelemetryProfileTraceFilterSubscriptionResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/telemetryProfiles/${getEncodedUri(telemetryProfileName)}/traceFilters/${getEncodedUri(traceFilterName)}/subscriptions`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get a Telemetry Trace Filter Subscription object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + telemetryProfileName - The name of the Telemetry Profile.
    # + traceFilterName - A name used to identify the trace filter. Consider a name that describes the subscriptions contained within the filter, such as the name of the application and/or the scenario in which the trace filter might be enabled, such as "appNameDebug".
    # + subscription - Messages matching this subscription will follow this filter's configuration.
    # + subscriptionSyntax - The syntax of the trace filter subscription.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The Telemetry Trace Filter Subscription object's attributes, and the request metadata. 
    remote isolated function getMsgVpnTelemetryProfileTraceFilterSubscription(string msgVpnName, string telemetryProfileName, string traceFilterName, string subscription, string subscriptionSyntax, map<string|string[]> headers = {}, *GetMsgVpnTelemetryProfileTraceFilterSubscriptionQueries queries) returns MsgVpnTelemetryProfileTraceFilterSubscriptionResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/telemetryProfiles/${getEncodedUri(telemetryProfileName)}/traceFilters/${getEncodedUri(traceFilterName)}/subscriptions/${getEncodedUri(subscription)},${getEncodedUri(subscriptionSyntax)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete a Telemetry Trace Filter Subscription object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + telemetryProfileName - The name of the Telemetry Profile.
    # + traceFilterName - A name used to identify the trace filter. Consider a name that describes the subscriptions contained within the filter, such as the name of the application and/or the scenario in which the trace filter might be enabled, such as "appNameDebug".
    # + subscription - Messages matching this subscription will follow this filter's configuration.
    # + subscriptionSyntax - The syntax of the trace filter subscription.
    # + headers - Headers to be sent with the request 
    # + return - The request metadata. 
    remote isolated function deleteMsgVpnTelemetryProfileTraceFilterSubscription(string msgVpnName, string telemetryProfileName, string traceFilterName, string subscription, string subscriptionSyntax, map<string|string[]> headers = {}) returns SempMetaOnlyResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/telemetryProfiles/${getEncodedUri(telemetryProfileName)}/traceFilters/${getEncodedUri(traceFilterName)}/subscriptions/${getEncodedUri(subscription)},${getEncodedUri(subscriptionSyntax)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Get a list of Topic Endpoint Template objects.
    #
    # + msgVpnName - The name of the Message VPN.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The list of Topic Endpoint Template objects' attributes, and the request metadata. 
    remote isolated function getMsgVpnTopicEndpointTemplates(string msgVpnName, map<string|string[]> headers = {}, *GetMsgVpnTopicEndpointTemplatesQueries queries) returns MsgVpnTopicEndpointTemplatesResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/topicEndpointTemplates`;
        map<Encoding> queryParamEncoding = {"where": {style: FORM, explode: false}, "select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create a Topic Endpoint Template object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Topic Endpoint Template object's attributes. 
    # + return - The Topic Endpoint Template object's attributes after being created, and the request metadata. 
    remote isolated function createMsgVpnTopicEndpointTemplate(string msgVpnName, MsgVpnTopicEndpointTemplate payload, map<string|string[]> headers = {}, *CreateMsgVpnTopicEndpointTemplateQueries queries) returns MsgVpnTopicEndpointTemplateResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/topicEndpointTemplates`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get a Topic Endpoint Template object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + topicEndpointTemplateName - The name of the Topic Endpoint Template.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The Topic Endpoint Template object's attributes, and the request metadata. 
    remote isolated function getMsgVpnTopicEndpointTemplate(string msgVpnName, string topicEndpointTemplateName, map<string|string[]> headers = {}, *GetMsgVpnTopicEndpointTemplateQueries queries) returns MsgVpnTopicEndpointTemplateResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/topicEndpointTemplates/${getEncodedUri(topicEndpointTemplateName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Replace a Topic Endpoint Template object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + topicEndpointTemplateName - The name of the Topic Endpoint Template.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Topic Endpoint Template object's attributes. 
    # + return - The Topic Endpoint Template object's attributes after being replaced, and the request metadata. 
    remote isolated function replaceMsgVpnTopicEndpointTemplate(string msgVpnName, string topicEndpointTemplateName, MsgVpnTopicEndpointTemplate payload, map<string|string[]> headers = {}, *ReplaceMsgVpnTopicEndpointTemplateQueries queries) returns MsgVpnTopicEndpointTemplateResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/topicEndpointTemplates/${getEncodedUri(topicEndpointTemplateName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Delete a Topic Endpoint Template object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + topicEndpointTemplateName - The name of the Topic Endpoint Template.
    # + headers - Headers to be sent with the request 
    # + return - The request metadata. 
    remote isolated function deleteMsgVpnTopicEndpointTemplate(string msgVpnName, string topicEndpointTemplateName, map<string|string[]> headers = {}) returns SempMetaOnlyResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/topicEndpointTemplates/${getEncodedUri(topicEndpointTemplateName)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Update a Topic Endpoint Template object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + topicEndpointTemplateName - The name of the Topic Endpoint Template.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Topic Endpoint Template object's attributes. 
    # + return - The Topic Endpoint Template object's attributes after being updated, and the request metadata. 
    remote isolated function updateMsgVpnTopicEndpointTemplate(string msgVpnName, string topicEndpointTemplateName, MsgVpnTopicEndpointTemplate payload, map<string|string[]> headers = {}, *UpdateMsgVpnTopicEndpointTemplateQueries queries) returns MsgVpnTopicEndpointTemplateResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/topicEndpointTemplates/${getEncodedUri(topicEndpointTemplateName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Get a list of Topic Endpoint objects.
    #
    # + msgVpnName - The name of the Message VPN.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The list of Topic Endpoint objects' attributes, and the request metadata. 
    remote isolated function getMsgVpnTopicEndpoints(string msgVpnName, map<string|string[]> headers = {}, *GetMsgVpnTopicEndpointsQueries queries) returns MsgVpnTopicEndpointsResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/topicEndpoints`;
        map<Encoding> queryParamEncoding = {"where": {style: FORM, explode: false}, "select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create a Topic Endpoint object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Topic Endpoint object's attributes. 
    # + return - The Topic Endpoint object's attributes after being created, and the request metadata. 
    remote isolated function createMsgVpnTopicEndpoint(string msgVpnName, MsgVpnTopicEndpoint payload, map<string|string[]> headers = {}, *CreateMsgVpnTopicEndpointQueries queries) returns MsgVpnTopicEndpointResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/topicEndpoints`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get a Topic Endpoint object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + topicEndpointName - The name of the Topic Endpoint.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The Topic Endpoint object's attributes, and the request metadata. 
    remote isolated function getMsgVpnTopicEndpoint(string msgVpnName, string topicEndpointName, map<string|string[]> headers = {}, *GetMsgVpnTopicEndpointQueries queries) returns MsgVpnTopicEndpointResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/topicEndpoints/${getEncodedUri(topicEndpointName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Replace a Topic Endpoint object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + topicEndpointName - The name of the Topic Endpoint.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Topic Endpoint object's attributes. 
    # + return - The Topic Endpoint object's attributes after being replaced, and the request metadata. 
    remote isolated function replaceMsgVpnTopicEndpoint(string msgVpnName, string topicEndpointName, MsgVpnTopicEndpoint payload, map<string|string[]> headers = {}, *ReplaceMsgVpnTopicEndpointQueries queries) returns MsgVpnTopicEndpointResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/topicEndpoints/${getEncodedUri(topicEndpointName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Delete a Topic Endpoint object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + topicEndpointName - The name of the Topic Endpoint.
    # + headers - Headers to be sent with the request 
    # + return - The request metadata. 
    remote isolated function deleteMsgVpnTopicEndpoint(string msgVpnName, string topicEndpointName, map<string|string[]> headers = {}) returns SempMetaOnlyResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/topicEndpoints/${getEncodedUri(topicEndpointName)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Update a Topic Endpoint object.
    #
    # + msgVpnName - The name of the Message VPN.
    # + topicEndpointName - The name of the Topic Endpoint.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Topic Endpoint object's attributes. 
    # + return - The Topic Endpoint object's attributes after being updated, and the request metadata. 
    remote isolated function updateMsgVpnTopicEndpoint(string msgVpnName, string topicEndpointName, MsgVpnTopicEndpoint payload, map<string|string[]> headers = {}, *UpdateMsgVpnTopicEndpointQueries queries) returns MsgVpnTopicEndpointResponse|error {
        string resourcePath = string `/msgVpns/${getEncodedUri(msgVpnName)}/topicEndpoints/${getEncodedUri(topicEndpointName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Get a list of OAuth Profile objects.
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The list of OAuth Profile objects' attributes, and the request metadata. 
    remote isolated function getOauthProfiles(map<string|string[]> headers = {}, *GetOauthProfilesQueries queries) returns OauthProfilesResponse|error {
        string resourcePath = string `/oauthProfiles`;
        map<Encoding> queryParamEncoding = {"where": {style: FORM, explode: false}, "select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create an OAuth Profile object.
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The OAuth Profile object's attributes. 
    # + return - The OAuth Profile object's attributes after being created, and the request metadata. 
    remote isolated function createOauthProfile(OauthProfile payload, map<string|string[]> headers = {}, *CreateOauthProfileQueries queries) returns OauthProfileResponse|error {
        string resourcePath = string `/oauthProfiles`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get an OAuth Profile object.
    #
    # + oauthProfileName - The name of the OAuth profile.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The OAuth Profile object's attributes, and the request metadata. 
    remote isolated function getOauthProfile(string oauthProfileName, map<string|string[]> headers = {}, *GetOauthProfileQueries queries) returns OauthProfileResponse|error {
        string resourcePath = string `/oauthProfiles/${getEncodedUri(oauthProfileName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Replace an OAuth Profile object.
    #
    # + oauthProfileName - The name of the OAuth profile.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The OAuth Profile object's attributes. 
    # + return - The OAuth Profile object's attributes after being replaced, and the request metadata. 
    remote isolated function replaceOauthProfile(string oauthProfileName, OauthProfile payload, map<string|string[]> headers = {}, *ReplaceOauthProfileQueries queries) returns OauthProfileResponse|error {
        string resourcePath = string `/oauthProfiles/${getEncodedUri(oauthProfileName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Delete an OAuth Profile object.
    #
    # + oauthProfileName - The name of the OAuth profile.
    # + headers - Headers to be sent with the request 
    # + return - The request metadata. 
    remote isolated function deleteOauthProfile(string oauthProfileName, map<string|string[]> headers = {}) returns SempMetaOnlyResponse|error {
        string resourcePath = string `/oauthProfiles/${getEncodedUri(oauthProfileName)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Update an OAuth Profile object.
    #
    # + oauthProfileName - The name of the OAuth profile.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The OAuth Profile object's attributes. 
    # + return - The OAuth Profile object's attributes after being updated, and the request metadata. 
    remote isolated function updateOauthProfile(string oauthProfileName, OauthProfile payload, map<string|string[]> headers = {}, *UpdateOauthProfileQueries queries) returns OauthProfileResponse|error {
        string resourcePath = string `/oauthProfiles/${getEncodedUri(oauthProfileName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Get a list of Group Access Level objects.
    #
    # + oauthProfileName - The name of the OAuth profile.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The list of Group Access Level objects' attributes, and the request metadata. 
    remote isolated function getOauthProfileAccessLevelGroups(string oauthProfileName, map<string|string[]> headers = {}, *GetOauthProfileAccessLevelGroupsQueries queries) returns OauthProfileAccessLevelGroupsResponse|error {
        string resourcePath = string `/oauthProfiles/${getEncodedUri(oauthProfileName)}/accessLevelGroups`;
        map<Encoding> queryParamEncoding = {"where": {style: FORM, explode: false}, "select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create a Group Access Level object.
    #
    # + oauthProfileName - The name of the OAuth profile.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Group Access Level object's attributes. 
    # + return - The Group Access Level object's attributes after being created, and the request metadata. 
    remote isolated function createOauthProfileAccessLevelGroup(string oauthProfileName, OauthProfileAccessLevelGroup payload, map<string|string[]> headers = {}, *CreateOauthProfileAccessLevelGroupQueries queries) returns OauthProfileAccessLevelGroupResponse|error {
        string resourcePath = string `/oauthProfiles/${getEncodedUri(oauthProfileName)}/accessLevelGroups`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get a Group Access Level object.
    #
    # + oauthProfileName - The name of the OAuth profile.
    # + groupName - The name of the group.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The Group Access Level object's attributes, and the request metadata. 
    remote isolated function getOauthProfileAccessLevelGroup(string oauthProfileName, string groupName, map<string|string[]> headers = {}, *GetOauthProfileAccessLevelGroupQueries queries) returns OauthProfileAccessLevelGroupResponse|error {
        string resourcePath = string `/oauthProfiles/${getEncodedUri(oauthProfileName)}/accessLevelGroups/${getEncodedUri(groupName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Replace a Group Access Level object.
    #
    # + oauthProfileName - The name of the OAuth profile.
    # + groupName - The name of the group.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Group Access Level object's attributes. 
    # + return - The Group Access Level object's attributes after being replaced, and the request metadata. 
    remote isolated function replaceOauthProfileAccessLevelGroup(string oauthProfileName, string groupName, OauthProfileAccessLevelGroup payload, map<string|string[]> headers = {}, *ReplaceOauthProfileAccessLevelGroupQueries queries) returns OauthProfileAccessLevelGroupResponse|error {
        string resourcePath = string `/oauthProfiles/${getEncodedUri(oauthProfileName)}/accessLevelGroups/${getEncodedUri(groupName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Delete a Group Access Level object.
    #
    # + oauthProfileName - The name of the OAuth profile.
    # + groupName - The name of the group.
    # + headers - Headers to be sent with the request 
    # + return - The request metadata. 
    remote isolated function deleteOauthProfileAccessLevelGroup(string oauthProfileName, string groupName, map<string|string[]> headers = {}) returns SempMetaOnlyResponse|error {
        string resourcePath = string `/oauthProfiles/${getEncodedUri(oauthProfileName)}/accessLevelGroups/${getEncodedUri(groupName)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Update a Group Access Level object.
    #
    # + oauthProfileName - The name of the OAuth profile.
    # + groupName - The name of the group.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Group Access Level object's attributes. 
    # + return - The Group Access Level object's attributes after being updated, and the request metadata. 
    remote isolated function updateOauthProfileAccessLevelGroup(string oauthProfileName, string groupName, OauthProfileAccessLevelGroup payload, map<string|string[]> headers = {}, *UpdateOauthProfileAccessLevelGroupQueries queries) returns OauthProfileAccessLevelGroupResponse|error {
        string resourcePath = string `/oauthProfiles/${getEncodedUri(oauthProfileName)}/accessLevelGroups/${getEncodedUri(groupName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Get a list of Message VPN Access-Level Exception objects.
    #
    # + oauthProfileName - The name of the OAuth profile.
    # + groupName - The name of the group.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The list of Message VPN Access-Level Exception objects' attributes, and the request metadata. 
    remote isolated function getOauthProfileAccessLevelGroupMsgVpnAccessLevelExceptions(string oauthProfileName, string groupName, map<string|string[]> headers = {}, *GetOauthProfileAccessLevelGroupMsgVpnAccessLevelExceptionsQueries queries) returns OauthProfileAccessLevelGroupMsgVpnAccessLevelExceptionsResponse|error {
        string resourcePath = string `/oauthProfiles/${getEncodedUri(oauthProfileName)}/accessLevelGroups/${getEncodedUri(groupName)}/msgVpnAccessLevelExceptions`;
        map<Encoding> queryParamEncoding = {"where": {style: FORM, explode: false}, "select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create a Message VPN Access-Level Exception object.
    #
    # + oauthProfileName - The name of the OAuth profile.
    # + groupName - The name of the group.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Message VPN Access-Level Exception object's attributes. 
    # + return - The Message VPN Access-Level Exception object's attributes after being created, and the request metadata. 
    remote isolated function createOauthProfileAccessLevelGroupMsgVpnAccessLevelException(string oauthProfileName, string groupName, OauthProfileAccessLevelGroupMsgVpnAccessLevelException payload, map<string|string[]> headers = {}, *CreateOauthProfileAccessLevelGroupMsgVpnAccessLevelExceptionQueries queries) returns OauthProfileAccessLevelGroupMsgVpnAccessLevelExceptionResponse|error {
        string resourcePath = string `/oauthProfiles/${getEncodedUri(oauthProfileName)}/accessLevelGroups/${getEncodedUri(groupName)}/msgVpnAccessLevelExceptions`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get a Message VPN Access-Level Exception object.
    #
    # + oauthProfileName - The name of the OAuth profile.
    # + groupName - The name of the group.
    # + msgVpnName - The name of the message VPN.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The Message VPN Access-Level Exception object's attributes, and the request metadata. 
    remote isolated function getOauthProfileAccessLevelGroupMsgVpnAccessLevelException(string oauthProfileName, string groupName, string msgVpnName, map<string|string[]> headers = {}, *GetOauthProfileAccessLevelGroupMsgVpnAccessLevelExceptionQueries queries) returns OauthProfileAccessLevelGroupMsgVpnAccessLevelExceptionResponse|error {
        string resourcePath = string `/oauthProfiles/${getEncodedUri(oauthProfileName)}/accessLevelGroups/${getEncodedUri(groupName)}/msgVpnAccessLevelExceptions/${getEncodedUri(msgVpnName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Replace a Message VPN Access-Level Exception object.
    #
    # + oauthProfileName - The name of the OAuth profile.
    # + groupName - The name of the group.
    # + msgVpnName - The name of the message VPN.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Message VPN Access-Level Exception object's attributes. 
    # + return - The Message VPN Access-Level Exception object's attributes after being replaced, and the request metadata. 
    remote isolated function replaceOauthProfileAccessLevelGroupMsgVpnAccessLevelException(string oauthProfileName, string groupName, string msgVpnName, OauthProfileAccessLevelGroupMsgVpnAccessLevelException payload, map<string|string[]> headers = {}, *ReplaceOauthProfileAccessLevelGroupMsgVpnAccessLevelExceptionQueries queries) returns OauthProfileAccessLevelGroupMsgVpnAccessLevelExceptionResponse|error {
        string resourcePath = string `/oauthProfiles/${getEncodedUri(oauthProfileName)}/accessLevelGroups/${getEncodedUri(groupName)}/msgVpnAccessLevelExceptions/${getEncodedUri(msgVpnName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Delete a Message VPN Access-Level Exception object.
    #
    # + oauthProfileName - The name of the OAuth profile.
    # + groupName - The name of the group.
    # + msgVpnName - The name of the message VPN.
    # + headers - Headers to be sent with the request 
    # + return - The request metadata. 
    remote isolated function deleteOauthProfileAccessLevelGroupMsgVpnAccessLevelException(string oauthProfileName, string groupName, string msgVpnName, map<string|string[]> headers = {}) returns SempMetaOnlyResponse|error {
        string resourcePath = string `/oauthProfiles/${getEncodedUri(oauthProfileName)}/accessLevelGroups/${getEncodedUri(groupName)}/msgVpnAccessLevelExceptions/${getEncodedUri(msgVpnName)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Update a Message VPN Access-Level Exception object.
    #
    # + oauthProfileName - The name of the OAuth profile.
    # + groupName - The name of the group.
    # + msgVpnName - The name of the message VPN.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Message VPN Access-Level Exception object's attributes. 
    # + return - The Message VPN Access-Level Exception object's attributes after being updated, and the request metadata. 
    remote isolated function updateOauthProfileAccessLevelGroupMsgVpnAccessLevelException(string oauthProfileName, string groupName, string msgVpnName, OauthProfileAccessLevelGroupMsgVpnAccessLevelException payload, map<string|string[]> headers = {}, *UpdateOauthProfileAccessLevelGroupMsgVpnAccessLevelExceptionQueries queries) returns OauthProfileAccessLevelGroupMsgVpnAccessLevelExceptionResponse|error {
        string resourcePath = string `/oauthProfiles/${getEncodedUri(oauthProfileName)}/accessLevelGroups/${getEncodedUri(groupName)}/msgVpnAccessLevelExceptions/${getEncodedUri(msgVpnName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Get a list of Allowed Host Value objects.
    #
    # + oauthProfileName - The name of the OAuth profile.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The list of Allowed Host Value objects' attributes, and the request metadata. 
    remote isolated function getOauthProfileClientAllowedHosts(string oauthProfileName, map<string|string[]> headers = {}, *GetOauthProfileClientAllowedHostsQueries queries) returns OauthProfileClientAllowedHostsResponse|error {
        string resourcePath = string `/oauthProfiles/${getEncodedUri(oauthProfileName)}/clientAllowedHosts`;
        map<Encoding> queryParamEncoding = {"where": {style: FORM, explode: false}, "select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create an Allowed Host Value object.
    #
    # + oauthProfileName - The name of the OAuth profile.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Allowed Host Value object's attributes. 
    # + return - The Allowed Host Value object's attributes after being created, and the request metadata. 
    remote isolated function createOauthProfileClientAllowedHost(string oauthProfileName, OauthProfileClientAllowedHost payload, map<string|string[]> headers = {}, *CreateOauthProfileClientAllowedHostQueries queries) returns OauthProfileClientAllowedHostResponse|error {
        string resourcePath = string `/oauthProfiles/${getEncodedUri(oauthProfileName)}/clientAllowedHosts`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get an Allowed Host Value object.
    #
    # + oauthProfileName - The name of the OAuth profile.
    # + allowedHost - An allowed value for the Host header.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The Allowed Host Value object's attributes, and the request metadata. 
    remote isolated function getOauthProfileClientAllowedHost(string oauthProfileName, string allowedHost, map<string|string[]> headers = {}, *GetOauthProfileClientAllowedHostQueries queries) returns OauthProfileClientAllowedHostResponse|error {
        string resourcePath = string `/oauthProfiles/${getEncodedUri(oauthProfileName)}/clientAllowedHosts/${getEncodedUri(allowedHost)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete an Allowed Host Value object.
    #
    # + oauthProfileName - The name of the OAuth profile.
    # + allowedHost - An allowed value for the Host header.
    # + headers - Headers to be sent with the request 
    # + return - The request metadata. 
    remote isolated function deleteOauthProfileClientAllowedHost(string oauthProfileName, string allowedHost, map<string|string[]> headers = {}) returns SempMetaOnlyResponse|error {
        string resourcePath = string `/oauthProfiles/${getEncodedUri(oauthProfileName)}/clientAllowedHosts/${getEncodedUri(allowedHost)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Get a list of Authorization Parameter objects.
    #
    # + oauthProfileName - The name of the OAuth profile.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The list of Authorization Parameter objects' attributes, and the request metadata. 
    remote isolated function getOauthProfileClientAuthorizationParameters(string oauthProfileName, map<string|string[]> headers = {}, *GetOauthProfileClientAuthorizationParametersQueries queries) returns OauthProfileClientAuthorizationParametersResponse|error {
        string resourcePath = string `/oauthProfiles/${getEncodedUri(oauthProfileName)}/clientAuthorizationParameters`;
        map<Encoding> queryParamEncoding = {"where": {style: FORM, explode: false}, "select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create an Authorization Parameter object.
    #
    # + oauthProfileName - The name of the OAuth profile.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Authorization Parameter object's attributes. 
    # + return - The Authorization Parameter object's attributes after being created, and the request metadata. 
    remote isolated function createOauthProfileClientAuthorizationParameter(string oauthProfileName, OauthProfileClientAuthorizationParameter payload, map<string|string[]> headers = {}, *CreateOauthProfileClientAuthorizationParameterQueries queries) returns OauthProfileClientAuthorizationParameterResponse|error {
        string resourcePath = string `/oauthProfiles/${getEncodedUri(oauthProfileName)}/clientAuthorizationParameters`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get an Authorization Parameter object.
    #
    # + oauthProfileName - The name of the OAuth profile.
    # + authorizationParameterName - The name of the authorization parameter.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The Authorization Parameter object's attributes, and the request metadata. 
    remote isolated function getOauthProfileClientAuthorizationParameter(string oauthProfileName, string authorizationParameterName, map<string|string[]> headers = {}, *GetOauthProfileClientAuthorizationParameterQueries queries) returns OauthProfileClientAuthorizationParameterResponse|error {
        string resourcePath = string `/oauthProfiles/${getEncodedUri(oauthProfileName)}/clientAuthorizationParameters/${getEncodedUri(authorizationParameterName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Replace an Authorization Parameter object.
    #
    # + oauthProfileName - The name of the OAuth profile.
    # + authorizationParameterName - The name of the authorization parameter.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Authorization Parameter object's attributes. 
    # + return - The Authorization Parameter object's attributes after being replaced, and the request metadata. 
    remote isolated function replaceOauthProfileClientAuthorizationParameter(string oauthProfileName, string authorizationParameterName, OauthProfileClientAuthorizationParameter payload, map<string|string[]> headers = {}, *ReplaceOauthProfileClientAuthorizationParameterQueries queries) returns OauthProfileClientAuthorizationParameterResponse|error {
        string resourcePath = string `/oauthProfiles/${getEncodedUri(oauthProfileName)}/clientAuthorizationParameters/${getEncodedUri(authorizationParameterName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Delete an Authorization Parameter object.
    #
    # + oauthProfileName - The name of the OAuth profile.
    # + authorizationParameterName - The name of the authorization parameter.
    # + headers - Headers to be sent with the request 
    # + return - The request metadata. 
    remote isolated function deleteOauthProfileClientAuthorizationParameter(string oauthProfileName, string authorizationParameterName, map<string|string[]> headers = {}) returns SempMetaOnlyResponse|error {
        string resourcePath = string `/oauthProfiles/${getEncodedUri(oauthProfileName)}/clientAuthorizationParameters/${getEncodedUri(authorizationParameterName)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Update an Authorization Parameter object.
    #
    # + oauthProfileName - The name of the OAuth profile.
    # + authorizationParameterName - The name of the authorization parameter.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Authorization Parameter object's attributes. 
    # + return - The Authorization Parameter object's attributes after being updated, and the request metadata. 
    remote isolated function updateOauthProfileClientAuthorizationParameter(string oauthProfileName, string authorizationParameterName, OauthProfileClientAuthorizationParameter payload, map<string|string[]> headers = {}, *UpdateOauthProfileClientAuthorizationParameterQueries queries) returns OauthProfileClientAuthorizationParameterResponse|error {
        string resourcePath = string `/oauthProfiles/${getEncodedUri(oauthProfileName)}/clientAuthorizationParameters/${getEncodedUri(authorizationParameterName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Get a list of Required Claim objects.
    #
    # + oauthProfileName - The name of the OAuth profile.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The list of Required Claim objects' attributes, and the request metadata. 
    remote isolated function getOauthProfileClientRequiredClaims(string oauthProfileName, map<string|string[]> headers = {}, *GetOauthProfileClientRequiredClaimsQueries queries) returns OauthProfileClientRequiredClaimsResponse|error {
        string resourcePath = string `/oauthProfiles/${getEncodedUri(oauthProfileName)}/clientRequiredClaims`;
        map<Encoding> queryParamEncoding = {"where": {style: FORM, explode: false}, "select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create a Required Claim object.
    #
    # + oauthProfileName - The name of the OAuth profile.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Required Claim object's attributes. 
    # + return - The Required Claim object's attributes after being created, and the request metadata. 
    remote isolated function createOauthProfileClientRequiredClaim(string oauthProfileName, OauthProfileClientRequiredClaim payload, map<string|string[]> headers = {}, *CreateOauthProfileClientRequiredClaimQueries queries) returns OauthProfileClientRequiredClaimResponse|error {
        string resourcePath = string `/oauthProfiles/${getEncodedUri(oauthProfileName)}/clientRequiredClaims`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get a Required Claim object.
    #
    # + oauthProfileName - The name of the OAuth profile.
    # + clientRequiredClaimName - The name of the ID token claim to verify.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The Required Claim object's attributes, and the request metadata. 
    remote isolated function getOauthProfileClientRequiredClaim(string oauthProfileName, string clientRequiredClaimName, map<string|string[]> headers = {}, *GetOauthProfileClientRequiredClaimQueries queries) returns OauthProfileClientRequiredClaimResponse|error {
        string resourcePath = string `/oauthProfiles/${getEncodedUri(oauthProfileName)}/clientRequiredClaims/${getEncodedUri(clientRequiredClaimName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete a Required Claim object.
    #
    # + oauthProfileName - The name of the OAuth profile.
    # + clientRequiredClaimName - The name of the ID token claim to verify.
    # + headers - Headers to be sent with the request 
    # + return - The request metadata. 
    remote isolated function deleteOauthProfileClientRequiredClaim(string oauthProfileName, string clientRequiredClaimName, map<string|string[]> headers = {}) returns SempMetaOnlyResponse|error {
        string resourcePath = string `/oauthProfiles/${getEncodedUri(oauthProfileName)}/clientRequiredClaims/${getEncodedUri(clientRequiredClaimName)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Get a list of Message VPN Access-Level Exception objects.
    #
    # + oauthProfileName - The name of the OAuth profile.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The list of Message VPN Access-Level Exception objects' attributes, and the request metadata. 
    remote isolated function getOauthProfileDefaultMsgVpnAccessLevelExceptions(string oauthProfileName, map<string|string[]> headers = {}, *GetOauthProfileDefaultMsgVpnAccessLevelExceptionsQueries queries) returns OauthProfileDefaultMsgVpnAccessLevelExceptionsResponse|error {
        string resourcePath = string `/oauthProfiles/${getEncodedUri(oauthProfileName)}/defaultMsgVpnAccessLevelExceptions`;
        map<Encoding> queryParamEncoding = {"where": {style: FORM, explode: false}, "select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create a Message VPN Access-Level Exception object.
    #
    # + oauthProfileName - The name of the OAuth profile.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Message VPN Access-Level Exception object's attributes. 
    # + return - The Message VPN Access-Level Exception object's attributes after being created, and the request metadata. 
    remote isolated function createOauthProfileDefaultMsgVpnAccessLevelException(string oauthProfileName, OauthProfileDefaultMsgVpnAccessLevelException payload, map<string|string[]> headers = {}, *CreateOauthProfileDefaultMsgVpnAccessLevelExceptionQueries queries) returns OauthProfileDefaultMsgVpnAccessLevelExceptionResponse|error {
        string resourcePath = string `/oauthProfiles/${getEncodedUri(oauthProfileName)}/defaultMsgVpnAccessLevelExceptions`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get a Message VPN Access-Level Exception object.
    #
    # + oauthProfileName - The name of the OAuth profile.
    # + msgVpnName - The name of the message VPN.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The Message VPN Access-Level Exception object's attributes, and the request metadata. 
    remote isolated function getOauthProfileDefaultMsgVpnAccessLevelException(string oauthProfileName, string msgVpnName, map<string|string[]> headers = {}, *GetOauthProfileDefaultMsgVpnAccessLevelExceptionQueries queries) returns OauthProfileDefaultMsgVpnAccessLevelExceptionResponse|error {
        string resourcePath = string `/oauthProfiles/${getEncodedUri(oauthProfileName)}/defaultMsgVpnAccessLevelExceptions/${getEncodedUri(msgVpnName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Replace a Message VPN Access-Level Exception object.
    #
    # + oauthProfileName - The name of the OAuth profile.
    # + msgVpnName - The name of the message VPN.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Message VPN Access-Level Exception object's attributes. 
    # + return - The Message VPN Access-Level Exception object's attributes after being replaced, and the request metadata. 
    remote isolated function replaceOauthProfileDefaultMsgVpnAccessLevelException(string oauthProfileName, string msgVpnName, OauthProfileDefaultMsgVpnAccessLevelException payload, map<string|string[]> headers = {}, *ReplaceOauthProfileDefaultMsgVpnAccessLevelExceptionQueries queries) returns OauthProfileDefaultMsgVpnAccessLevelExceptionResponse|error {
        string resourcePath = string `/oauthProfiles/${getEncodedUri(oauthProfileName)}/defaultMsgVpnAccessLevelExceptions/${getEncodedUri(msgVpnName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Delete a Message VPN Access-Level Exception object.
    #
    # + oauthProfileName - The name of the OAuth profile.
    # + msgVpnName - The name of the message VPN.
    # + headers - Headers to be sent with the request 
    # + return - The request metadata. 
    remote isolated function deleteOauthProfileDefaultMsgVpnAccessLevelException(string oauthProfileName, string msgVpnName, map<string|string[]> headers = {}) returns SempMetaOnlyResponse|error {
        string resourcePath = string `/oauthProfiles/${getEncodedUri(oauthProfileName)}/defaultMsgVpnAccessLevelExceptions/${getEncodedUri(msgVpnName)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Update a Message VPN Access-Level Exception object.
    #
    # + oauthProfileName - The name of the OAuth profile.
    # + msgVpnName - The name of the message VPN.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Message VPN Access-Level Exception object's attributes. 
    # + return - The Message VPN Access-Level Exception object's attributes after being updated, and the request metadata. 
    remote isolated function updateOauthProfileDefaultMsgVpnAccessLevelException(string oauthProfileName, string msgVpnName, OauthProfileDefaultMsgVpnAccessLevelException payload, map<string|string[]> headers = {}, *UpdateOauthProfileDefaultMsgVpnAccessLevelExceptionQueries queries) returns OauthProfileDefaultMsgVpnAccessLevelExceptionResponse|error {
        string resourcePath = string `/oauthProfiles/${getEncodedUri(oauthProfileName)}/defaultMsgVpnAccessLevelExceptions/${getEncodedUri(msgVpnName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Get a list of Required Claim objects.
    #
    # + oauthProfileName - The name of the OAuth profile.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The list of Required Claim objects' attributes, and the request metadata. 
    remote isolated function getOauthProfileResourceServerRequiredClaims(string oauthProfileName, map<string|string[]> headers = {}, *GetOauthProfileResourceServerRequiredClaimsQueries queries) returns OauthProfileResourceServerRequiredClaimsResponse|error {
        string resourcePath = string `/oauthProfiles/${getEncodedUri(oauthProfileName)}/resourceServerRequiredClaims`;
        map<Encoding> queryParamEncoding = {"where": {style: FORM, explode: false}, "select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create a Required Claim object.
    #
    # + oauthProfileName - The name of the OAuth profile.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Required Claim object's attributes. 
    # + return - The Required Claim object's attributes after being created, and the request metadata. 
    remote isolated function createOauthProfileResourceServerRequiredClaim(string oauthProfileName, OauthProfileResourceServerRequiredClaim payload, map<string|string[]> headers = {}, *CreateOauthProfileResourceServerRequiredClaimQueries queries) returns OauthProfileResourceServerRequiredClaimResponse|error {
        string resourcePath = string `/oauthProfiles/${getEncodedUri(oauthProfileName)}/resourceServerRequiredClaims`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get a Required Claim object.
    #
    # + oauthProfileName - The name of the OAuth profile.
    # + resourceServerRequiredClaimName - The name of the access token claim to verify.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The Required Claim object's attributes, and the request metadata. 
    remote isolated function getOauthProfileResourceServerRequiredClaim(string oauthProfileName, string resourceServerRequiredClaimName, map<string|string[]> headers = {}, *GetOauthProfileResourceServerRequiredClaimQueries queries) returns OauthProfileResourceServerRequiredClaimResponse|error {
        string resourcePath = string `/oauthProfiles/${getEncodedUri(oauthProfileName)}/resourceServerRequiredClaims/${getEncodedUri(resourceServerRequiredClaimName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete a Required Claim object.
    #
    # + oauthProfileName - The name of the OAuth profile.
    # + resourceServerRequiredClaimName - The name of the access token claim to verify.
    # + headers - Headers to be sent with the request 
    # + return - The request metadata. 
    remote isolated function deleteOauthProfileResourceServerRequiredClaim(string oauthProfileName, string resourceServerRequiredClaimName, map<string|string[]> headers = {}) returns SempMetaOnlyResponse|error {
        string resourcePath = string `/oauthProfiles/${getEncodedUri(oauthProfileName)}/resourceServerRequiredClaims/${getEncodedUri(resourceServerRequiredClaimName)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Get a list of Proxy objects.
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The list of Proxy objects' attributes, and the request metadata. 
    remote isolated function getProxies(map<string|string[]> headers = {}, *GetProxiesQueries queries) returns ProxiesResponse|error {
        string resourcePath = string `/proxies`;
        map<Encoding> queryParamEncoding = {"where": {style: FORM, explode: false}, "select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create a Proxy object.
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Proxy object's attributes. 
    # + return - The Proxy object's attributes after being created, and the request metadata. 
    remote isolated function createProxy(Proxy payload, map<string|string[]> headers = {}, *CreateProxyQueries queries) returns ProxyResponse|error {
        string resourcePath = string `/proxies`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get a Proxy object.
    #
    # + proxyName - The name of the proxy.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The Proxy object's attributes, and the request metadata. 
    remote isolated function getProxy(string proxyName, map<string|string[]> headers = {}, *GetProxyQueries queries) returns ProxyResponse|error {
        string resourcePath = string `/proxies/${getEncodedUri(proxyName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Replace a Proxy object.
    #
    # + proxyName - The name of the proxy.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Proxy object's attributes. 
    # + return - The Proxy object's attributes after being replaced, and the request metadata. 
    remote isolated function replaceProxy(string proxyName, Proxy payload, map<string|string[]> headers = {}, *ReplaceProxyQueries queries) returns ProxyResponse|error {
        string resourcePath = string `/proxies/${getEncodedUri(proxyName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Delete a Proxy object.
    #
    # + proxyName - The name of the proxy.
    # + headers - Headers to be sent with the request 
    # + return - The request metadata. 
    remote isolated function deleteProxy(string proxyName, map<string|string[]> headers = {}) returns SempMetaOnlyResponse|error {
        string resourcePath = string `/proxies/${getEncodedUri(proxyName)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Update a Proxy object.
    #
    # + proxyName - The name of the proxy.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Proxy object's attributes. 
    # + return - The Proxy object's attributes after being updated, and the request metadata. 
    remote isolated function updateProxy(string proxyName, Proxy payload, map<string|string[]> headers = {}, *UpdateProxyQueries queries) returns ProxyResponse|error {
        string resourcePath = string `/proxies/${getEncodedUri(proxyName)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }

    # Get a System Information object.
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The System Information object's attributes, and the request metadata. 
    #
    # # Deprecated
    @deprecated
    remote isolated function getSystemInformation(map<string|string[]> headers = {}, *GetSystemInformationQueries queries) returns SystemInformationResponse|error {
        string resourcePath = string `/systemInformation`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Get a list of Virtual Hostname objects.
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The list of Virtual Hostname objects' attributes, and the request metadata. 
    remote isolated function getVirtualHostnames(map<string|string[]> headers = {}, *GetVirtualHostnamesQueries queries) returns VirtualHostnamesResponse|error {
        string resourcePath = string `/virtualHostnames`;
        map<Encoding> queryParamEncoding = {"where": {style: FORM, explode: false}, "select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create a Virtual Hostname object.
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Virtual Hostname object's attributes. 
    # + return - The Virtual Hostname object's attributes after being created, and the request metadata. 
    remote isolated function createVirtualHostname(VirtualHostname payload, map<string|string[]> headers = {}, *CreateVirtualHostnameQueries queries) returns VirtualHostnameResponse|error {
        string resourcePath = string `/virtualHostnames`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get a Virtual Hostname object.
    #
    # + virtualHostname - The virtual hostname.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The Virtual Hostname object's attributes, and the request metadata. 
    remote isolated function getVirtualHostname(string virtualHostname, map<string|string[]> headers = {}, *GetVirtualHostnameQueries queries) returns VirtualHostnameResponse|error {
        string resourcePath = string `/virtualHostnames/${getEncodedUri(virtualHostname)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Replace a Virtual Hostname object.
    #
    # + virtualHostname - The virtual hostname.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Virtual Hostname object's attributes. 
    # + return - The Virtual Hostname object's attributes after being replaced, and the request metadata. 
    remote isolated function replaceVirtualHostname(string virtualHostname, VirtualHostname payload, map<string|string[]> headers = {}, *ReplaceVirtualHostnameQueries queries) returns VirtualHostnameResponse|error {
        string resourcePath = string `/virtualHostnames/${getEncodedUri(virtualHostname)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Delete a Virtual Hostname object.
    #
    # + virtualHostname - The virtual hostname.
    # + headers - Headers to be sent with the request 
    # + return - The request metadata. 
    remote isolated function deleteVirtualHostname(string virtualHostname, map<string|string[]> headers = {}) returns SempMetaOnlyResponse|error {
        string resourcePath = string `/virtualHostnames/${getEncodedUri(virtualHostname)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Update a Virtual Hostname object.
    #
    # + virtualHostname - The virtual hostname.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - The Virtual Hostname object's attributes. 
    # + return - The Virtual Hostname object's attributes after being updated, and the request metadata. 
    remote isolated function updateVirtualHostname(string virtualHostname, VirtualHostname payload, map<string|string[]> headers = {}, *UpdateVirtualHostnameQueries queries) returns VirtualHostnameResponse|error {
        string resourcePath = string `/virtualHostnames/${getEncodedUri(virtualHostname)}`;
        map<Encoding> queryParamEncoding = {"select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, headers);
    }
}
